/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

export interface paths {
    "/api/health": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get Health
         * @description Is the server up? **Cheap, and it touches nothing** — no engine, no GPU, no disk. It is what a
         *     test harness and the launcher poll while the window is still painting.
         */
        get: operations["get_health_api_health_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/gpu": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get Gpu
         * @description See `gpu_info()`. The first call costs ~200 ms and is memoised for the life of the process.
         */
        get: operations["get_gpu_api_gpu_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/settings": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Get Settings */
        get: operations["get_settings_api_settings_get"];
        /**
         * Put Settings
         * @description ⛔ Three keys, all of them paths. See `camea.settings` — a settings file that remembered an
         *     exclusion would be answering, on the user's behalf, the question the app exists to help him
         *     answer.
         */
        put: operations["put_settings_api_settings_put"];
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/fs/list": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get Fs List
         * @description List a directory's **sub-directories** — the served folder picker.
         *
         *     ⭐ **This is the route that keeps `--browser` and `--headless` usable.** The native dialogs need a
         *     pywebview window; this needs nothing. It is how the UI lets the user choose a dataset folder, a
         *     workspace folder or an export folder when there is no native dialog to open — and it is what
         *     makes the whole app drivable by Playwright.
         *
         *     ⛔ **Directories only. It never returns a file's contents, and there is no write path here.**
         */
        get: operations["get_fs_list_api_fs_list_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/datasets": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get Datasets
         * @description **The home screen.** Every dataset under the folders the user has pointed at, with no pixel
         *     loaded (~0.2 s each). An empty `roots` is the honest first-run state — the UI shows the folder
         *     picker (`GET /api/fs/list`) rather than pretending to know where his data is.
         */
        get: operations["get_datasets_api_datasets_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/datasets/scan": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Post Datasets Scan
         * @description *"Point me at a folder."* A POST, not a GET: a Windows path in a query string is an encoding
         *     trap, and scanning also updates `settings.dataset_roots`.
         *
         *     ⛔ Nothing here recognises a dataset by name. A folder is a dataset iff it has a `log.txt` and at
         *     least one `NNN.xml`. That is the whole rule.
         */
        post: operations["post_datasets_scan_api_datasets_scan_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/datasets/{key}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get Dataset
         * @description Everything sayable about a dataset **without loading a pixel**: every trial (of any type), its
         *     timestamp, its per-trial shape from its own XML, and the contiguous Snapshot blocks.
         *
         *     ⛔ **Core reports the blocks; it does not choose one.** "The run is the longest block, restricted
         *     to 512x512" is the *mosaic feature's* selection rule, and the next feature will want a different
         *     one over the same frames.
         */
        get: operations["get_dataset_api_datasets__key__get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/datasets/{key}/thumbnail.png": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get Dataset Thumbnail
         * @description One frame, **with no session**: the browser card must not cost 5 s and 340 MiB.
         *
         *     The window is computed **globally over a small sample** of this dataset's own frames, never
         *     per-frame: a per-frame stretch would over-brighten a near-empty acquisition and make the card lie
         *     about which datasets are dim. (~10 ms, then cached.)
         */
        get: operations["get_dataset_thumbnail_api_datasets__key__thumbnail_png_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/sessions": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Get Sessions */
        get: operations["get_sessions_api_sessions_get"];
        put?: never;
        /**
         * Post Sessions
         * @description Open a dataset -> **202 `JobRef`**. Poll `GET /api/jobs/{id}`; `result` is an `OpenJobResult`.
         *
         *     ⛔ **NOTHING IS EVER DROPPED BY TRIAL NUMBER.** A frame leaves the selection for exactly two
         *     reasons, both facts about the file on disk — it is `not_snapshot` (no readable 1-frame .dat/.xml
         *     pair) or it is `off_shape` — and every one of them is reported in `SessionResponse.skipped` with
         *     its reason. **The app has no exclusion list, and this route is where one would have gone.**
         *
         *     `trials: null` ⇒ every snapshot trial. If they are not all one shape the open is refused with
         *     **409 `mixed_shape`**, listing the groups: a FrameStore holds ONE shape, and *which one you meant*
         *     is a decision core is not entitled to make for you.
         *
         *     ⚠️ The shape check runs **before** the job is submitted (`open_dataset` is ~0.2 s and touches no
         *     pixels), so `mixed_shape` is a real 409 the client can act on, and not a job that fails 5 s later.
         */
        post: operations["post_sessions_api_sessions_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/sessions/{session_id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Get Session */
        get: operations["get_session_api_sessions__session_id__get"];
        put?: never;
        post?: never;
        /**
         * Delete Session
         * @description Frees the stack (~340 MiB, plus the band stack's second copy).
         */
        delete: operations["delete_session_api_sessions__session_id__delete"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/sessions/{session_id}/log": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get Session Log
         * @description ⚠️ `log.txt` prints the date **only** on `New experiment:` lines; every other line carries
         *     `HH:MM:SS` alone. The parser carries the date forward and rolls it over at midnight — get that
         *     wrong and you inject a -86,000 s gap into the timing-split rule.
         */
        get: operations["get_session_log_api_sessions__session_id__log_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/sessions/{session_id}/texture": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get Session Texture
         * @description ⭐ **THE MEASUREMENT, AND IT IS CORE.** `std(DoG(3,30))` answers *"how much texture is in this
         *     image"*, which is a property of the FRAME — every future feature asks it.
         *
         *     ⛔ **There is no threshold here, no list, and no policy.** Turning a number into a *proposal* is a
         *     feature's job (`POST /api/mosaic/screen/propose`); turning a proposal into a *decision* is the
         *     human's, and it lands in his document. Never in the session.
         */
        get: operations["get_session_texture_api_sessions__session_id__texture_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/sessions/{session_id}/tone": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Get Tone */
        get: operations["get_tone_api_sessions__session_id__tone_get"];
        /**
         * Put Tone
         * @description ⚠️⚠️ **GLOBAL, NEVER PER-TILE.** A per-tile percentile stretch over-brightens near-empty frames
         *     and makes overlapping tiles disagree in brightness — **which destroys the Difference-mode check
         *     the whole verification loop depends on.** There is no per-tile path and there must never be one.
         *
         *     Tone is display-only: it never touches the matcher (which sees band-passed, mean-subtracted
         *     pixels) and it never touches the exported TIFF.
         */
        put: operations["put_tone_api_sessions__session_id__tone_put"];
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/sessions/{session_id}/tiles/{trial}.png": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get Tile Png
         * @description 8-bit grayscale PNG: flat-fielded, through the **one global** tone window.
         */
        get: operations["get_tile_png_api_sessions__session_id__tiles__trial__png_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/sessions/{session_id}/tiles/{trial}.raw": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get Tile Raw
         * @description uint16 **little-endian**, row-major, **ALREADY FLIPPED** per this acquisition's XML, and
         *     **RAW CAMERA COUNTS** — no flat-field, no tone. This is pixel data, not a picture.
         */
        get: operations["get_tile_raw_api_sessions__session_id__tiles__trial__raw_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/sessions/{session_id}/thumbs.png": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get Thumbs Png
         * @description The contact sheet: ONE sprite sheet, `grid = ceil(sqrt(n))`, row-major in trial order.
         *     Same **global** window — a per-tile stretch here would make the sheet lie about which frames are
         *     dim, which is the one thing the Screen step exists to show him.
         */
        get: operations["get_thumbs_png_api_sessions__session_id__thumbs_png_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/sessions/{session_id}/thumbs.json": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get Thumbs Json
         * @description Cell `i` holds `trials[i]` at `(x, y) = ((i % grid) * cell, (i // grid) * cell)`.
         */
        get: operations["get_thumbs_json_api_sessions__session_id__thumbs_json_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/workspace": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get Workspace
         * @description `writable` is **proved, not assumed** (it writes and deletes a probe file): a workspace on a
         *     drive that has been unplugged must show up on the Load screen as `writable: false`, not as a 500
         *     an hour into a sweep.
         */
        get: operations["get_workspace_api_workspace_get"];
        /**
         * Put Workspace
         * @description ⛔ **NEVER inside a dataset. NEVER inside the repo.** Both are refused here, at the one moment
         *     the user names the place — a workspace under the checkout would be committed, `.gitignore`d, or
         *     blown away by a clean. His work does not live in the app's source tree.
         */
        put: operations["put_workspace_api_workspace_put"];
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/workspace/analyses": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Get Analyses */
        get: operations["get_analyses_api_workspace_analyses_get"];
        put?: never;
        /**
         * Post Analyses
         * @description ⭐ **THE SERVER CREATES THE DOCUMENT**, via the feature's `new_payload` hook.
         *
         *     🔴 In v1 `new_doc()` and `seed_from_build()` were **dead code** — the front end reimplemented both
         *     in JavaScript. That is how `human_edits`' divert counters were silently dropped on every save, and
         *     how *"Skip — place by hand"* could erase `seeded_from` while every tile still sat exactly where
         *     t33 put it. **The document is authored on the server. It is not negotiable.**
         *
         *     ⭐ The document's `id` **is** the `analysis_id` — `workspace._guard_slot` refuses any document
         *     whose `id` is not the analysis it is being written into, and that is what makes v1's autosave slot
         *     collision (pass 2 silently overwriting pass 1's ground-truth records) impossible rather than
         *     merely unlikely.
         */
        post: operations["post_analyses_api_workspace_analyses_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/workspace/analyses/{analysis_id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /** Delete Analysis */
        delete: operations["delete_analysis_api_workspace_analyses__analysis_id__delete"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/analyses/{analysis_id}/document": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get Document
         * @description `recovered=true` reads the **autosave** instead — ask `Workspace.recovery()` first: an autosave
         *     NEWER than the document is a recovery prompt, an older one is noise.
         */
        get: operations["get_document_api_analyses__analysis_id__document_get"];
        /**
         * Put Document
         * @description ⚠️ **THE ORDER, ON THE SERVER: structural-validate -> normalise -> stamp -> full-validate ->
         *     write, atomically.** Not `validate -> normalise`: the derived fields are exactly the ones that
         *     drift the moment the user excludes a tile, and `normalise` is what repairs them — validating them
         *     first rejects a perfectly good document for drift the very next line of code fixes.
         *     (`core.document.prepare` owns that order; this route only picks the destination.)
         */
        put: operations["put_document_api_analyses__analysis_id__document_put"];
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/analyses/{analysis_id}/autosave": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Post Autosave
         * @description The crash net. Debounced 2 s, **plus unconditionally on every `A` and `E`**.
         *
         *     🔴 **A FAILURE IS LOUD** — it is never swallowed. (`localStorage` failed *silently* in the sandbox
         *     and nearly cost a day's work; that is why the crash net is a server file at all.)
         *
         *     🔴 **THE SLOT GUARD RUNS HERE, AND IN v1 IT DID NOT** — `project.autosave()` carried it and nothing
         *     ever called it. Pass 2's autosave once silently overwrote pass 1's ground-truth records. A
         *     document whose `id`/`dataset_key` disagrees with the slot's is refused with `409 range_mismatch`.
         *     It is not merged, not renamed, not "repaired".
         */
        post: operations["post_autosave_api_analyses__analysis_id__autosave_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/documents/load": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Post Document Load
         * @description *"Load a project…"* — and it **must work COLD**, with no session open. That is the whole point:
         *     the app remembers nothing between launches, so this file is its only memory. Save -> quit ->
         *     load restores the session whole, and nothing else does.
         *
         *     The server bootstraps a session from the file's own `data_dir` when one is not given, then
         *     re-reads the file **against that session's scope**, so the range guard actually runs against the
         *     session the document belongs to.
         */
        post: operations["post_document_load_api_documents_load_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/documents/save-as": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Post Document Save As
         * @description **`Save…`** — a file the user names, reachable from EVERY screen (not buried behind the last
         *     step: since the app carries no dataset knowledge, this file is its only memory, and the one
         *     artefact that makes a session resumable must not be the one thing he cannot reach mid-sweep).
         *
         *     ⛔ Refused inside `data/`, inside the repo's dataset mirror, or inside the dataset itself.
         */
        post: operations["post_document_save_as_api_documents_save_as_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/documents/validate": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Post Document Validate
         * @description ⛔ **NO TRIAL NUMBER IS SPECIAL.** Nothing here may reject a document for WHICH trials it
         *     placed. The guard that used to live in v1 ("tile 284 is thrown out and carries a position") made
         *     the user's own session **unsaveable** the moment he anchored 284.
         */
        post: operations["post_document_validate_api_documents_validate_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/jobs": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Get Jobs */
        get: operations["get_jobs_api_jobs_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/jobs/{job_id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get Job
         * @description Poll at 500 ms. There is no websocket — it is one client on localhost and a poll is 0.6 ms.
         *
         *     ⚠️ **`eta_s` is only recomputed when the child prints a recognised line.** Re-anchor the UI's
         *     countdown **only when this raw number changes**; re-anchoring on every tick resets the clock and
         *     the countdown never moves. And it may **jump UP** — an honest revision beats a smooth lie.
         */
        get: operations["get_job_api_jobs__job_id__get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/jobs/{job_id}/cancel": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Post Job Cancel
         * @description Idempotent. A finished job is a **409**, not a lie.
         *
         *     A process job's cancel is `terminate()` — that is not a shortcut, it is the only cancel there is:
         *     `t33.place` runs 25 s – 10 min synchronously with no callback and nothing inside it that checks a
         *     flag.
         */
        post: operations["post_job_cancel_api_jobs__job_id__cancel_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/dialog/open-directory": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Post Dialog Open Directory */
        post: operations["post_dialog_open_directory_api_dialog_open_directory_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/dialog/open-file": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Post Dialog Open File */
        post: operations["post_dialog_open_file_api_dialog_open_file_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/dialog/save-file": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Post Dialog Save File */
        post: operations["post_dialog_save_file_api_dialog_save_file_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/mosaic/run": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Post Run
         * @description `POST /api/mosaic/run` — detect the run and the pass split, **or override them.**
         *
         *     ⭐ **NOT A SESSION RELOAD.** v1 made this a full re-open (5 s, and it invalidated the tone and
         *     the blank scan) because the run lived *on* the session. It does not any more: this is a pure
         *     function of the session's inventory, and it costs milliseconds.
         *
         *     ⚠️ `gaps` is **derived** and is returned with the run, because the caller needs it the moment
         *     the trial list changes: a "consecutive" pair across a gap is a multi-step stage jump, and the
         *     serpentine one-axis step prior does **not** hold there. Miss this and the next solve is silently
         *     poisoned.
         */
        post: operations["post_run_api_mosaic_run_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/mosaic/gaps": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Post Gaps
         * @description `POST /api/mosaic/gaps` — a **pure function over a trial list.**
         *
         *     ⭐ `engine.excluded.gaps()` is the ONLY symbol the app may import from that module — never
         *     `EXCLUDED` / `BLANK` / `BLURRY` / `usable_trials`. There is no toggle. **This route is the single
         *     place the app touches it**, it reaches it through `camea.core.dataset.gaps`, and it is called on
         *     every change to the excluded set.
         */
        post: operations["post_gaps_api_mosaic_gaps_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/mosaic/screen/propose": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Post Screen Propose
         * @description `POST /api/mosaic/screen/propose` — ⭐ **IT RECOMMENDS. THE HUMAN TICKS. NOTHING IS
         *     AUTO-EXCLUDED.**
         *
         *     The *measurement* is core's (`FrameStore.texture()` — `std(DoG(3,30))`, a property of the frame,
         *     and free because the matcher's band stack and this measure are the same array). The *policy* —
         *     "the 2nd percentile of PASS-1 texture" — is the mosaic's, because `pass 1` only exists because
         *     `pass_split` does. The *decision* is the human's and it lands in the **document**, not here.
         *
         *     ⚠️⚠️ **ZERO MARGIN AT THE BOUNDARY, AND IT IS THE WHOLE REASON THIS MAY ONLY PROPOSE.**
         *     Genuinely-blank frames and perfectly good ones **interleave** below the threshold. Measured on
         *     260620d with the refusal lifted, the four trials the scan named landed **0.24 px**, **0.18 px**,
         *     **2.07 px** and **679 px** from the human truth. Three of four are ordinary tiles. One of four is
         *     real. A rule that cannot do better than that does not get to reject anything.
         *
         *     ❌ **NO BLUR JUDGEMENT. EVER.** Across all 338 snapshots and 15 focus measures the best global
         *     blur threshold reaches **F1 = 0.37**, and variance-of-Laplacian — the textbook autofocus metric —
         *     scores **worse than chance**. No slider, no number, nowhere in the UI. Blur is his eye, in the
         *     sweep, with `E`.
         */
        post: operations["post_screen_propose_api_mosaic_screen_propose_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/mosaic/build": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Post Build
         * @description `POST /api/mosaic/build` -> **202 `JobRef`**. Poll `GET /api/jobs/{id}`.
         *
         *     It runs in a **CHILD PROCESS** and holds the **`gpu` lease** for its whole run. Both are
         *     forced, not chosen:
         *
         *     * `t33.place` runs 25 s – 10 min **synchronously**, has **no progress callback of any kind**,
         *       and has **nothing inside it that checks a flag** — so there is no cooperative cancel to hook.
         *       `proc.terminate()` is the only cancel that can work. That is the whole reason for the process.
         *     * The parent may hold ~2 GB of device memory from interactive matching and the child needs ~2 GB
         *       more. On a 4 GB card the lease is the difference between a build and an OOM — so we also
         *       **release the parent's context before spawning**.
         *
         *     `config: null` = t33's shipped **312/312 defaults**, and that is the one-button path.
         *     ⚠️ `pass_split` is the **DETECTED** value the caller sends, never t33's literal 166.
         */
        post: operations["post_build_api_mosaic_build_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/mosaic/builds/{build_id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get Build
         * @description `GET /api/mosaic/builds/{build_id}` — a finished build's full result.
         *
         *     ⚠️ `per_tile` is a **"go look here first" list, not a verdict** — and 🔴 **pass-1 tiles have no
         *     per-tile confidence at all** (t27's info is aggregate-only), while the worst tile in the shipped
         *     312/312 build (trial 127, at 9.94 px) **is a pass-1 tile.** The absence of a warning is not a
         *     clean bill of health, and the UI must say so.
         */
        get: operations["get_build_api_mosaic_builds__build_id__get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/mosaic/seed": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Post Seed
         * @description `POST /api/mosaic/seed` — apply a build to a document. **The SERVER does this, not the JS.**
         *
         *     🔴 **A RE-SOLVE MUST NOT DESTROY THE HUMAN'S WORK.** `document.seed_from_build` keeps every tile
         *     that is `anchored` **or** flagged `human` and seeds everything else *around* them, translating
         *     the build onto the human's field by the **MEDIAN** offset over the protected tiles — a median,
         *     not a mean, because a tile the human corrected *because the solver was wrong* is precisely an
         *     outlier, and one 2,969 px correction would drag a mean into nonsense. If none of the human's
         *     tiles are in the build the two frames cannot be tied together, and the server **refuses to
         *     seed** rather than guess.
         *
         *     (v1 reimplemented all of this in `sweep.js` and called `setState` unconditionally on every
         *     non-excluded tile — so a 150-tile sweep with three catastrophic hand-corrections in it was wiped
         *     by taking the app's own advice to re-solve, and the autosave then wrote the wiped document over
         *     the crash-recovery file.)
         */
        post: operations["post_seed_api_mosaic_seed_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/mosaic/document/machine-evidence": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Post Machine Evidence
         * @description ⚠️ **DERIVED FROM THE DOCUMENT'S HISTORY, NEVER FROM WHAT IT SAYS ABOUT ITSELF** — a `build`
         *     block, or a single tile still carrying a `machine` position. `seeded_from` is writable, and
         *     "Skip — place from scratch" once *erased it* while every tile kept t33's answer.
         *
         *     🔴 **This project has already destroyed one benchmark exactly this way**
         *     (`analysis/archive/challenge_2026-07/benchmark/ground_truth/260620d.json` **is T27's own
         *     output**). Powers the provenance panel — which is a **live warning about current state**, and so
         *     it stays on the page rather than hiding behind a `?`.
         */
        post: operations["post_machine_evidence_api_mosaic_document_machine_evidence_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/mosaic/document/discard-machine": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Post Discard Machine
         * @description *"Skip — place by hand"*. 🔴 **IT IS DESTRUCTIVE, OR IT IS NOTHING.**
         *
         *     If anything in the document came from a machine it discards **every position and the build**.
         *     Otherwise it launders a machine build into an "independent ground truth": v1 nulled `build`,
         *     nulled `seeded_from`, set `independent_of_method: true`, deleted the warning — and **did not
         *     touch a single tile.** Every tile kept t33's position. Score t33 against that and it gets ~100 %
         *     **by construction.**
         *
         *     `confirm` is `Literal[True]` in the schema: there is no path through this route by accident.
         */
        post: operations["post_discard_machine_api_mosaic_document_discard_machine_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/mosaic/match/anchor": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Post Match Anchor
         * @description ⭐⭐ **THE PRIMITIVE. ONE ENDPOINT, FOUR FEATURES**: place the next tile (`Space`), the ranked
         *     alternatives (`V`, `1`–`9`), rescue an unplaced tile, and snap a drag (`mode: "local"`).
         *
         *     *Why a composite is the right primitive and not merely a convenient one:* of 719 genuinely
         *     overlapping tile PAIRS on this data the exact-NCC argmax is **> 20 px wrong for 38 of them (5 %),
         *     at scores up to 0.760** — one 0.760 winner is **757 px wrong** and the truth is the runner-up at
         *     0.677. The **same tile against a composite** scores 0.654 vs 0.416 next-best and lands **1.7 px**
         *     from the human. **Aperture is everything**, and the anchor field the user is building is a big,
         *     human-certified one.
         *
         *     ⛔ **BLANK ⇒ REFUSED, NOT SCORED.** 200, with `candidates: []`, `best: null` and a populated
         *     `refused`. **There is no `force` flag and there will not be one.** Two blank frames 136 trials
         *     apart correlate **+0.43 at zero shift** (honest noise floor 0.115) because what they share is
         *     *fixed-pattern sensor structure*, which does not move with the stage — **they register
         *     confidently and wrongly.** A blank *anchor*, by contrast, is **dropped from the composite and is
         *     not fatal**: the rule that made it an error dead-ended the whole app, the moment the user
         *     anchored the first near-threshold trial the sweep refused **forever**, and it died one tile later.
         *
         *     ⭐ **A plain `def`, not `async def`** — Starlette runs it in its thread pool, which is what stops
         *     an in-flight prefetch from blocking the foreground request the user is actually waiting on.
         */
        post: operations["post_match_anchor_api_mosaic_match_anchor_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/mosaic/match/score": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Post Match Score
         * @description `POST /api/mosaic/match/score` — *"you dropped it here; what do the pixels say?"*
         *
         *     One `exact_ncc`, no search (~31 ms). Used live during a drag (debounced), to label the
         *     alternatives, and to **re-measure a diverted tile's NCC where it actually sits** — because
         *     `TileRecord.ncc` is defined as the NCC **at the tile's final position**, on every path, and
         *     writing the rejected alias's score there would attribute it to the position that shipped.
         *
         *     ⚠️ `ncc` is **`null`** below `exact_ncc`'s overlap floor. The honest answer is *"not
         *     measurable"* — **never `0.0`**.
         *
         *     ⭐ Deliberately **NOT** serialised behind an in-flight `match/anchor`: it used to wait out a whole
         *     match, so the live NCC under the cursor was up to a full match out of date.
         */
        post: operations["post_match_score_api_mosaic_match_score_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/mosaic/recheck": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Post Recheck
         * @description `POST /api/mosaic/recheck` -> **202 `JobRef`**. It is N x ~1 s, so it never blocks.
         *
         *     🔴 **THE RE-CHECK IS GLOBAL, AND IT IS ALLOWED TO SAY NO.** It **never moves a tile** — it only
         *     measures it. Anything that disagrees with where the tile sits by more than `tolerance_px`
         *     **STAYS FLAGGED**, and the caller clears only the flags it is told it may clear.
         *
         *     ⚠️ **IT MUST BE GLOBAL, AND THIS IS WHY.** v1 fired a *local* match (±64 px) and then cleared
         *     `stale` unconditionally. But a tile knocked off by a moved anchor is off by **hundreds** of px —
         *     failure on this data is **binary**, sub-pixel-right or wildly wrong — so a ±64 px window is
         *     *structurally blind to the one error the panel exists for*. Measured: a tile **380 px** from the
         *     truth re-checked locally to `ncc -0.0678`, was not moved, and had its flag **cleared** — the loud
         *     panel disappeared, the tile stayed 380 px wrong, and a negative NCC went into the document as its
         *     evidence.
         */
        post: operations["post_recheck_api_mosaic_recheck_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/mosaic/export": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Post Export
         * @description `POST /api/mosaic/export` -> **202 `JobRef`**. Holds the `gpu` lease.
         *
         *     ⭐⭐ **TIFF AND PNG ARE TWO DIFFERENT RENDERS**, and `flat` decides what the pixels *are*:
         *     `flat=True` is a *picture* (per-tile gain); `flat=False` is **raw camera counts** = the TIFF. The
         *     TIFF was once rendered `flat=True` while its header said RAW — trial 11's median went 2111 ->
         *     3435 (x1.63).
         *
         *     ⚠️⚠️ **THE COVERAGE MASK IS MANDATORY, NOT AN OPTION.** 13.1 % of the canvas is background
         *     encoded as exactly `0.0`, indistinguishable from a legitimately black pixel, and a TIFF has **no
         *     alpha channel**. Without the sidecar, "empty" and "black" merge forever. Asking for `tiff`
         *     implies `coverage`; the exporter writes it either way.
         *
         *     🔴 The export describes the document **AS EXPORTED** (the *stamped* one), never as posted. A
         *     laundered doc once produced a GT JSON saying "NOT AN INDEPENDENT GROUND TRUTH" beside a TIFF
         *     header saying "hand-placed from scratch".
         */
        post: operations["post_export_api_mosaic_export_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/mosaic/qc": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Post Qc
         * @description `POST /api/mosaic/qc` — what the human did vs what the machine said.
         *
         *     ⭐ **EVERY NUMBER STATES ITS DENOMINATOR**, and the denominator is the document's own `excluded`
         *     set and nothing else — the app has no list of its own. (A percentage without one is how the
         *     182-vs-156 confusion started.)
         *
         *     ⚠️ `diverted_to_solver` is counted **separately and named next to the number it contaminates**: a
         *     diverted tile *is* sitting exactly on the machine's position, so it lands inside
         *     `accepted_unchanged` and reads as agreement. It is not. The app never gave the matcher a vote
         *     there. In the defer flow this can be **302 of 311 tiles**.
         */
        post: operations["post_qc_api_mosaic_qc_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
}
export type webhooks = Record<string, never>;
export interface components {
    schemas: {
        /** AnalysisListResponse */
        AnalysisListResponse: {
            /** Workspace */
            workspace: string | null;
            /** Analyses */
            analyses: components["schemas"]["AnalysisSummary"][];
        };
        /**
         * AnalysisRef
         * @description A card on the dataset's tile in the browser: "you already have work here".
         */
        AnalysisRef: {
            /** Analysis Id */
            analysis_id: string;
            /** Feature */
            feature: string;
            /** Name */
            name: string;
            /** Modified */
            modified: string;
            /** N Anchored */
            n_anchored?: number | null;
            /** N Excluded */
            n_excluded?: number | null;
        };
        /**
         * AnalysisSummary
         * @description An analysis = a document + its outputs, in the workspace. It is bound to ONE dataset.
         */
        AnalysisSummary: {
            /** Analysis Id */
            analysis_id: string;
            /**
             * Feature
             * @description "mosaic". Features register their own name.
             */
            feature: string;
            /** Name */
            name: string;
            /** Dataset Key */
            dataset_key: string;
            /** Dataset */
            dataset: string;
            /** Path */
            path: string;
            /** Created */
            created: string;
            /** Modified */
            modified: string;
            /** Bytes */
            bytes: number;
            /** N Tiles */
            n_tiles?: number | null;
            /** N Anchored */
            n_anchored?: number | null;
            /** N Excluded */
            n_excluded?: number | null;
            /**
             * Independent Of Method
             * @description ⚠️ Derived from the document's HISTORY, never from what it claims about itself. Shown on the card so a seeded document can never be mistaken for a truth.
             */
            independent_of_method?: boolean | null;
        };
        /** AppStamp */
        AppStamp: {
            /** Name */
            name: string;
            /** Version */
            version: string;
        };
        /**
         * AutosaveRequest
         * @description `POST /api/analyses/{analysis_id}/autosave`. Debounced 2 s, **plus unconditionally on every
         *     `A` and `E`**. 🔴 **A FAILURE IS LOUD** — never swallowed. (`localStorage` failed *silently* in
         *     the sandbox and nearly cost a day's work. Autosave is a server file.)
         *
         *     🔴 **THE RANGE GUARD RUNS HERE**, and in v1 it did not: `autosave()` carried it and nothing
         *     called `autosave()`. Pass 2's autosave once silently overwrote pass 1's ground-truth records,
         *     and the autosave slot keys on the DIRECTORY, not the trial range — so the collision is still
         *     open. A document whose range/dataset disagrees with the slot's is refused with
         *     `409 range_mismatch`. It is not merged, not renamed, not "repaired".
         */
        AutosaveRequest: {
            doc: components["schemas"]["Document"];
        };
        /**
         * BlankProposal
         * @description ⭐ **IT RECOMMENDS. IT NEVER REJECTS.**
         *
         *     ⚠️ **ZERO MARGIN AT THE BOUNDARY.** Genuinely-blank frames and perfectly good ones *interleave*
         *     below the threshold. Measured with the refusal lifted, three of the four near-threshold trials
         *     land 0.24 / 0.18 / 2.07 px from the human truth — they are **ordinary tiles** — while the fourth
         *     lands **679 px wrong**. One of four. That is exactly why the measure may only ever propose.
         *
         *     ❌ **NO BLUR JUDGEMENT. EVER.** Across 15 focus measures the best global blur threshold reaches
         *     F1 = 0.37, and variance-of-Laplacian scores **worse than chance**. No slider, no number, nowhere
         *     in the UI. Blur is his eye, in the sweep, with `E`.
         *
         *     ⛔ There is no `BLANK_THRESHOLD` fallback constant. The threshold is **computed from the frames
         *     in front of it, every time.** A measured number carried in the source would be dataset knowledge.
         */
        BlankProposal: {
            /** Threshold */
            threshold: number | null;
            /** Threshold Source */
            threshold_source: string;
            /** Measure */
            measure: string;
            /** Texture */
            texture: {
                [key: string]: number;
            };
            /** Proposed */
            proposed: number[];
            /** N Proposed */
            n_proposed: number;
            /** N Scanned */
            n_scanned: number;
            /** Margin Warning */
            margin_warning?: string | null;
        };
        /**
         * BlankProposeRequest
         * @description `POST /api/mosaic/screen/propose`. Needs `pass_split` because the threshold is defined in
         *     mosaic's terms: *the 2.0th percentile of PASS-1 texture*.
         */
        BlankProposeRequest: {
            /** Session Id */
            session_id: string;
            /** Trials */
            trials: number[];
            /** Pass Split */
            pass_split?: number | null;
        };
        /**
         * BlankScanBlock
         * @description ⭐ **THE SCAN RECOMMENDS. THE USER TICKS. NOTHING IS AUTO-EXCLUDED.**
         *
         *     `blank` is the DECISION — the list the human left standing, which the matcher will refuse. It
         *     lives **here, in the document**, and it travels to the matcher **in the request body**
         *     (`MatchAnchorRequest.refuse`). It is *not* server state. (In v1 it was, and that single field
         *     is what made the match endpoints impure.)
         */
        BlankScanBlock: {
            /** Threshold */
            threshold?: number | null;
            /**
             * Measure
             * @default std of DoG(sigma=3, sigma=30) of the frame as read
             */
            measure: string;
            /** Blank */
            blank?: number[];
            /** Scanned */
            scanned?: number[];
            /**
             * Accepted
             * @default false
             */
            accepted: boolean;
            /**
             * Overruled By User
             * @description Frames the scan proposed and the human pressed Keep on.
             */
            overruled_by_user?: number[];
        } & {
            [key: string]: unknown;
        };
        /**
         * BuildBlock
         * @description The build, as recorded in the document.
         *
         *     ⭐ `trials` + `gaps` are **what the solver was actually given**. Without them the document can
         *     never know its build was solved on a *different* input, and the staleness check degenerates into
         *     comparing the current trial list with itself. **A build with no recorded `trials` is treated as
         *     STALE** — the backend refuses to guess.
         */
        BuildBlock: {
            /** Build Id */
            build_id?: string | null;
            /**
             * Method
             * @default t33
             */
            method: string;
            /** Created */
            created?: string | null;
            /** Seconds */
            seconds?: number | null;
            /** Gpu */
            gpu?: boolean | null;
            /**
             * N Placed
             * @default 0
             */
            n_placed: number;
            /** Config */
            config?: {
                [key: string]: number | boolean | string | {
                    [key: string]: number | boolean | string | null;
                } | (number | boolean | string | null)[] | null;
            };
            info?: components["schemas"]["BuildInfo"] | null;
            /** Positions */
            positions?: {
                [key: string]: [
                    number,
                    number
                ];
            };
            /** Trials */
            trials?: number[];
            /** Gaps */
            gaps?: [
                number,
                number
            ][];
            /**
             * Stale
             * @default false
             */
            stale: boolean;
            /** Stale Reason */
            stale_reason?: string | null;
        } & {
            [key: string]: unknown;
        };
        /**
         * BuildConfig
         * @description The Advanced drawer. ⚠️ The drawer must say: *"Off the validated path. The shipped 312/312
         *     build used the defaults."*
         *
         *     ⚠️ **`pass_split` defaults to the session's DETECTED value, not to t33's literal 166.**
         *     Validation is done by **constructing** a real `t33.Config` — it raises `TypeError` on an unknown
         *     knob itself. ⛔ Do not hard-code the knob list here; `extra="allow"` is what lets a new t33 knob
         *     through without a schema change.
         */
        BuildConfig: {
            /** Pass Split */
            pass_split?: number | null;
            /** Anchor Ncc */
            anchor_ncc?: number | null;
            /** Split Px */
            split_px?: number | null;
            /** Look */
            look?: number | null;
            /** Min Side */
            min_side?: number | null;
            t27?: components["schemas"]["T27Config"] | null;
        } & {
            [key: string]: unknown;
        };
        /**
         * BuildInfo
         * @description t33's own `info` dict, made JSON-safe. ⚠️ `info["config"]` holds a nested `t27.Config` and
         *     `json.dumps(info)` **crashes** without a coercer. And a **warm cache hit hands back a plain
         *     dict**, not a `t33.Config` — do not key the coercion on `hasattr(cfg, "t27_config")`, or it is a
         *     no-op on every cached build, which is the common case.
         */
        BuildInfo: {
            /** Seconds */
            seconds?: number | null;
            /** Gpu */
            gpu?: boolean | null;
        } & {
            [key: string]: unknown;
        };
        /**
         * BuildResult
         * @description `Job.result` when `kind == "build"`. Also `GET /api/mosaic/builds/{build_id}`.
         */
        BuildResult: {
            /**
             * @description discriminator enum property added by openapi-typescript
             * @enum {string}
             */
            kind: "build";
            /** Build Id */
            build_id: string;
            /** Created */
            created: string;
            /**
             * Trials
             * @description ⭐ WHAT THE SOLVER WAS ACTUALLY GIVEN.
             */
            trials: number[];
            /**
             * Gaps
             * @description ⭐ ditto — see `BuildBlock`.
             */
            gaps: [
                number,
                number
            ][];
            /** Pass Split */
            pass_split: number;
            /** Positions */
            positions: {
                [key: string]: [
                    number,
                    number
                ];
            };
            /** N Placed */
            n_placed: number;
            /** Unplaced */
            unplaced: number[];
            /** Seconds */
            seconds: number;
            /** Gpu */
            gpu: boolean;
            info: components["schemas"]["BuildInfo"];
            /** Per Tile */
            per_tile: {
                [key: string]: components["schemas"]["PerTileEvidence"];
            };
        };
        /**
         * BuildStartRequest
         * @description `POST /api/mosaic/build` → 202 `JobRef`. Holds the **`gpu` exclusive lease** for its whole
         *     run; another job that wants it gets `409 busy`.
         *
         *     ⭐ **`trials` IS THE DOCUMENT'S ACTIVE LIST** — everything the user has *not* excluded. This is
         *     the ONLY way a frame ever leaves a build. Hard-wiring the session's run list here meant pressing
         *     `E` and then "re-solve" re-solved the **identical problem** and put the excluded frame straight
         *     back into the chain.
         *
         *     `config: null` = t33's shipped 312/312 defaults, and that is the one-button path.
         *
         *     `use_cache` is a **speed** switch, not a correctness one (warm ≈ 25 s vs cold ≈ 3 min). t33's
         *     cache filename carries a hash of the trial list + config and a mismatch is **refused, not
         *     repaired** — it cannot serve a stale answer for a different input.
         */
        BuildStartRequest: {
            /** Session Id */
            session_id: string;
            /** Trials */
            trials: number[];
            /** Pass Split */
            pass_split: number;
            config?: components["schemas"]["BuildConfig"] | null;
            /**
             * Use Cache
             * @default true
             */
            use_cache: boolean;
            /** Analysis Id */
            analysis_id?: string | null;
        };
        /**
         * Candidate
         * @description A peak. `x`/`y` are **world top-left positions** — the server has already done the
         *     `m0 + (dx, dy)` conversion; the front end never sees a composite coordinate.
         *
         *     ⚠️ `npix = 0` from a tier-A candidate means **"not measured"**, not "no overlap". Pass it
         *     through untouched.
         */
        Candidate: {
            /**
             * Rank
             * @description 0-indexed. **DISPLAYED as +1** — key `1` is rank 0.
             */
            rank: number;
            /** X */
            x: number;
            /** Y */
            y: number;
            /** Ncc */
            ncc: number | null;
            /** Npix */
            npix: number;
            /** Subpixel */
            subpixel: boolean;
        };
        /** CompositeInfo */
        CompositeInfo: {
            /** W */
            w: number;
            /** H */
            h: number;
            /** Valid Px */
            valid_px: number;
            /** M0 */
            m0: [
                number,
                number
            ];
        };
        /**
         * CreateAnalysisRequest
         * @description ⭐ **THE SERVER CREATES THE DOCUMENT**, via the feature's `new_document` hook.
         *
         *     🔴 In v1 `project.new_doc()` and `project.seed_from_build()` were **dead code** — the front end
         *     reimplemented both in JS. That is how `human_edits`' divert counters were silently dropped on
         *     every save, and how "Skip — place by hand" could erase `seeded_from` while every tile still sat
         *     exactly where t33 put it. The document is authored on the server. It is not negotiable.
         */
        CreateAnalysisRequest: {
            /** Session Id */
            session_id: string;
            /** Feature */
            feature: string;
            /** Name */
            name: string;
            /**
             * Trials
             * @description The feature's selection. null = the session's whole trial list.
             */
            trials?: number[] | null;
        };
        /**
         * DatasetDetail
         * @description Everything the browser can say about a dataset **without loading a pixel**.
         */
        DatasetDetail: {
            summary: components["schemas"]["DatasetSummary"];
            /** Trials */
            trials: components["schemas"]["TrialMeta"][];
            /**
             * Blocks
             * @description Contiguous runs of Snapshot trials. A feature selects from these; core does not choose for it.
             */
            blocks: components["schemas"]["SnapshotBlock"][];
        };
        /** DatasetListResponse */
        DatasetListResponse: {
            /** Roots */
            roots: string[];
            /** Datasets */
            datasets: components["schemas"]["DatasetSummary"][];
            /**
             * Skipped
             * @description Folders that looked like a dataset but could not be read.
             */
            skipped?: string[];
        };
        /**
         * DatasetScanRequest
         * @description `POST /api/datasets/scan` — the user points at a folder. (A POST, not a GET: Windows paths
         *     in query strings are an encoding trap, and scanning also updates `settings.dataset_roots`.)
         */
        DatasetScanRequest: {
            /** Root */
            root: string;
            /**
             * Depth
             * @default 3
             */
            depth: number;
            /**
             * Remember
             * @default true
             */
            remember: boolean;
        };
        /**
         * DatasetSummary
         * @description One card in the browser.
         */
        DatasetSummary: {
            /**
             * Key
             * @description Stable id: basename + sha1 of the RESOLVED directory. ⭐ Two folders both named `260620d` under different parents must not share a cache file or an autosave slot.
             */
            key: string;
            /**
             * Name
             * @description The folder's basename.
             */
            name: string;
            /** Path */
            path: string;
            /**
             * Experiment
             * @description The acquisition's own name (log.txt `New experiment:`), which may differ from the folder's. Recorded because a directory label can lie. Nothing branches on it.
             * @default
             */
            experiment: string;
            /**
             * N Trials
             * @description Every trial in log.txt, of any type.
             */
            n_trials: number;
            /** N Snapshots */
            n_snapshots: number;
            /** First At */
            first_at?: string | null;
            /** Last At */
            last_at?: string | null;
            /** Shapes */
            shapes?: components["schemas"]["ShapeGroup"][];
            /**
             * Thumbnail Url
             * @description GET /api/datasets/{key}/thumbnail.png — one frame, no session.
             */
            thumbnail_url?: string | null;
            /** Analyses */
            analyses?: components["schemas"]["AnalysisRef"][];
            /** Last Opened */
            last_opened?: string | null;
            /** Warnings */
            warnings?: string[];
        };
        /** DialogOpenDirectoryRequest */
        DialogOpenDirectoryRequest: {
            /**
             * Title
             * @default Choose a directory
             */
            title: string;
            /** Start */
            start?: string | null;
        };
        /** DialogOpenFileRequest */
        DialogOpenFileRequest: {
            /**
             * Title
             * @default Open a file
             */
            title: string;
            /** Filters */
            filters?: string[];
        };
        /** DialogPathResponse */
        DialogPathResponse: {
            /**
             * Path
             * @description null = the user cancelled.
             */
            path?: string | null;
        };
        /** DialogSaveFileRequest */
        DialogSaveFileRequest: {
            /**
             * Title
             * @default Save
             */
            title: string;
            /** Default Name */
            default_name?: string | null;
            /** Filters */
            filters?: string[];
        };
        /**
         * DiscardMachineRequest
         * @description `POST /api/mosaic/document/discard-machine` — *"Skip — place by hand"*.
         *
         *     🔴 **IT IS DESTRUCTIVE, OR IT IS NOTHING.** If anything in the document came from a machine it
         *     discards **every position and the build** (and `machine`, `ncc`, `margin`, `seq`, `human`,
         *     `source`). Otherwise it launders a machine build into an "independent ground truth": v1 nulled
         *     `build`, nulled `seeded_from`, set `independent_of_method: true`, deleted the warning — and
         *     **did not touch a single tile.** Every tile kept t33's position. **Score t33 against that and it
         *     gets ~100 % by construction. That is the exact mechanism that already destroyed one benchmark in
         *     this project.**
         *
         *     Call `machine-evidence` first to name the count in the confirm.
         */
        DiscardMachineRequest: {
            doc: components["schemas"]["MosaicDocument"];
            /**
             * Confirm
             * @description Must be literally `true`. There is no path through this route by accident.
             * @constant
             */
            confirm: true;
        };
        /** DiscardMachineResponse */
        DiscardMachineResponse: {
            doc: components["schemas"]["MosaicDocument"];
            /** N Positions Discarded */
            n_positions_discarded: number;
            /** Had Build */
            had_build: boolean;
        };
        /**
         * Document
         * @description The generic envelope. **What every analysis has, whatever the feature is.**
         *
         *     Unknown top-level keys — including a whole feature payload — are preserved **verbatim**.
         */
        Document: {
            /** Schema Version */
            schema_version: string;
            app: components["schemas"]["AppStamp"];
            /** Id */
            id: string;
            /** Feature */
            feature: string;
            /** Dataset */
            dataset: string;
            /**
             * Experiment
             * @default
             */
            experiment: string;
            /** Data Dir */
            data_dir: string;
            /** Dataset Key */
            dataset_key: string;
            /** Created */
            created: string;
            /** Modified */
            modified: string;
            provenance: components["schemas"]["Provenance"];
        } & {
            [key: string]: unknown;
        };
        /** DocumentProblem */
        DocumentProblem: {
            /** Kind */
            kind: string;
            /** Message */
            message: string;
            /**
             * Severity
             * @default error
             * @enum {string}
             */
            severity: "error" | "warning";
        };
        /** DocumentResponse */
        DocumentResponse: {
            doc: components["schemas"]["Document"];
        };
        /** DroppedTrial */
        DroppedTrial: {
            /** Trial */
            trial: number;
            /**
             * Reason
             * @enum {string}
             */
            reason: "off_shape" | "not_snapshot";
            /** W */
            w?: number | null;
            /** H */
            h?: number | null;
        };
        /**
         * ExportRequest
         * @description `POST /api/mosaic/export` → 202 `JobRef`. Holds the `gpu` lease.
         *
         *     **What is rendered:** `anchored`, plus `unverified` **iff `include_unverified`**. `excluded` and
         *     `unplaced` are **never** rendered. Positions are normalised so `origin_trial` sits at exactly
         *     `(0, 0)`, matching `analysis/ground_truth/`.
         *
         *     `render_mode` default **feather** — the only interactive one (feather 1.11 s; median 41.7 s;
         *     alpha 74 s).
         *
         *     ⭐⭐ **TIFF AND PNG ARE TWO DIFFERENT RENDERS.** The `flat` flag decides what the pixels *are*:
         *     `flat=True` is a *picture* (per-tile gain); `flat=False` is **raw camera counts** = the TIFF.
         *     The TIFF was once rendered `flat=True` while its header said RAW — trial 11's median went
         *     2111 → 3435 (×1.63).
         *
         *     🔴 The export must describe the document **AS EXPORTED** (the *stamped* one), not as posted. A
         *     laundered doc once produced a GT JSON saying "NOT AN INDEPENDENT GROUND TRUTH" beside a TIFF
         *     header saying "hand-placed from scratch".
         */
        ExportRequest: {
            /** Session Id */
            session_id: string;
            /** Dir */
            dir: string;
            /** Basename */
            basename: string;
            doc: components["schemas"]["MosaicDocument"];
            /** Outputs */
            outputs?: ("tiff" | "coverage" | "png" | "positions" | "gt" | "qc")[];
            /**
             * Render Mode
             * @default feather
             * @enum {string}
             */
            render_mode: "feather" | "median" | "alpha";
            /**
             * Include Unverified
             * @default true
             */
            include_unverified: boolean;
            /**
             * Um Per Px
             * @description 📏 See `Scale`. Pixels only by default.
             */
            um_per_px?: number | null;
        };
        /**
         * ExportResult
         * @description `Job.result` when `kind == "export"`. **7 files.**
         *
         *     ⚠️⚠️ **THE COVERAGE MASK IS MANDATORY**, not an option: **13.1 % of the canvas is background
         *     encoded as exactly `0.0`**, indistinguishable from a legitimately black pixel, and a TIFF has
         *     **no alpha channel**. Without the sidecar, "empty" and "black" merge forever. It is free
         *     (`wsum > 0` in the feather path). Asking for `tiff` implies `coverage`.
         *
         *     `positions.csv`'s header is **exactly** `trial,x,y,state` — `score.load_positions` DictReads the
         *     first three. The GT JSON is asserted **scoreable** (`tiles[k].status`, `tolerance_px.region_default`)
         *     before it leaves the process.
         */
        ExportResult: {
            /**
             * @description discriminator enum property added by openapi-typescript
             * @enum {string}
             */
            kind: "export";
            /** Files */
            files: components["schemas"]["ExportedFile"][];
            /** @description The document **as exported** — stamped. Not as posted. */
            doc: components["schemas"]["MosaicDocument"];
            /** Warnings */
            warnings?: string[];
        };
        /** ExportedFile */
        ExportedFile: {
            /**
             * Kind
             * @enum {string}
             */
            kind: "tiff" | "coverage" | "png" | "positions" | "gt" | "qc";
            /** Path */
            path: string;
            /** Bytes */
            bytes: number;
        };
        /**
         * FsEntry
         * @description One row in the folder picker.
         */
        FsEntry: {
            /** Name */
            name: string;
            /** Path */
            path: string;
            /**
             * Is Dataset
             * @description It has a `log.txt` and at least one `NNN.xml`. ⛔ Recognised by SHAPE, never by name — nothing in this app knows what a dataset is called.
             */
            is_dataset: boolean;
            /**
             * N Children
             * @description Sub-directories. null when the folder could not be read.
             */
            n_children?: number | null;
        };
        /**
         * FsListResponse
         * @description `GET /api/fs/list` — the served folder picker. See the section note above.
         */
        FsListResponse: {
            /** Path */
            path: string;
            /**
             * Parent
             * @description null at a filesystem root.
             */
            parent?: string | null;
            /** Is Dataset */
            is_dataset: boolean;
            /** Entries */
            entries: components["schemas"]["FsEntry"][];
            /**
             * Roots
             * @description The drives / mount points, so the picker can start from nothing.
             */
            roots: string[];
            /**
             * Error
             * @description Set when `path` itself could not be listed. Not an HTTP error — the picker must still render its roots and its parent.
             */
            error?: string | null;
        };
        /**
         * GapsRequest
         * @description `POST /api/mosaic/gaps` — a **pure function over a trial list**.
         *
         *     ⭐ `engine.excluded.gaps()` is the ONLY symbol the app may import from that module. Never
         *     `EXCLUDED` / `BLANK` / `BLURRY` / `usable_trials`. There is no toggle. This route is the single
         *     place the app touches it, and it is called on every change to the excluded set.
         */
        GapsRequest: {
            /** Trials */
            trials: number[];
        };
        /** GapsResponse */
        GapsResponse: {
            /** Gaps */
            gaps: [
                number,
                number
            ][];
        };
        /**
         * GpuInfo
         * @description 🔴 **DETECTION MUST EXECUTE A REAL OP.** `import cupy` *succeeds* on a broken CUDA install;
         *     it is `cupy.zeros(1) + 1` that raises. There is exactly **one** detector — `t27.xp()` — and
         *     there must never be a second. `reason` is a *string for the human*, never a verdict: "no GPU"
         *     because a CUDA DLL is off the search path is **fixable** and is not the same thing as "no card".
         */
        GpuInfo: {
            /** Available */
            available: boolean;
            /**
             * Backend
             * @enum {string}
             */
            backend: "cupy" | "numpy";
            /** Name */
            name?: string | null;
            /** Cupy */
            cupy?: string | null;
            /** Cuda Runtime */
            cuda_runtime?: number | null;
            /**
             * Reason
             * @description Why there is no GPU. Human-readable, and often actionable.
             */
            reason?: string | null;
            /**
             * Note
             * @description What it costs the user, in real numbers.
             */
            note?: string | null;
        };
        /** HTTPValidationError */
        HTTPValidationError: {
            /** Detail */
            detail?: components["schemas"]["ValidationError"][];
        };
        /** HealthResponse */
        HealthResponse: {
            /** Ok */
            ok: boolean;
            /** Version */
            version: string;
            /** Python */
            python: string;
            /** Uptime S */
            uptime_s: number;
        };
        /**
         * HumanEdits
         * @description The honest record of what the human actually did. Recomputed on every save. **Every number
         *     states its denominator.**
         *
         *     ⚠️ `diverted_to_solver` is counted **separately and named next to the number it contaminates**:
         *     a diverted tile *is* sitting exactly on the machine's position, so it lands inside
         *     `accepted_unchanged` and reads as agreement. It is not. The app never gave the matcher a vote
         *     there. (In the defer flow this can be 302 of 311 tiles.)
         */
        HumanEdits: {
            /**
             * Accepted Unchanged
             * @default 0
             */
            accepted_unchanged: number;
            /**
             * Moved
             * @default 0
             */
            moved: number;
            /**
             * Excluded
             * @default 0
             */
            excluded: number;
            /**
             * Unverified
             * @default 0
             */
            unverified: number;
            /**
             * Rescued
             * @default 0
             */
            rescued: number;
            /**
             * Median Move Px
             * @default 0
             */
            median_move_px: number;
            /**
             * Max Move Px
             * @default 0
             */
            max_move_px: number;
            /**
             * Diverted To Solver
             * @default 0
             */
            diverted_to_solver: number;
            /** Diverted Trials */
            diverted_trials?: number[];
            /** Diverted Note */
            diverted_note?: string | null;
        } & {
            [key: string]: unknown;
        };
        /**
         * Job
         * @description Poll at 500 ms. There is no websocket — it is one client on localhost and a poll is 0.6 ms.
         *
         *     ⚠️ **`eta_s` is only recomputed when the child prints a recognised line.** The front end must
         *     re-anchor its countdown **only when this raw number changes** (BEHAVIOUR R8.2) — re-anchoring on
         *     every 500 ms tick resets the clock and the countdown never moves. And there is deliberately **no
         *     server-side ETA heartbeat** (R8b): with a global-linear estimator, re-emitting during a silent
         *     phase makes the ETA count *up*.
         */
        Job: {
            /** Job Id */
            job_id: string;
            /** Kind */
            kind: string;
            /**
             * State
             * @enum {string}
             */
            state: "queued" | "running" | "done" | "failed" | "cancelled";
            /** Phase */
            phase?: string | null;
            /** Phase Index */
            phase_index?: number | null;
            /** N Phases */
            n_phases?: number | null;
            /** Pct */
            pct?: number | null;
            /** Message */
            message?: string | null;
            /** Started At */
            started_at?: string | null;
            /** Elapsed S */
            elapsed_s?: number | null;
            /**
             * Eta S
             * @description Seconds remaining, or null when the phase is silent. May JUMP UP.
             */
            eta_s?: number | null;
            /**
             * Log Tail
             * @description The last 200 lines the job narrated (the UI shows ~8).
             */
            log_tail?: string[];
            /**
             * Cancellable
             * @default true
             */
            cancellable: boolean;
            /**
             * Exclusive
             * @description The exclusive resource lease this job holds, e.g. "gpu". While one is held, another job wanting the same lease gets 409 busy.
             */
            exclusive?: string | null;
            /**
             * Result
             * @description null until state == 'done'. Discriminated on `kind`.
             */
            result?: (components["schemas"]["OpenJobResult"] | components["schemas"]["BuildResult"] | components["schemas"]["ExportResult"] | components["schemas"]["RecheckResult"]) | null;
            /** @description Set iff state == 'failed'. */
            error?: components["schemas"]["JobError"] | null;
        };
        /** JobCancelResponse */
        JobCancelResponse: {
            /** Job Id */
            job_id: string;
            /**
             * State
             * @enum {string}
             */
            state: "queued" | "running" | "done" | "failed" | "cancelled";
        };
        /** JobError */
        JobError: {
            /** Code */
            code: string;
            /** Message */
            message: string;
            /** Traceback */
            traceback?: string | null;
        };
        /** JobListResponse */
        JobListResponse: {
            /**
             * Jobs
             * @description Newest first.
             */
            jobs: components["schemas"]["Job"][];
        };
        /**
         * JobRef
         * @description The 202 body of every long operation.
         */
        JobRef: {
            /** Job Id */
            job_id: string;
            /** Kind */
            kind: string;
        };
        /**
         * LoadDocumentRequest
         * @description `POST /api/documents/load` — the Load screen's *"Load a project…"*, which must be reachable
         *     **cold** (no session). The server bootstraps a session from the file's own `data_dir`, then
         *     re-reads the file so the range guard actually runs against the session it belongs to.
         */
        LoadDocumentRequest: {
            /** Path */
            path: string;
            /** Session Id */
            session_id?: string | null;
        };
        /** LoadDocumentResponse */
        LoadDocumentResponse: {
            doc: components["schemas"]["Document"];
            /** @description Set when the load had to open the dataset itself. */
            session?: components["schemas"]["SessionResponse"] | null;
            /** Warnings */
            warnings?: string[];
            /**
             * Migrated From
             * @description The old schema_version, if the file was migrated on the way in.
             */
            migrated_from?: string | null;
        };
        /** LogEntry */
        LogEntry: {
            /** Trial */
            trial: number;
            /** Type */
            type: string;
            /** Time */
            time: string;
            /** Gap S */
            gap_s?: number | null;
        };
        /**
         * LogResponse
         * @description ⚠️ `log.txt` prints the date **only** on `New experiment:` lines; every other line carries
         *     `HH:MM:SS` alone. The parser carries the date forward and handles midnight rollover.
         */
        LogResponse: {
            /** Experiment */
            experiment: string;
            /** Entries */
            entries: components["schemas"]["LogEntry"][];
            /** N Snapshot */
            n_snapshot: number;
            /** N Other */
            n_other: number;
        };
        /** MachineEvidenceRequest */
        MachineEvidenceRequest: {
            doc: components["schemas"]["MosaicDocument"];
        };
        /**
         * MachineEvidenceResponse
         * @description Powers the provenance panel (a **live warning**, and it stays on the page) and the confirm
         *     text of "Skip — place by hand". Derived from HISTORY, never from what the document claims.
         */
        MachineEvidenceResponse: {
            seeded_from: components["schemas"]["SeededFrom"] | null;
            /** Has Build */
            has_build: boolean;
            /** N Machine Tiles */
            n_machine_tiles: number;
            /** Independent Of Method */
            independent_of_method: boolean;
            /**
             * Warning
             * @description `PROVENANCE_WARNING`, verbatim, or null.
             */
            warning?: string | null;
        };
        /**
         * MatchAnchorRequest
         * @description ⭐⭐ **A PURE FUNCTION OF THIS BODY.** The server holds no tile state to be out of sync with.
         *
         *     🔴 **THE PREFETCH, AND THE CORRECTNESS TRAP IN IT.** Every `Space` costs ~1 s, so the front end
         *     fires the *next* tile's match the instant the current one is **displayed** — and that prefetch
         *     **MUST INCLUDE THE TILE CURRENTLY UNDER JUDGEMENT IN `anchors`**, i.e. it must assume the user
         *     will press `A`. Prefetching from a composite **without** it **disagrees with the truth in 18 % of
         *     presses and is catastrophically wrong (up to 1,143 px) in 6 %**.
         *
         *     The server memoises on
         *
         *         cache_key = sha1({session nonce, target, [(t, x, y) for t in sorted(anchors)],
         *                           mode, near, radius, max_candidates, sorted(refuse)})
         *
         *     — i.e. **the key IS the anchor set and their positions.** Press `E` instead of `A` and the set
         *     genuinely differs ⇒ the key differs ⇒ the memo misses ⇒ the server recomputes honestly. **The
         *     trap is structurally impossible to fall into, as long as the server holds no tile state.**
         *
         *     ⇒ ⛔ **NEVER add a client-side prefetch cache keyed on the trial number.** The prefetch is
         *     literally the same POST, fired early, and its answer is thrown away.
         *     ⇒ ⛔ **Never invent a second server cache keyed on the trial number.**
         */
        MatchAnchorRequest: {
            /** Session Id */
            session_id: string;
            /** Target */
            target: number;
            /**
             * Anchors
             * @description The reference field. Order is irrelevant (the server sorts). Must be non-empty and must not contain `target`.
             */
            anchors: number[];
            /**
             * Positions
             * @description World top-left of **every** trial in `anchors`. Extras ignored; a missing anchor is a 400.
             */
            positions: {
                [key: string]: [
                    number,
                    number
                ];
            };
            /**
             * Mode
             * @default global
             * @enum {string}
             */
            mode: "global" | "local";
            /**
             * Near
             * @description REQUIRED when mode == 'local': where the user dropped the tile.
             */
            near?: [
                number,
                number
            ] | null;
            /**
             * Radius
             * @description local only. ⚠️ **Never widen past 128 in the UI**: the electrode grid repeats every **256 px** and a wide *local* search will confidently lock onto a grid alias. To search wide, use `mode: 'global'` — the FFT + margin is what survives the aliases.
             * @default 64
             */
            radius: number;
            /**
             * Max Candidates
             * @description ⭐ 9, or key `9` can never fire.
             * @default 9
             */
            max_candidates: number;
            /**
             * Refuse
             * @description 🔴 **THE REFUSAL SET, IN THE BODY** — the blank list the human left standing (`MosaicDocument.blank_scan.blank`). It is part of `cache_key`. In v1 this was hidden server state that `PUT /api/scan/blank` mutated, which is precisely what made this endpoint impure. **That endpoint no longer exists.**
             */
            refuse?: number[];
        };
        /**
         * MatchResult
         * @description `candidates` is sorted by `ncc` descending, peaks ≥ 24 px apart (t33's NMS). It may be empty.
         *
         *     ⚠️ **The aperture is small at the start.** With one anchor down this call *is* a tile-pair — the
         *     weak case above. It works anyway because consecutive snapshots overlap ~78 %. **But surface the
         *     evidence** (`n_anchors`, `composite.valid_px`, `ncc`, `margin`) on every placement, so the user
         *     watches the evidence strengthen instead of taking it on faith.
         */
        MatchResult: {
            /** Target */
            target: number;
            /**
             * Mode
             * @enum {string}
             */
            mode: "global" | "local";
            /** N Anchors */
            n_anchors: number;
            composite?: components["schemas"]["CompositeInfo"] | null;
            /** Candidates */
            candidates?: components["schemas"]["Candidate"][];
            best?: components["schemas"]["Candidate"] | null;
            /**
             * Margin
             * @description candidates[0].ncc − candidates[1].ncc. null if there is only one.
             */
            margin?: number | null;
            /**
             * Margin Thin
             * @description margin < 0.10. ⚠️ **THE UI MUST FLAG IT LOUDLY** — this is what a surviving grid alias looks like. The shipped build's worst run margin is 0.081 vs a ~0.47 typical.
             * @default false
             */
            margin_thin: boolean;
            refused?: components["schemas"]["Refusal"] | null;
            /**
             * Dropped Anchors
             * @description Blank anchors dropped from the composite. Not an error.
             */
            dropped_anchors?: number[];
            /** Gpu */
            gpu: boolean;
            /** Elapsed Ms */
            elapsed_ms: number;
            /** Cached */
            cached: boolean;
            /** Cache Key */
            cache_key: string;
        };
        /**
         * MatchScoreRequest
         * @description `POST /api/mosaic/match/score` — *"you dropped it here; what do the pixels say?"* One
         *     `exact_ncc`, no search (~31 ms). Used live during a drag (debounced 150 ms), to label the
         *     alternatives, and to **re-measure a diverted tile's NCC where it actually sits**.
         *
         *     ⭐ Deliberately **NOT** serialised behind an in-flight `match/anchor`: it used to wait out a
         *     whole match, so the live NCC under the cursor was up to a full match out of date.
         */
        MatchScoreRequest: {
            /** Session Id */
            session_id: string;
            /** Target */
            target: number;
            /** Anchors */
            anchors: number[];
            /** Positions */
            positions: {
                [key: string]: [
                    number,
                    number
                ];
            };
            /** At */
            at: [
                number,
                number
            ];
            /** Refuse */
            refuse?: number[];
        };
        /**
         * MosaicDocument
         * @description ⭐ **THE MOSAIC PAYLOAD, FLAT, BESIDE THE ENVELOPE.** (See the `Document` note: nesting it
         *     under `payload` would make it unreadable to the scorer, and a project the scorer cannot read is
         *     a project that cannot be checked.)
         *
         *     ⛔ **NOTHING STARTS `excluded`.** Every trial in the run starts `unplaced`. A frame becomes
         *     `excluded` only when the human presses `E`, or when a project file he loaded says so.
         */
        MosaicDocument: {
            /** Schema Version */
            schema_version: string;
            app: components["schemas"]["AppStamp"];
            /** Id */
            id: string;
            /** Feature */
            feature: string;
            /** Dataset */
            dataset: string;
            /**
             * Experiment
             * @default
             */
            experiment: string;
            /** Data Dir */
            data_dir: string;
            /** Dataset Key */
            dataset_key: string;
            /** Created */
            created: string;
            /** Modified */
            modified: string;
            provenance: components["schemas"]["Provenance"];
            /** Tiles */
            tiles: {
                [key: string]: components["schemas"]["TileRecord"];
            };
            /** Trial Range */
            trial_range: [
                number,
                number
            ];
            /** Pass Split */
            pass_split?: number | null;
            /** Origin Trial */
            origin_trial: number;
            /**
             * Tile Px
             * @default 512
             */
            tile_px: number;
            /**
             * Coordinates
             * @description ⚠️ Built from `SessionResponse.frame_note` — i.e. from THIS acquisition's XML. Never the unconditional literal "180deg-flipped" the v1 constant carried.
             */
            coordinates: string;
            tolerance_px: components["schemas"]["TolerancePx"];
            /**
             * Gaps
             * @description ⚠️ **DERIVED. Recomputed on EVERY change to the excluded set.** Trial numbers are acquisition ORDER but are NOT contiguous once anything is excluded, and a 'consecutive' pair across a gap is a multi-step jump where the serpentine one-axis step prior does NOT hold. Miss this and the next solve is silently poisoned.
             */
            gaps?: [
                number,
                number
            ][];
            /** Unusable Tiles */
            unusable_tiles?: number[];
            /**
             * Cursor
             * @description The sweep position, saved so a load resumes there. 🔴 An **integer**, never null-on-Escape: `Esc` deselects, it does not abandon the tile you are judging.
             */
            cursor?: number | null;
            blank_scan?: components["schemas"]["BlankScanBlock"];
            run?: components["schemas"]["RunBlock"];
            build?: components["schemas"]["BuildBlock"] | null;
            tone?: components["schemas"]["ToneSnapshot"];
        } & {
            [key: string]: unknown;
        };
        /** OkResponse */
        OkResponse: {
            /**
             * Ok
             * @default true
             */
            ok: boolean;
        };
        /**
         * OpenJobResult
         * @description `Job.result` when `kind == "open"`.
         */
        OpenJobResult: {
            /**
             * @description discriminator enum property added by openapi-typescript
             * @enum {string}
             */
            kind: "open";
            session: components["schemas"]["SessionResponse"];
        };
        /**
         * OpenSessionRequest
         * @description ⚠️ `trials` is the caller's selection. A FrameStore holds one shape.
         *
         *     * `trials: null` ⇒ every snapshot trial. If they are **not** all one shape the open fails with
         *       `409 mixed_shape`, listing the groups — core **refuses to guess** which shape you meant.
         *     * `trials: [...]` ⇒ exactly those. The mosaic feature passes its 512×512 selection here.
         *
         *     ⛔ Nothing is ever dropped by TRIAL NUMBER. Only by shape, and only with a reason, reported in
         *     `SessionResponse.skipped`.
         */
        OpenSessionRequest: {
            /** Path */
            path?: string | null;
            /** Dataset Key */
            dataset_key?: string | null;
            /** Trials */
            trials?: number[] | null;
        };
        /**
         * PassSplit
         * @description ⭐ `value` is the **LAST TRIAL OF PASS 1** (166 on 260620d), never 167.
         *
         *     ⚠️ Keep the `MIN_SIDE_FRAC = 0.20` guard: without it a naive argmax returns **11**, because
         *     11→12 is *also* 20.0 s (settling right after `Settings loaded`) and ties the true boundary.
         */
        PassSplit: {
            /** Value */
            value: number | null;
            /** Detected */
            detected: boolean;
            /** Why */
            why: string;
            /** Gap S */
            gap_s?: number | null;
            /** Median Gap S */
            median_gap_s?: number | null;
            runner_up?: components["schemas"]["RunnerUpSplit"] | null;
            /**
             * N Pass1
             * @default 0
             */
            n_pass1: number;
            /**
             * N Pass2
             * @default 0
             */
            n_pass2: number;
        };
        /**
         * PerTileEvidence
         * @description **A "go look here first" list, NOT a verdict.**
         *
         *     🔴 **PASS-1 TILES HAVE NO PER-TILE CONFIDENCE AT ALL** — t27's info is aggregate-only, and the
         *     worst tile in the shipped 312/312 build (trial 127, at 9.94 px) **is a pass-1 tile**. Say so,
         *     loudly, on the screen: **the absence of a warning is not a clean bill of health.**
         *
         *     ⛔ Do **not** rebuild this on `quality.score_positions`: on the ground-truth-perfect 312/312
         *     build it flags 11 tiles and **all 11 are false positives (precision 0/11)**. And
         *     `quality.score_build()` / `quality.leaderboard()` are **dead** — they raise.
         */
        PerTileEvidence: {
            /**
             * Anchor Ncc
             * @description PASS 2 ONLY. The best signal there is.
             */
            anchor_ncc?: number | null;
            /**
             * Anchor Residual Px
             * @description Probably THE number to sort a worklist on (it caught a tile at 2,706 px) — but it has fired exactly once and its false-positive rate is unmeasured. 'Go look', never a verdict.
             */
            anchor_residual_px?: number | null;
            /** Run */
            run?: string | null;
            /** Run Margin */
            run_margin?: number | null;
            /** Pass */
            pass: number;
        };
        /**
         * Provenance
         * @description ⚠️ **NOT DECORATION.**
         *
         *     `warning` is present **iff** `independent_of_method` is false, and it is the
         *     `PROVENANCE_WARNING` constant **verbatim, never paraphrased**. The verdict is derived from the
         *     document's **HISTORY** — a `build` block, or a single tile still carrying a `machine` position —
         *     and **never** from what the document says about itself, because `seeded_from` is writable and
         *     "Skip — place from scratch" once erased it while every tile kept t33's answer.
         *
         *     🔴 **This project has already destroyed one benchmark exactly this way**
         *     (`analysis/archive/challenge_2026-07/benchmark/ground_truth/260620d.json` **is T27's own
         *     output**), and pass 1's ground truth got tiles 128/129/130/148 wrong for the same reason.
         */
        Provenance: {
            /** Authored By */
            authored_by: string;
            /** App Version */
            app_version: string;
            /** Workflow */
            workflow: string;
            seeded_from?: components["schemas"]["SeededFrom"] | null;
            /** Independent Of Method */
            independent_of_method: boolean;
            /** Warning */
            warning?: string | null;
            human_edits?: components["schemas"]["HumanEdits"];
            scale?: components["schemas"]["Scale"];
        } & {
            [key: string]: unknown;
        };
        /**
         * QcDenominator
         * @description ⭐ **EVERY NUMBER STATES ITS DENOMINATOR.** A percentage without one is how the 182-vs-156
         *     confusion started. The denominator is the document's own `excluded` set, and nothing else — the
         *     app has no list of its own.
         *
         *     ⚠️ **`not_excluded` was called `usable_trials` in v1's `qc.json`, and it is RENAMED here on
         *     purpose.** The name collided, character for character, with `excluded.usable_trials()` — the one
         *     symbol the standing ruling forbids the app to import. A key in a report is not a function, but
         *     the whole point of that ruling is that nobody should ever have to make that distinction. The
         *     number is unchanged: **every tile the human did not exclude.** (Nothing machine-readable depends
         *     on the old name: `score.py` reads `gt.json` and `positions.csv`, never `qc.json`.)
         */
        QcDenominator: {
            /** Trials In Document */
            trials_in_document: number;
            /** Not Excluded */
            not_excluded: number;
            /** Excluded */
            excluded: number;
        } & {
            [key: string]: unknown;
        };
        /** QcMovedRow */
        QcMovedRow: {
            /** Trial */
            trial: number;
            /** Moved Px */
            moved_px: number;
            /** From */
            from: [
                number,
                number
            ];
            /** To */
            to: [
                number,
                number
            ];
            /**
             * State
             * @enum {string}
             */
            state: "unplaced" | "unverified" | "anchored" | "excluded";
            /** Ncc */
            ncc?: number | null;
            /** Margin */
            margin?: number | null;
            /** N Anchors */
            n_anchors?: number | null;
            /** Note */
            note?: string | null;
        } & {
            [key: string]: unknown;
        };
        /**
         * QcReport
         * @description What the human did vs what the machine said. This report **is** the honest record of the
         *     anchoring hazard, and it is what makes the output safe to hand to somebody else.
         */
        QcReport: {
            /** Dataset */
            dataset: string;
            /** Trial Range */
            trial_range: [
                number,
                number
            ];
            /** Pass Split */
            pass_split: number | null;
            /** Generated */
            generated: string;
            app: components["schemas"]["AppStamp"];
            denominator: components["schemas"]["QcDenominator"];
            /** By State */
            by_state: {
                [key: string]: number[];
            };
            /** Moved */
            moved: components["schemas"]["QcMovedRow"][];
            /** Thin Margin */
            thin_margin: number[];
            /** Rescued */
            rescued: number[];
            /** Build Stale */
            build_stale: boolean;
            /** Build Stale Reason */
            build_stale_reason?: string | null;
            provenance: components["schemas"]["Provenance"];
        } & {
            [key: string]: unknown;
        };
        /** QcRequest */
        QcRequest: {
            doc: components["schemas"]["MosaicDocument"];
        };
        /**
         * RecheckRequest
         * @description `POST /api/mosaic/recheck` → 202 `JobRef` (it is N × ~1 s — it never blocks).
         *
         *     🔴 **THE RE-CHECK IS GLOBAL, AND IT IS ALLOWED TO SAY NO.** It **never moves a tile** — it only
         *     measures it. Anything that disagrees with where the tile sits by more than `tolerance_px`
         *     **STAYS FLAGGED**.
         *
         *     v1 fired a *local* match (±64 px) and then cleared `stale` unconditionally. But a tile knocked
         *     off by a moved anchor is off by **hundreds** of px — failure on this data is **binary**,
         *     sub-pixel-right or wildly wrong — so a ±64 px window is *structurally blind* to the one error the
         *     panel exists for. Measured: a tile **380 px** from the truth re-checked locally to `ncc −0.0678`,
         *     was not moved, and had its flag **cleared** — the loud panel disappeared and the tile stayed 380
         *     px wrong, with a negative NCC recorded in the document as its evidence.
         */
        RecheckRequest: {
            /** Session Id */
            session_id: string;
            doc: components["schemas"]["MosaicDocument"];
            /**
             * Trials
             * @description null = every tile flagged stale.
             */
            trials?: number[] | null;
            /**
             * Tolerance Px
             * @default 5
             */
            tolerance_px: number;
        };
        /**
         * RecheckResult
         * @description `Job.result` when `kind == "recheck"`. ⚠️ It returns **rows, not a document** — the re-check
         *     does not move anything; the front end clears the flags it is told it may clear.
         */
        RecheckResult: {
            /**
             * @description discriminator enum property added by openapi-typescript
             * @enum {string}
             */
            kind: "recheck";
            /** N Checked */
            n_checked: number;
            /** Cleared */
            cleared: number[];
            /**
             * Disagree
             * @description **Go and look.** These are still wrong.
             */
            disagree: components["schemas"]["RecheckRow"][];
        };
        /** RecheckRow */
        RecheckRow: {
            /** Trial */
            trial: number;
            /** At */
            at: [
                number,
                number
            ];
            /** Best */
            best: [
                number,
                number
            ] | null;
            /** Disagree Px */
            disagree_px: number | null;
            /** Ncc */
            ncc: number | null;
            /** Margin */
            margin: number | null;
            /** Still Stale */
            still_stale: boolean;
        };
        /**
         * Refusal
         * @description ⛔ **BLANK FRAMES ARE REFUSED, NOT SCORED. There is no `force` flag and there will not be
         *     one.** Two blank frames 136 trials apart correlate **+0.43 at zero shift** (honest noise floor
         *     0.115) because what they share is *fixed-pattern sensor structure*, which does not move with the
         *     stage. **They register confidently and wrongly.**
         *
         *     * **`target` is blank** → REFUSE. `candidates: []`, `best: null`. This is the real hazard.
         *     * **an `anchor` is blank** → **DROPPED from the composite** (`MatchResult.dropped_anchors`).
         *       **Not fatal.** 🔴 The rule that made a blank anchor an error dead-ended the app: the moment the
         *       user anchored the first near-threshold trial, every subsequent `Space` and snap refused
         *       **forever** and the sweep died at the next tile.
         *     * **every anchor is blank** → `reason: "no_anchors"`.
         *
         *     **A human may still place a blank tile by hand** — a human eye is allowed to do what the
         *     correlator must not. It gets **no NCC**, because there is no honest one.
         */
        Refusal: {
            /**
             * Reason
             * @enum {string}
             */
            reason: "blank" | "no_anchors";
            /** Trials */
            trials: number[];
            /** Texture */
            texture?: number | null;
            /** Threshold */
            threshold?: number | null;
            /** Message */
            message: string;
        };
        /**
         * RejectedMatch
         * @description What the matcher wanted, on a tile the app **diverted** to the solver's answer. It is the
         *     *evidence for* the divert and the QC report needs it in full.
         */
        RejectedMatch: {
            /** X */
            x: number;
            /** Y */
            y: number;
            /** Ncc */
            ncc?: number | null;
            /** Margin */
            margin?: number | null;
            /** Npix */
            npix?: number | null;
            /** Rank */
            rank?: number | null;
            /** N Anchors */
            n_anchors?: number | null;
        } & {
            [key: string]: unknown;
        };
        /**
         * RunBlock
         * @description What the run detection said, recorded so the document can explain itself later.
         */
        RunBlock: {
            /**
             * Detected
             * @default true
             */
            detected: boolean;
            /**
             * Why
             * @default
             */
            why: string;
            /**
             * Pass Split Detected
             * @default true
             */
            pass_split_detected: boolean;
            /**
             * Pass Split Why
             * @default
             */
            pass_split_why: string;
            /**
             * N Trials
             * @default 0
             */
            n_trials: number;
        } & {
            [key: string]: unknown;
        };
        /**
         * RunDetectRequest
         * @description `POST /api/mosaic/run` — detect, or override. **A pure function of the session's inventory.**
         *
         *     ⭐ It is NOT a session reload. v1 made `PATCH /api/session/run` a full re-open (5 s, and it
         *     invalidated the tone and the scan) because the run lived *on* the session. It does not any more.
         */
        RunDetectRequest: {
            /** Session Id */
            session_id: string;
            /** Lo */
            lo?: number | null;
            /** Hi */
            hi?: number | null;
            /** Pass Split */
            pass_split?: number | null;
        };
        /** RunDetection */
        RunDetection: {
            /** Lo */
            lo: number;
            /** Hi */
            hi: number;
            /**
             * Trials
             * @description ⛔ The run, gated **by shape only**. Nothing is dropped by trial number, ever. Every snapshot on disk in range is here.
             */
            trials: number[];
            /** N */
            n: number;
            /** N In Range */
            n_in_range: number;
            /** Detected */
            detected: boolean;
            /**
             * Why
             * @description MEASURED. Goes behind the `?`, verbatim.
             */
            why: string;
            /** Blocks */
            blocks?: components["schemas"]["SnapshotBlock"][];
            /** Dropped */
            dropped?: components["schemas"]["DroppedTrial"][];
            pass_split: components["schemas"]["PassSplit"];
            /** Gaps */
            gaps?: [
                number,
                number
            ][];
        };
        /** RunnerUpSplit */
        RunnerUpSplit: {
            /** After Trial */
            after_trial: number;
            /** Gap S */
            gap_s: number;
        };
        /**
         * SaveDocumentRequest
         * @description `PUT /api/analyses/{analysis_id}/document` (into the workspace) or
         *     `POST /api/documents/save-as` (a file the user names — `Save…`, reachable from EVERY screen).
         *
         *     ⚠️ **ORDER, on the server:** structural-validate → normalise → stamp → full-validate → write,
         *     **atomically** (temp + `os.replace`, under a lock, with fsync and a retry). Not
         *     `validate → normalise`: the derived fields are exactly the ones that drift when the user
         *     excludes a tile, and `normalise` is what repairs them. 🔴 On Windows two concurrent `os.replace`
         *     on one target give `WinError 5` and the loser's document was **silently lost**.
         */
        SaveDocumentRequest: {
            /**
             * Path
             * @description save-as only. Refused inside `data/`, the repo, or the dataset.
             */
            path?: string | null;
            doc: components["schemas"]["Document"];
        };
        /** SaveResult */
        SaveResult: {
            /** Path */
            path: string;
            /** Bytes */
            bytes: number;
            /** Saved At */
            saved_at: string;
            /** Warnings */
            warnings?: string[];
        };
        /**
         * Scale
         * @description 📏 **PIXELS ONLY.** `um_per_px` is `null` by default and the exporter writes no scale bar and
         *     no OME-TIFF `PhysicalSizeX/Y`. If the user types a number in, it is written **and stamped
         *     "user-supplied by hand, not measured"**. ❌ Never 1.237 µm/px — that came from a broken
         *     inference. (There is no magnification difference between the passes: cross-pass tissue scale is
         *     1.0000 ± 0.0002. The MEA grid pitch tracks *focus*, not magnification.)
         */
        Scale: {
            /** Um Per Px */
            um_per_px?: number | null;
            /**
             * Source
             * @default unknown
             * @enum {string}
             */
            source: "unknown" | "user-supplied by hand, not measured";
        };
        /**
         * ScoreResult
         * @description ⚠️ `ncc` is **`null`** when the overlap is below `exact_ncc`'s floor (< 3000 valid px, or
         *     < 64 px on a side). The honest answer is *"not measurable"* — **never `0.0`**.
         */
        ScoreResult: {
            /** Target */
            target: number;
            /** At */
            at: [
                number,
                number
            ];
            /** Ncc */
            ncc: number | null;
            /** Npix */
            npix: number;
            refused?: components["schemas"]["Refusal"] | null;
            /** Dropped Anchors */
            dropped_anchors?: number[];
            /** Elapsed Ms */
            elapsed_ms: number;
        };
        /**
         * SeedRequest
         * @description `POST /api/mosaic/seed` — apply a build to a document. **The SERVER does this**, not the
         *     front end (see `CreateAnalysisRequest`).
         *
         *     🔴 **A RE-SOLVE MUST NOT DESTROY THE HUMAN'S WORK.** It **keeps** every tile that is `anchored`
         *     **or** flagged `human`, and seeds everything else *around* them — translating the build onto the
         *     human's field by the **MEDIAN** offset over the protected tiles (a median, not a mean: a tile the
         *     human corrected *because t33 was wrong* is precisely an outlier, and one 2,969 px correction
         *     would drag a mean into nonsense). If **none** of the human's tiles are in the build, the two
         *     frames cannot be tied together and the server **refuses to seed** (`409 refused`) rather than
         *     guess.
         *
         *     (v1 called `setState` unconditionally on every non-excluded tile, so a 150-tile sweep with three
         *     catastrophic hand-corrections in it was wiped by taking the app's own advice to re-solve — and
         *     the autosave then wrote the wiped document over the crash-recovery file.)
         */
        SeedRequest: {
            doc: components["schemas"]["MosaicDocument"];
            /** Build Id */
            build_id: string;
        };
        /** SeedResponse */
        SeedResponse: {
            doc: components["schemas"]["MosaicDocument"];
            /** N Seeded */
            n_seeded: number;
            /**
             * N Protected
             * @description anchored + human tiles the build was NOT allowed to touch.
             */
            n_protected: number;
            /** Seed Translation */
            seed_translation: [
                number,
                number
            ];
        };
        /** SeededFrom */
        SeededFrom: {
            /** Method */
            method: string;
            /** Build Id */
            build_id?: string | null;
            /** Config */
            config?: {
                [key: string]: number | boolean | string | {
                    [key: string]: number | boolean | string | null;
                } | (number | boolean | string | null)[] | null;
            };
            /**
             * Detected From
             * @description HOW the machine was detected. Evidence, not a self-declaration.
             */
            detected_from?: string | null;
        } & {
            [key: string]: unknown;
        };
        /** SessionListResponse */
        SessionListResponse: {
            /** Sessions */
            sessions: components["schemas"]["SessionResponse"][];
        };
        /** SessionResponse */
        SessionResponse: {
            /** Session Id */
            session_id: string;
            /** Dataset Key */
            dataset_key: string;
            /** Dataset */
            dataset: string;
            /** Experiment */
            experiment: string;
            /** Path */
            path: string;
            /** Opened At */
            opened_at: string;
            /**
             * Nonce
             * @description ⭐ A FRESH identity per open. Cache-bust every pixel URL with `?v={nonce}.{tone.version}`. **NOT the tone version alone** — that is a dataclass default and resets to 1 on every open, while the pixels behind an `immutable, max-age=1y` URL change. Open a second acquisition whose trial numbers overlap and the browser serves the FIRST dataset's pixels. (And `id()` is recycled — do not use it.)
             */
            nonce: string;
            /** W */
            w: number;
            /** H */
            h: number;
            /**
             * Trials
             * @description What is actually loaded, in order.
             */
            trials: number[];
            /** N */
            n: number;
            /** Skipped */
            skipped?: components["schemas"]["SkippedFrame"][];
            tone: components["schemas"]["Tone"];
            /**
             * Frame Note
             * @description ⭐ What the reader ACTUALLY did to these pixels, derived from THIS acquisition's XML — never asserted. It goes into the TIFF header. Do not hard-code "180-degree-flipped" anywhere: the reader flips CONDITIONALLY on ax/ay, and a header that lies about its own coordinate frame is the one thing this project has been burned by.
             */
            frame_note: string;
            gpu: components["schemas"]["GpuInfo"];
            /** N Log Entries */
            n_log_entries: number;
        };
        /**
         * Settings
         * @description Everything the app remembers between launches. ⛔ **No dataset knowledge lives here.**
         *     A remembered *path* is not knowledge about the data at that path.
         */
        Settings: {
            /**
             * Workspace
             * @description Where analyses are written.
             */
            workspace?: string | null;
            /**
             * Dataset Roots
             * @description Folders the user pointed at. The browser scans these.
             */
            dataset_roots?: string[];
            /** Recent Datasets */
            recent_datasets?: string[];
        };
        /** SettingsUpdate */
        SettingsUpdate: {
            /** Workspace */
            workspace?: string | null;
            /** Dataset Roots */
            dataset_roots?: string[] | null;
        };
        /**
         * ShapeGroup
         * @description Snapshot trials sharing one frame shape. ⛔ The browser reports shapes; it does not *gate*
         *     on one. The 512×512 gate is the mosaic feature's policy (512 is `t33.TILE`), not core's.
         */
        ShapeGroup: {
            /** W */
            w: number;
            /** H */
            h: number;
            /** N */
            n: number;
            /** Trials */
            trials: number[];
        };
        /** SkippedFrame */
        SkippedFrame: {
            /** Trial */
            trial: number;
            /**
             * Reason
             * @enum {string}
             */
            reason: "off_shape" | "unreadable" | "not_snapshot";
            /** W */
            w?: number | null;
            /** H */
            h?: number | null;
            /** Message */
            message: string;
        };
        /**
         * SnapshotBlock
         * @description A contiguous run of Snapshot trials in `log.txt`.
         */
        SnapshotBlock: {
            /** Lo */
            lo: number;
            /** Hi */
            hi: number;
            /** N */
            n: number;
        };
        /** T27Config */
        T27Config: {
            /** Conf */
            conf?: number | null;
            /** Run Conf */
            run_conf?: number | null;
            /** Span */
            span?: number | null;
            /** Control */
            control?: boolean | null;
        } & {
            [key: string]: unknown;
        };
        /**
         * TextureResponse
         * @description ⭐ **THE MEASUREMENT — and it is CORE, not mosaic.** `std(DoG(3,30))` answers "how much
         *     texture is in this image", which is a property of the FRAME. Every future feature asks it (a
         *     segmentation feature must not segment glare; the browser wants to say "these frames are empty").
         *
         *     It is also **free** here and costs +3.0 s / +624 MiB anywhere else: the matcher's band-passed
         *     stack and this measure are **the same array**.
         *
         *     ⛔ There is **no threshold here, no list, and no policy.** Turning a number into a *proposal*
         *     is the mosaic feature's job (`POST /api/mosaic/screen/propose`), and turning a proposal into a
         *     *decision* is the human's — it lands in the document. Never in the session.
         */
        TextureResponse: {
            /**
             * Measure
             * @default std of DoG(sigma=3, sigma=30) of the frame as read
             */
            measure: string;
            /** Texture */
            texture: {
                [key: string]: number;
            };
            /** N */
            n: number;
        };
        /**
         * ThumbsResponse
         * @description The contact sheet: ONE sprite sheet. Cell `i` holds `trials[i]` at
         *     `(x, y) = ((i % grid) * cell, (i // grid) * cell)`.
         */
        ThumbsResponse: {
            /** Grid */
            grid: number;
            /** Cell */
            cell: number;
            /** Trials */
            trials: number[];
            /** N */
            n: number;
            /**
             * Version
             * @description `{nonce}.{tone.version}` — pass it as `?v=`.
             */
            version: string;
        };
        /**
         * TileRecord
         * @description One tile. `extra="allow"`: unknown per-tile keys round-trip verbatim (a hand-written note in
         *     somebody's ground truth must survive an app that has never heard of it).
         *
         *     **Facts that are NOT states** (`blank` / `diverted` / `human` / `stale` / `machine` / `seq` /
         *     `alt_rank`): model them as fields, never as states, or the counters and the exports go wrong.
         *
         *     ⚠️ **`ncc` is defined as THE NCC AT THE TILE'S FINAL POSITION**, on every path. On a divert the
         *     tile is *not* at the match's best position, so writing the rejected alias's score here would
         *     attribute it to the position that shipped. Re-measure with one `POST /api/mosaic/match/score`.
         *
         *     ⚠️ A tile that leaves `excluded` must **stop claiming it was thrown out** — `excluded_reason` /
         *     `unusable_reason` die with the judgement, or a tile goes into the exported ground truth reading
         *     `status: "anchor", excluded: true`. **But `blank` is NOT cleared**: that is a *measurement*
         *     about the pixels, not a judgement.
         */
        TileRecord: {
            /**
             * Status
             * @enum {string}
             */
            status: "unplaced" | "unverified" | "anchor" | "excluded";
            /**
             * State
             * @enum {string}
             */
            state: "unplaced" | "unverified" | "anchored" | "excluded";
            /** X */
            x?: number | null;
            /** Y */
            y?: number | null;
            /**
             * R
             * @description Draw radius/opacity hint. Display only.
             */
            r?: number | null;
            /** Pass */
            pass?: number | null;
            /**
             * Machine
             * @description What the solver said, translated into the document's frame. ⭐ Its mere presence makes the provenance non-independent — see `Provenance`.
             */
            machine?: [
                number,
                number
            ] | null;
            /**
             * Moved Px
             * @description |position − machine|.
             */
            moved_px?: number | null;
            /** Ncc */
            ncc?: number | null;
            /** Margin */
            margin?: number | null;
            /** Npix */
            npix?: number | null;
            /** N Anchors */
            n_anchors?: number | null;
            /**
             * Blank
             * @description MEASURED. The matcher refuses this tile. **It excludes nothing and it never will.** Never cleared — it is a fact about the pixels.
             * @default false
             */
            blank: boolean;
            /** Texture */
            texture?: number | null;
            /** Judged At */
            judged_at?: string | null;
            /**
             * Seq
             * @description Judgement order. The stale cascade is seq >.
             */
            seq?: number | null;
            /**
             * Human
             * @description A hand put this position here. ⚠️ `advance()` must NEVER re-match over it — when he drags a tile it is usually *because the matcher was wrong*.
             * @default false
             */
            human: boolean;
            /**
             * Stale
             * @description Matched against an anchor field that has since changed. Cleared only by a GLOBAL re-check that agrees within 5 px — which is allowed to say NO.
             * @default false
             */
            stale: boolean;
            /**
             * Diverted
             * @description Sitting on the SOLVER's position because the match was not trustworthy. Dies the instant a hand rewrites the position. **Not** 'the human accepted the machine'.
             * @default false
             */
            diverted: boolean;
            /** Divert Reason */
            divert_reason?: string | null;
            rejected_match?: components["schemas"]["RejectedMatch"] | null;
            /**
             * Alt Rank
             * @description Which peak the human chose. **0-indexed in storage; DISPLAYED as +1.** The number he reads and the number he presses must agree.
             */
            alt_rank?: number | null;
            /**
             * Last Xy
             * @description The position an `E` took away. For undo.
             */
            last_xy?: [
                number,
                number
            ] | null;
            /**
             * Source
             * @description Human-readable provenance of the position.
             */
            source?: string | null;
            /** Note */
            note?: string | null;
            /** Excluded Reason */
            excluded_reason?: string | null;
            /** Unusable Reason */
            unusable_reason?: string | null;
        } & {
            [key: string]: unknown;
        };
        /**
         * TolerancePx
         * @description ⛔ `region_default` is **REQUIRED** — `benchmark/score.py :: load_gt()` reads it, at the top
         *     level, and the exporter asserts it is there before the file leaves the process.
         */
        TolerancePx: {
            /** Region Default */
            region_default: number;
        } & {
            [key: string]: unknown;
        };
        /**
         * Tone
         * @description The display window. ⚠️⚠️ **GLOBAL, NEVER PER-TILE.** A per-tile percentile stretch
         *     over-brightens near-empty frames and makes overlapping tiles disagree in brightness — **which
         *     destroys the Difference-mode check the whole verification loop depends on.** There is no
         *     per-tile path and there must never be one. Tone is display-only: it never touches the matcher
         *     (which sees band-passed, mean-subtracted pixels) and never touches the exported TIFF.
         */
        Tone: {
            /** Lo */
            lo: number;
            /** Hi */
            hi: number;
            /** Level */
            level: number;
            /** Flat Sigma */
            flat_sigma: number;
            /** Pct Lo */
            pct_lo: number;
            /** Pct Hi */
            pct_hi: number;
            /** N Sample */
            n_sample: number;
            /** Auto */
            auto: boolean;
            /**
             * Version
             * @description Bumped on every change. Half of the pixel cache-buster — see `Session.nonce`.
             */
            version: number;
        };
        /**
         * ToneSnapshot
         * @description A *display* snapshot, so a reload resumes where he left off. Core owns the tone; the document
         *     only records it. (⛔ The opacity slider is **not** here: `.camea.json` records the mosaic, not
         *     the operator's viewing preferences.)
         */
        ToneSnapshot: {
            /** Lo */
            lo?: number | null;
            /** Hi */
            hi?: number | null;
            /** Level */
            level?: number | null;
            /** Flat Sigma */
            flat_sigma?: number | null;
            /** Pct Lo */
            pct_lo?: number | null;
            /** Pct Hi */
            pct_hi?: number | null;
            /** Auto */
            auto?: boolean | null;
        } & {
            [key: string]: unknown;
        };
        /** ToneUpdate */
        ToneUpdate: {
            /** Lo */
            lo?: number | null;
            /** Hi */
            hi?: number | null;
            /**
             * Auto
             * @description true = recompute and discard lo/hi.
             */
            auto?: boolean | null;
        };
        /**
         * TrialMeta
         * @description ⚠️ **Shape is PER-TRIAL.** It is parsed from that trial's XML. It is never inferred from a
         *     file size, and a trial with `frames != 1` is rejected, not reshaped.
         */
        TrialMeta: {
            /** Trial */
            trial: number;
            /**
             * Type
             * @description The log.txt trial type, e.g. "Snapshot".
             */
            type: string;
            /** Time */
            time?: string | null;
            /**
             * Gap S
             * @description Seconds since the previous SNAPSHOT. null for the first.
             */
            gap_s?: number | null;
            /** W */
            w?: number | null;
            /** H */
            h?: number | null;
            /** Dtype */
            dtype?: string | null;
            /** Bytes */
            bytes?: number | null;
            /** Flip X */
            flip_x?: boolean | null;
            /** Flip Y */
            flip_y?: boolean | null;
            /** Dat */
            dat?: string | null;
        };
        /** ValidateDocumentRequest */
        ValidateDocumentRequest: {
            doc: components["schemas"]["Document"];
        };
        /** ValidationError */
        ValidationError: {
            /** Location */
            loc: (string | number)[];
            /** Message */
            msg: string;
            /** Error Type */
            type: string;
            /** Input */
            input?: unknown;
            /** Context */
            ctx?: Record<string, never>;
        };
        /**
         * ValidationReport
         * @description ⛔ **NO TRIAL NUMBER IS SPECIAL.** Nothing here may reject a document for WHICH trials it
         *     placed. The guard that used to live in v1 ("tile 284 is thrown out and carries a position")
         *     made the user's own session **unsaveable** the moment he anchored 284.
         */
        ValidationReport: {
            /** Ok */
            ok: boolean;
            /** Problems */
            problems?: components["schemas"]["DocumentProblem"][];
        };
        /** WorkspaceInfo */
        WorkspaceInfo: {
            /** Path */
            path: string | null;
            /** Exists */
            exists: boolean;
            /** Writable */
            writable: boolean;
            /**
             * N Analyses
             * @default 0
             */
            n_analyses: number;
        };
        /** WorkspaceSetRequest */
        WorkspaceSetRequest: {
            /** Path */
            path: string;
            /**
             * Create
             * @default true
             */
            create: boolean;
        };
    };
    responses: never;
    parameters: never;
    requestBodies: never;
    headers: never;
    pathItems: never;
}
export type $defs = Record<string, never>;
export interface operations {
    get_health_api_health_get: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HealthResponse"];
                };
            };
        };
    };
    get_gpu_api_gpu_get: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["GpuInfo"];
                };
            };
        };
    };
    get_settings_api_settings_get: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Settings"];
                };
            };
        };
    };
    put_settings_api_settings_put: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["SettingsUpdate"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Settings"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    get_fs_list_api_fs_list_get: {
        parameters: {
            query?: {
                /** @description Absolute directory. Omit to get the roots. */
                path?: string | null;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["FsListResponse"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    get_datasets_api_datasets_get: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DatasetListResponse"];
                };
            };
        };
    };
    post_datasets_scan_api_datasets_scan_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["DatasetScanRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DatasetListResponse"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    get_dataset_api_datasets__key__get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                key: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DatasetDetail"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    get_dataset_thumbnail_api_datasets__key__thumbnail_png_get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                key: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "image/png": unknown;
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    get_sessions_api_sessions_get: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SessionListResponse"];
                };
            };
        };
    };
    post_sessions_api_sessions_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["OpenSessionRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            202: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["JobRef"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    get_session_api_sessions__session_id__get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                session_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SessionResponse"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    delete_session_api_sessions__session_id__delete: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                session_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["OkResponse"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    get_session_log_api_sessions__session_id__log_get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                session_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["LogResponse"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    get_session_texture_api_sessions__session_id__texture_get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                session_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["TextureResponse"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    get_tone_api_sessions__session_id__tone_get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                session_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Tone"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    put_tone_api_sessions__session_id__tone_put: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                session_id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ToneUpdate"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Tone"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    get_tile_png_api_sessions__session_id__tiles__trial__png_get: {
        parameters: {
            query?: {
                v?: string | null;
            };
            header?: never;
            path: {
                session_id: string;
                trial: number;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "image/png": unknown;
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    get_tile_raw_api_sessions__session_id__tiles__trial__raw_get: {
        parameters: {
            query?: {
                v?: string | null;
            };
            header?: never;
            path: {
                session_id: string;
                trial: number;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/octet-stream": unknown;
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    get_thumbs_png_api_sessions__session_id__thumbs_png_get: {
        parameters: {
            query?: {
                v?: string | null;
                cell?: number;
            };
            header?: never;
            path: {
                session_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "image/png": unknown;
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    get_thumbs_json_api_sessions__session_id__thumbs_json_get: {
        parameters: {
            query?: {
                cell?: number;
            };
            header?: never;
            path: {
                session_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ThumbsResponse"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    get_workspace_api_workspace_get: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["WorkspaceInfo"];
                };
            };
        };
    };
    put_workspace_api_workspace_put: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["WorkspaceSetRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["WorkspaceInfo"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    get_analyses_api_workspace_analyses_get: {
        parameters: {
            query?: {
                dataset_key?: string | null;
                feature?: string | null;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AnalysisListResponse"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    post_analyses_api_workspace_analyses_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateAnalysisRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AnalysisSummary"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    delete_analysis_api_workspace_analyses__analysis_id__delete: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                analysis_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["OkResponse"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    get_document_api_analyses__analysis_id__document_get: {
        parameters: {
            query?: {
                recovered?: boolean;
            };
            header?: never;
            path: {
                analysis_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DocumentResponse"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    put_document_api_analyses__analysis_id__document_put: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                analysis_id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["SaveDocumentRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SaveResult"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    post_autosave_api_analyses__analysis_id__autosave_post: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                analysis_id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AutosaveRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SaveResult"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    post_document_load_api_documents_load_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["LoadDocumentRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["LoadDocumentResponse"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    post_document_save_as_api_documents_save_as_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["SaveDocumentRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SaveResult"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    post_document_validate_api_documents_validate_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ValidateDocumentRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ValidationReport"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    get_jobs_api_jobs_get: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["JobListResponse"];
                };
            };
        };
    };
    get_job_api_jobs__job_id__get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                job_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Job"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    post_job_cancel_api_jobs__job_id__cancel_post: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                job_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            202: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["JobCancelResponse"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    post_dialog_open_directory_api_dialog_open_directory_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["DialogOpenDirectoryRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DialogPathResponse"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    post_dialog_open_file_api_dialog_open_file_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["DialogOpenFileRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DialogPathResponse"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    post_dialog_save_file_api_dialog_save_file_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["DialogSaveFileRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DialogPathResponse"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    post_run_api_mosaic_run_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["RunDetectRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RunDetection"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    post_gaps_api_mosaic_gaps_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["GapsRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["GapsResponse"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    post_screen_propose_api_mosaic_screen_propose_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["BlankProposeRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["BlankProposal"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    post_build_api_mosaic_build_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["BuildStartRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            202: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["JobRef"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    get_build_api_mosaic_builds__build_id__get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                build_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["BuildResult"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    post_seed_api_mosaic_seed_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["SeedRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SeedResponse"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    post_machine_evidence_api_mosaic_document_machine_evidence_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["MachineEvidenceRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["MachineEvidenceResponse"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    post_discard_machine_api_mosaic_document_discard_machine_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["DiscardMachineRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DiscardMachineResponse"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    post_match_anchor_api_mosaic_match_anchor_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["MatchAnchorRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["MatchResult"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    post_match_score_api_mosaic_match_score_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["MatchScoreRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ScoreResult"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    post_recheck_api_mosaic_recheck_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["RecheckRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            202: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["JobRef"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    post_export_api_mosaic_export_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ExportRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            202: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["JobRef"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    post_qc_api_mosaic_qc_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["QcRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["QcReport"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
}

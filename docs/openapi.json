{
  "openapi": "3.1.0",
  "info": {
    "title": "Camea",
    "description": "A microscopy analysis desktop app. A DATASET is raw and read-only; an ANALYSIS is what you did to it, and it lives in the WORKSPACE you chose. This schema is THE CONTRACT: the TypeScript client is generated from it. See src/camea/api/schemas.py.",
    "version": "0.2.0"
  },
  "paths": {
    "/api/health": {
      "get": {
        "tags": [
          "core"
        ],
        "summary": "Get Health",
        "description": "Is the server up? **Cheap, and it touches nothing** \u2014 no engine, no GPU, no disk. It is what a\ntest harness and the launcher poll while the window is still painting.",
        "operationId": "get_health_api_health_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HealthResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/gpu": {
      "get": {
        "tags": [
          "core"
        ],
        "summary": "Get Gpu",
        "description": "See `gpu_info()`. The first call costs ~200 ms and is memoised for the life of the process.",
        "operationId": "get_gpu_api_gpu_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GpuInfo"
                }
              }
            }
          }
        }
      }
    },
    "/api/settings": {
      "get": {
        "tags": [
          "core"
        ],
        "summary": "Get Settings",
        "operationId": "get_settings_api_settings_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Settings"
                }
              }
            }
          }
        }
      },
      "put": {
        "tags": [
          "core"
        ],
        "summary": "Put Settings",
        "description": "\u26d4 One key, and it is a list of paths. See `camea.settings` \u2014 a settings file that remembered\nan exclusion would be answering, on the user's behalf, the question the app exists to help him\nanswer. (`projects` went with R44: the store is the index.)",
        "operationId": "put_settings_api_settings_put",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SettingsUpdate"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Settings"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/electrodes/device": {
      "get": {
        "tags": [
          "core"
        ],
        "summary": "Get Electrode Device",
        "description": "The chip a *\"whole chip imaged\"* fit is held to \u2014 `MAXWELL`, read at call time, not copied.\n\n\u26a0\ufe0f `as_dict()` derives `electrodes` from `axes`, so the count on the wire cannot disagree with\nthe shape beside it. Cheap and total: it touches no image, no session and no disk.",
        "operationId": "get_electrode_device_api_electrodes_device_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ElectrodeDevice"
                }
              }
            }
          }
        }
      }
    },
    "/api/fs/list": {
      "get": {
        "tags": [
          "core"
        ],
        "summary": "Get Fs List",
        "description": "List a directory's **sub-directories** \u2014 the served folder picker.\n\n\u2b50 **This is the route that keeps `--browser` and `--headless` usable.** The native dialogs need a\npywebview window; this needs nothing. It is how the UI lets the user choose a dataset folder, a\nworkspace folder or an export folder when there is no native dialog to open \u2014 and it is what\nmakes the whole app drivable by Playwright.\n\n\u26d4 **Directories only. It never returns a file's contents, and there is no write path here.**",
        "operationId": "get_fs_list_api_fs_list_get",
        "parameters": [
          {
            "name": "path",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Absolute directory. Omit to get the roots.",
              "title": "Path"
            },
            "description": "Absolute directory. Omit to get the roots."
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FsListResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/datasets/at": {
      "post": {
        "tags": [
          "core"
        ],
        "summary": "Post Datasets At",
        "description": "\u2b50 *\"Look at THIS folder.\"* A POST, not a GET: a Windows path in a query string is an\nencoding trap.\n\n\u26d4 **NOTHING IS REMEMBERED AND NOTHING IS RECOMMENDED.** This replaced `POST /api/datasets/scan`\n+ `GET /api/datasets` on 2026-07-25. There is no root registry, no depth-3 walk on every launch,\nand no list of datasets the app went looking for: the user names one folder and is told what is\nin it. Either it *is* an acquisition (`is_dataset: true`, one entry), or it directly contains\nsome and he picks which \u2014 a disambiguation of his own typing, not a suggestion.\n\n\u26d4 Nothing here recognises a dataset by name. A folder is a dataset iff it has a `log.txt` and at\nleast one `NNN.xml`. That is the whole rule.",
        "operationId": "post_datasets_at_api_datasets_at_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DatasetAtRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DatasetListResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/datasets/{key}": {
      "get": {
        "tags": [
          "core"
        ],
        "summary": "Get Dataset",
        "description": "Everything sayable about a dataset **without loading a pixel**: every trial (of any type), its\ntimestamp, its per-trial shape from its own XML, and the contiguous Snapshot blocks.\n\n\u26d4 **Core reports the blocks; it does not choose one.** \"The run is the longest block, restricted\nto 512x512\" is the *mosaic feature's* selection rule, and the next feature will want a different\none over the same frames.",
        "operationId": "get_dataset_api_datasets__key__get",
        "parameters": [
          {
            "name": "key",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Key"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DatasetDetail"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/datasets/{key}/thumbnail.png": {
      "get": {
        "tags": [
          "core"
        ],
        "summary": "Get Dataset Thumbnail",
        "description": "One frame, **with no session**: the browser card must not cost 5 s and 340 MiB.\n\nThe window is computed **globally over a small sample** of this dataset's own frames, never\nper-frame: a per-frame stretch would over-brighten a near-empty acquisition and make the card lie\nabout which datasets are dim. (~10 ms, then cached.)",
        "operationId": "get_dataset_thumbnail_api_datasets__key__thumbnail_png_get",
        "parameters": [
          {
            "name": "key",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Key"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "image/png": {}
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/sessions": {
      "get": {
        "tags": [
          "core"
        ],
        "summary": "Get Sessions",
        "operationId": "get_sessions_api_sessions_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SessionListResponse"
                }
              }
            }
          }
        }
      },
      "post": {
        "tags": [
          "core"
        ],
        "summary": "Post Sessions",
        "description": "Open a dataset -> **202 `JobRef`**. Poll `GET /api/jobs/{id}`; `result` is an `OpenJobResult`.\n\n\u26d4 **NOTHING IS EVER DROPPED BY TRIAL NUMBER.** A frame leaves the selection for exactly two\nreasons, both facts about the file on disk \u2014 it is `not_snapshot` (no readable 1-frame .dat/.xml\npair) or it is `off_shape` \u2014 and every one of them is reported in `SessionResponse.skipped` with\nits reason. **The app has no exclusion list, and this route is where one would have gone.**\n\n`trials: null` \u21d2 every snapshot trial. If they are not all one shape the open is refused with\n**409 `mixed_shape`**, listing the groups: a FrameStore holds ONE shape, and *which one you meant*\nis a decision core is not entitled to make for you.\n\n\u26a0\ufe0f The shape check runs **before** the job is submitted (`open_dataset` is ~0.2 s and touches no\npixels), so `mixed_shape` is a real 409 the client can act on, and not a job that fails 5 s later.",
        "operationId": "post_sessions_api_sessions_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/OpenSessionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "202": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JobRef"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/sessions/{session_id}": {
      "get": {
        "tags": [
          "core"
        ],
        "summary": "Get Session",
        "operationId": "get_session_api_sessions__session_id__get",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Session Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SessionResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      },
      "delete": {
        "tags": [
          "core"
        ],
        "summary": "Delete Session",
        "description": "Frees the stack (~340 MiB, plus the band stack's second copy).",
        "operationId": "delete_session_api_sessions__session_id__delete",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Session Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/OkResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/sessions/{session_id}/log": {
      "get": {
        "tags": [
          "core"
        ],
        "summary": "Get Session Log",
        "description": "\u26a0\ufe0f `log.txt` prints the date **only** on `New experiment:` lines; every other line carries\n`HH:MM:SS` alone. The parser carries the date forward and rolls it over at midnight \u2014 get that\nwrong and you inject a -86,000 s gap into the timing-split rule.",
        "operationId": "get_session_log_api_sessions__session_id__log_get",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Session Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LogResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/sessions/{session_id}/texture": {
      "get": {
        "tags": [
          "core"
        ],
        "summary": "Get Session Texture",
        "description": "\u2b50 **THE MEASUREMENT, AND IT IS CORE.** `std(DoG(3,30))` answers *\"how much texture is in this\nimage\"*, which is a property of the FRAME \u2014 every future feature asks it.\n\n\u26d4 **There is no threshold here, no list, and no policy.** Turning a number into a *proposal* is a\nfeature's job (`POST /api/mosaic/screen/propose`); turning a proposal into a *decision* is the\nhuman's, and it lands in his document. Never in the session.",
        "operationId": "get_session_texture_api_sessions__session_id__texture_get",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Session Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TextureResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/sessions/{session_id}/tone": {
      "get": {
        "tags": [
          "core"
        ],
        "summary": "Get Tone",
        "operationId": "get_tone_api_sessions__session_id__tone_get",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Session Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Tone"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      },
      "put": {
        "tags": [
          "core"
        ],
        "summary": "Put Tone",
        "description": "\u26a0\ufe0f\u26a0\ufe0f **GLOBAL, NEVER PER-TILE.** A per-tile percentile stretch over-brightens near-empty frames\nand makes overlapping tiles disagree in brightness \u2014 **which destroys the Difference-mode check\nthe whole verification loop depends on.** There is no per-tile path and there must never be one.\n\nTone is display-only: it never touches the matcher (which sees band-passed, mean-subtracted\npixels) and it never touches the exported TIFF.",
        "operationId": "put_tone_api_sessions__session_id__tone_put",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Session Id"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ToneUpdate"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Tone"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/sessions/{session_id}/tiles/{trial}.png": {
      "get": {
        "tags": [
          "core"
        ],
        "summary": "Get Tile Png",
        "description": "8-bit grayscale PNG: flat-fielded, through the **one global** tone window.",
        "operationId": "get_tile_png_api_sessions__session_id__tiles__trial__png_get",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Session Id"
            }
          },
          {
            "name": "trial",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Trial"
            }
          },
          {
            "name": "v",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "V"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "image/png": {}
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/sessions/{session_id}/tiles/{trial}.raw": {
      "get": {
        "tags": [
          "core"
        ],
        "summary": "Get Tile Raw",
        "description": "uint16 **little-endian**, row-major, **ALREADY FLIPPED** per this acquisition's XML, and\n**RAW CAMERA COUNTS** \u2014 no flat-field, no tone. This is pixel data, not a picture.",
        "operationId": "get_tile_raw_api_sessions__session_id__tiles__trial__raw_get",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Session Id"
            }
          },
          {
            "name": "trial",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Trial"
            }
          },
          {
            "name": "v",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "V"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/octet-stream": {}
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/sessions/{session_id}/thumbs.png": {
      "get": {
        "tags": [
          "core"
        ],
        "summary": "Get Thumbs Png",
        "description": "The contact sheet: ONE sprite sheet, `grid = ceil(sqrt(n))`, row-major in trial order.\nSame **global** window \u2014 a per-tile stretch here would make the sheet lie about which frames are\ndim, which is the one thing the Screen step exists to show him.",
        "operationId": "get_thumbs_png_api_sessions__session_id__thumbs_png_get",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Session Id"
            }
          },
          {
            "name": "v",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "V"
            }
          },
          {
            "name": "cell",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "default": 64,
              "title": "Cell"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "image/png": {}
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/sessions/{session_id}/thumbs.json": {
      "get": {
        "tags": [
          "core"
        ],
        "summary": "Get Thumbs Json",
        "description": "Cell `i` holds `trials[i]` at `(x, y) = ((i % grid) * cell, (i // grid) * cell)`.",
        "operationId": "get_thumbs_json_api_sessions__session_id__thumbs_json_get",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Session Id"
            }
          },
          {
            "name": "cell",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "default": 64,
              "title": "Cell"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ThumbsResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/projects": {
      "get": {
        "tags": [
          "core"
        ],
        "summary": "Get Projects",
        "description": "**The home screen.** Every project in Camea's store (R44).\n\n\u26a0\ufe0f A project folder whose manifest cannot be read is reported in `unreadable`, **not** silently\ndropped and **not** a failure of the whole listing: one corrupt file must cost the user that\nproject's card, never his home screen.\n\n\u2b50 `migration` is set on the first launch after R44 and null on every launch after that. It is\nstated once, on the home screen, because those projects moved out of folders the user chose.",
        "operationId": "get_projects_api_projects_get",
        "parameters": [
          {
            "name": "dataset_key",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Dataset Key"
            }
          },
          {
            "name": "feature",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Feature"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AnalysisListResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      },
      "post": {
        "tags": [
          "core"
        ],
        "summary": "Post Analyses",
        "description": "\u2b50 **THE SERVER CREATES THE DOCUMENT**, via the feature's `new_payload` hook.\n\n\ud83d\udd34 In v1 `new_doc()` and `seed_from_build()` were **dead code** \u2014 the front end reimplemented both\nin JavaScript. That is how `human_edits`' divert counters were silently dropped on every save, and\nhow *\"Skip \u2014 place by hand\"* could erase `seeded_from` while every tile still sat exactly where\nt33 put it. **The document is authored on the server. It is not negotiable.**\n\n\u2b50 The document's `id` **is** the `analysis_id` \u2014 `workspace._guard_slot` refuses any document\nwhose `id` is not the analysis it is being written into, and that is what makes v1's autosave slot\ncollision (pass 2 silently overwriting pass 1's ground-truth records) impossible rather than\nmerely unlikely.",
        "operationId": "post_analyses_api_projects_post",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateAnalysisRequest"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AnalysisSummary"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/projects/{analysis_id}": {
      "get": {
        "tags": [
          "core"
        ],
        "summary": "Get Project",
        "description": "ONE project, by id. What `/project/:id` opens.\n\n\u2b50 It asks for **this** project rather than filtering the whole listing, and that is not just\ncheaper: a **draft** is reachable but deliberately not listed (R43.3), so a screen that found\nprojects by scanning the list could not open a video mosaic that is still building.\n\n\u26a0\ufe0f Declared AFTER `/api/projects/folder` so the literal route keeps winning that path.",
        "operationId": "get_project_api_projects__analysis_id__get",
        "parameters": [
          {
            "name": "analysis_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Analysis Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AnalysisSummary"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      },
      "patch": {
        "tags": [
          "core"
        ],
        "summary": "Rename Analysis",
        "description": "Rename a project \u2014 the project manager's rename. \u2b50 Rewrites the manifest ONLY; the folder\nnever moves and the `analysis_id` is forever (a rename that moved the folder would break the slot\nguard, every path the document carries, and any Explorer window he has open on it).",
        "operationId": "rename_analysis_api_projects__analysis_id__patch",
        "parameters": [
          {
            "name": "analysis_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Analysis Id"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RenameAnalysisRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AnalysisSummary"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      },
      "delete": {
        "tags": [
          "core"
        ],
        "summary": "Delete Analysis",
        "description": "\u2b50 **DELETE MEANS DELETE** (R44) \u2014 the project and everything in it, including its outputs.\n\n\u26a0\ufe0f **`delete_files` is gone, and with it R42.8's Remove-vs-Delete.** That distinction existed\nbecause the folder was one the user had named and might hold his own files: *remove* took the\ncard off the home screen and left every byte. In an app-owned store there is no such folder and\nno such choice \u2014 a project the app is not listing is a project nobody can ever reach again, so\n\"remove the card\" and \"delete the work\" are the same act, and pretending otherwise would just\naccumulate unreachable bytes on his C: drive.\n\n\ud83d\udd34 The **confirmation is the front end's job** and it is not optional; this route does what it\nis told. `Project.delete` is where the rmtree target is re-checked against the store.",
        "operationId": "delete_analysis_api_projects__analysis_id__delete",
        "parameters": [
          {
            "name": "analysis_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Analysis Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/OkResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/projects/{analysis_id}/outputs": {
      "get": {
        "tags": [
          "core"
        ],
        "summary": "Get Outputs",
        "description": "\u2b50 **WHAT THIS PROJECT HAS BUILT** \u2014 newest first. The outputs browser's whole source.\n\nRead off the directory every call, never from the document: `build.outputs` records what the\nlast build *wrote*, and this must answer what is *there*. A file deleted underneath us, or one\nan older Camea wrote under a different name, has to show up as it actually is.\n\nAn empty list is a normal answer (nothing built yet), not a 404.",
        "operationId": "get_outputs_api_projects__analysis_id__outputs_get",
        "parameters": [
          {
            "name": "analysis_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Analysis Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/OutputListResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/projects/{analysis_id}/outputs/{name}": {
      "get": {
        "tags": [
          "core"
        ],
        "summary": "Get Output File",
        "description": "One output's bytes \u2014 the preview in the browser, and the click-to-open.\n\n`download=true` sets `Content-Disposition: attachment`, which is how the web build lets the user\ntake a copy without a native dialog. `no-store`, because a rebuild overwrites in place.",
        "operationId": "get_output_file_api_projects__analysis_id__outputs__name__get",
        "parameters": [
          {
            "name": "analysis_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Analysis Id"
            }
          },
          {
            "name": "name",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Name"
            }
          },
          {
            "name": "download",
            "in": "query",
            "required": false,
            "schema": {
              "type": "boolean",
              "default": false,
              "title": "Download"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response"
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/projects/{analysis_id}/outputs/copy": {
      "post": {
        "tags": [
          "core"
        ],
        "summary": "Post Copy Outputs",
        "description": "\u2b50 **THE ONE WAY WORK LEAVES CAMEA** (R44) \u2014 copy the chosen outputs into a folder the user\nnames, right now, while looking at them.\n\n\ud83d\udd34 **THE THREE REFUSALS, IN ORDER, BEFORE A SINGLE BYTE IS WRITTEN:**\n  1. `refuse_write(dest)` \u2014 \u26d4 never onto the evidence. A destination inside `data/`, inside a\n     raw acquisition folder, or inside one on the way up is refused, exactly as every other\n     write in the app is. (The **repo** is *not* refused here: root `output/` has always been a\n     legitimate place to put an export, and this is an export.)\n  2. Every name is resolved through `_output_file` \u2014 so a name that is not this project's\n     output cannot be copied out of it.\n  3. \u26d4 **Nothing at the destination is overwritten.** A clash refuses the whole request and\n     names the files, rather than half-copying and destroying one of his. The destination is\n     his folder; this route does not get to be the reason something in it is gone.\n\nA copy is not a move: the project keeps its outputs. That is the point \u2014 the store stays the\nhome, and what leaves is a copy the user asked for.",
        "operationId": "post_copy_outputs_api_projects__analysis_id__outputs_copy_post",
        "parameters": [
          {
            "name": "analysis_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Analysis Id"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CopyOutputsRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CopyOutputsResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/analyses/{analysis_id}/document": {
      "get": {
        "tags": [
          "core"
        ],
        "summary": "Get Document",
        "description": "`recovered=true` reads the **autosave** instead \u2014 ask `Workspace.recovery()` first: an autosave\nNEWER than the document is a recovery prompt, an older one is noise.",
        "operationId": "get_document_api_analyses__analysis_id__document_get",
        "parameters": [
          {
            "name": "analysis_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Analysis Id"
            }
          },
          {
            "name": "recovered",
            "in": "query",
            "required": false,
            "schema": {
              "type": "boolean",
              "default": false,
              "title": "Recovered"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DocumentResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      },
      "put": {
        "tags": [
          "core"
        ],
        "summary": "Put Document",
        "description": "\u26a0\ufe0f **THE ORDER, ON THE SERVER: structural-validate -> normalise -> stamp -> full-validate ->\nwrite, atomically.** Not `validate -> normalise`: the derived fields are exactly the ones that\ndrift the moment the user excludes a tile, and `normalise` is what repairs them \u2014 validating them\nfirst rejects a perfectly good document for drift the very next line of code fixes.\n(`core.document.prepare` owns that order; this route only picks the destination.)",
        "operationId": "put_document_api_analyses__analysis_id__document_put",
        "parameters": [
          {
            "name": "analysis_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Analysis Id"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SaveDocumentRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SaveResult"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/analyses/{analysis_id}/autosave": {
      "post": {
        "tags": [
          "core"
        ],
        "summary": "Post Autosave",
        "description": "The crash net. Debounced 2 s, **plus unconditionally on every `A` and `E`**.\n\n\ud83d\udd34 **A FAILURE IS LOUD** \u2014 it is never swallowed. (`localStorage` failed *silently* in the sandbox\nand nearly cost a day's work; that is why the crash net is a server file at all.)\n\n\ud83d\udd34 **THE SLOT GUARD RUNS HERE, AND IN v1 IT DID NOT** \u2014 `project.autosave()` carried it and nothing\never called it. Pass 2's autosave once silently overwrote pass 1's ground-truth records. A\ndocument whose `id`/`dataset_key` disagrees with the slot's is refused with `409 range_mismatch`.\nIt is not merged, not renamed, not \"repaired\".",
        "operationId": "post_autosave_api_analyses__analysis_id__autosave_post",
        "parameters": [
          {
            "name": "analysis_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Analysis Id"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AutosaveRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SaveResult"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/documents/load": {
      "post": {
        "tags": [
          "core"
        ],
        "summary": "Post Document Load",
        "description": "*\"Load a project\u2026\"* \u2014 and it **must work COLD**, with no session open. That is the whole point:\nthe app remembers nothing between launches, so this file is its only memory. Save -> quit ->\nload restores the session whole, and nothing else does.\n\nThe server bootstraps a session from the file's own `data_dir` when one is not given, then\nre-reads the file **against that session's scope**, so the range guard actually runs against the\nsession the document belongs to.",
        "operationId": "post_document_load_api_documents_load_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/LoadDocumentRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LoadDocumentResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/documents/save-as": {
      "post": {
        "tags": [
          "core"
        ],
        "summary": "Post Document Save As",
        "description": "**`Save\u2026`** \u2014 a file the user names, reachable from EVERY screen (not buried behind the last\nstep: since the app carries no dataset knowledge, this file is its only memory, and the one\nartefact that makes a session resumable must not be the one thing he cannot reach mid-sweep).\n\n\u26d4 Refused inside `data/`, inside the repo's dataset mirror, or inside the dataset itself.",
        "operationId": "post_document_save_as_api_documents_save_as_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SaveDocumentRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SaveResult"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/documents/validate": {
      "post": {
        "tags": [
          "core"
        ],
        "summary": "Post Document Validate",
        "description": "\u26d4 **NO TRIAL NUMBER IS SPECIAL.** Nothing here may reject a document for WHICH trials it\nplaced. The guard that used to live in v1 (\"tile 284 is thrown out and carries a position\") made\nthe user's own session **unsaveable** the moment he anchored 284.",
        "operationId": "post_document_validate_api_documents_validate_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ValidateDocumentRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ValidationReport"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/jobs": {
      "get": {
        "tags": [
          "core"
        ],
        "summary": "Get Jobs",
        "operationId": "get_jobs_api_jobs_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JobListResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/jobs/{job_id}": {
      "get": {
        "tags": [
          "core"
        ],
        "summary": "Get Job",
        "description": "Poll at 500 ms. There is no websocket \u2014 it is one client on localhost and a poll is 0.6 ms.\n\n\u26a0\ufe0f **`eta_s` is only recomputed when the child prints a recognised line.** Re-anchor the UI's\ncountdown **only when this raw number changes**; re-anchoring on every tick resets the clock and\nthe countdown never moves. And it may **jump UP** \u2014 an honest revision beats a smooth lie.",
        "operationId": "get_job_api_jobs__job_id__get",
        "parameters": [
          {
            "name": "job_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Job Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Job"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/jobs/{job_id}/cancel": {
      "post": {
        "tags": [
          "core"
        ],
        "summary": "Post Job Cancel",
        "description": "Idempotent. A finished job is a **409**, not a lie.\n\nA process job's cancel is `terminate()` \u2014 that is not a shortcut, it is the only cancel there is:\n`t33.place` runs 25 s \u2013 10 min synchronously with no callback and nothing inside it that checks a\nflag.",
        "operationId": "post_job_cancel_api_jobs__job_id__cancel_post",
        "parameters": [
          {
            "name": "job_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Job Id"
            }
          }
        ],
        "responses": {
          "202": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JobCancelResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/dialog/open-directory": {
      "post": {
        "tags": [
          "core"
        ],
        "summary": "Post Dialog Open Directory",
        "operationId": "post_dialog_open_directory_api_dialog_open_directory_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DialogOpenDirectoryRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DialogPathResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/dialog/open-file": {
      "post": {
        "tags": [
          "core"
        ],
        "summary": "Post Dialog Open File",
        "description": "\u2b50 `allow_multiple` arrived 2026-08-14 (plan 002): *\"opens file explorer, can import multiple\nat a time\"*. It was hard-coded `False` here, which is still the default, so nothing that already\ncalled this route changes \u2014 and `path` still carries the first choice beside the new `paths`.\n\n\u26a0\ufe0f Reachable only with `--window`. He drives Camea over VSCode remote, where this is a 501 and\nthe served tick-list (`GET /api/mea/browse`) is the picker he actually meets.",
        "operationId": "post_dialog_open_file_api_dialog_open_file_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DialogOpenFileRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DialogPathResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/dialog/save-file": {
      "post": {
        "tags": [
          "core"
        ],
        "summary": "Post Dialog Save File",
        "operationId": "post_dialog_save_file_api_dialog_save_file_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DialogSaveFileRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DialogPathResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/mosaic/run": {
      "post": {
        "tags": [
          "mosaic"
        ],
        "summary": "Post Run",
        "description": "`POST /api/mosaic/run` \u2014 detect the run and the pass split, **or override them.**\n\n\u2b50 **NOT A SESSION RELOAD.** v1 made this a full re-open (5 s, and it invalidated the tone and\nthe blank scan) because the run lived *on* the session. It does not any more: this is a pure\nfunction of the session's inventory, and it costs milliseconds.\n\n\u26a0\ufe0f `gaps` is **derived** and is returned with the run, because the caller needs it the moment\nthe trial list changes: a \"consecutive\" pair across a gap is a multi-step stage jump, and the\nserpentine one-axis step prior does **not** hold there. Miss this and the next solve is silently\npoisoned.",
        "operationId": "post_run_api_mosaic_run_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RunDetectRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RunDetection"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/mosaic/document/rescope": {
      "post": {
        "tags": [
          "mosaic"
        ],
        "summary": "Post Rescope",
        "description": "`POST /api/mosaic/document/rescope` \u2014 \u2b50 **the Range step's `Apply`, made to STICK.**\n\n`POST /run` measures; this one writes. A project opens on every square snapshot the dataset holds,\nand on a real acquisition that includes the stray snapshots taken before the mosaic scan started\n(`1`, `5-7` on 260620d, ahead of the run at `11-348`). Until now `Apply` re-measured the run and\nleft them in the document, so they were swept, solved and exported as tiles of a mosaic they were\nnever part of. This re-authors the tile set to exactly the trials in range.\n\n\u26d4 **NO DATASET KNOWLEDGE.** The trial list is `_detect_run` over `log.txt` + the per-trial XML\nshape, bounded by the range the USER typed. No number is named here, and nothing is defaulted for\n\"his\" dataset.\n\u26d4 **A DROPPED TRIAL IS NOT AN EXCLUSION.** It does not land in `unusable_tiles` and it does not\ncount as a human edit: it was never a tile of this mosaic. `E` remains the only thing that\nexcludes.\n\n\ud83d\udd34 Every surviving tile keeps its position, state and provenance. The reply's `n_placed_removed`\nis the work that was thrown away \u2014 the caller confirms **before** calling (the Range step does).",
        "operationId": "post_rescope_api_mosaic_document_rescope_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RescopeRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RescopeResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/mosaic/gaps": {
      "post": {
        "tags": [
          "mosaic"
        ],
        "summary": "Post Gaps",
        "description": "`POST /api/mosaic/gaps` \u2014 a **pure function over a trial list.**\n\n\u2b50 `engine.excluded.gaps()` is the ONLY symbol the app may import from that module \u2014 never\n`EXCLUDED` / `BLANK` / `BLURRY` / `usable_trials`. There is no toggle. **This route is the single\nplace the app touches it**, it reaches it through `camea.core.dataset.gaps`, and it is called on\nevery change to the excluded set.",
        "operationId": "post_gaps_api_mosaic_gaps_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/GapsRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GapsResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/mosaic/screen/propose": {
      "post": {
        "tags": [
          "mosaic"
        ],
        "summary": "Post Screen Propose",
        "description": "`POST /api/mosaic/screen/propose` \u2014 \u2b50 **IT RECOMMENDS. THE HUMAN TICKS. NOTHING IS\nAUTO-EXCLUDED.**\n\nThe *measurement* is core's (`FrameStore.texture()` \u2014 `std(DoG(3,30))`, a property of the frame,\nand free because the matcher's band stack and this measure are the same array). The *policy* \u2014\n\"the 2nd percentile of PASS-1 texture\" \u2014 is the mosaic's, because `pass 1` only exists because\n`pass_split` does. The *decision* is the human's and it lands in the **document**, not here.\n\n\u26a0\ufe0f\u26a0\ufe0f **ZERO MARGIN AT THE BOUNDARY, AND IT IS THE WHOLE REASON THIS MAY ONLY PROPOSE.**\nGenuinely-blank frames and perfectly good ones **interleave** below the threshold. Measured on\n260620d with the refusal lifted, the four trials the scan named landed **0.24 px**, **0.18 px**,\n**2.07 px** and **679 px** from the human truth. Three of four are ordinary tiles. One of four is\nreal. A rule that cannot do better than that does not get to reject anything.\n\n\u274c **NO BLUR JUDGEMENT. EVER.** Across all 338 snapshots and 15 focus measures the best global\nblur threshold reaches **F1 = 0.37**, and variance-of-Laplacian \u2014 the textbook autofocus metric \u2014\nscores **worse than chance**. No slider, no number, nowhere in the UI. Blur is his eye, in the\nsweep, with `E`.",
        "operationId": "post_screen_propose_api_mosaic_screen_propose_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/BlankProposeRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/BlankProposal"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/mosaic/build": {
      "post": {
        "tags": [
          "mosaic"
        ],
        "summary": "Post Build",
        "description": "`POST /api/mosaic/build` -> **202 `JobRef`**. Poll `GET /api/jobs/{id}`.\n\nIt runs in a **CHILD PROCESS** and holds the **`gpu` lease** for its whole run. Both are\nforced, not chosen:\n\n* `t33.place` runs 25 s \u2013 10 min **synchronously**, has **no progress callback of any kind**,\n  and has **nothing inside it that checks a flag** \u2014 so there is no cooperative cancel to hook.\n  `proc.terminate()` is the only cancel that can work. That is the whole reason for the process.\n* The parent may hold ~2 GB of device memory from interactive matching and the child needs ~2 GB\n  more. On a 4 GB card the lease is the difference between a build and an OOM \u2014 so we also\n  **release the parent's context before spawning**.\n\n`config: null` = t33's shipped **312/312 defaults**, and that is the one-button path.\n\u26a0\ufe0f `pass_split` is the **DETECTED** value the caller sends, never t33's literal 166.",
        "operationId": "post_build_api_mosaic_build_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/BuildStartRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "202": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JobRef"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/mosaic/builds/{build_id}": {
      "get": {
        "tags": [
          "mosaic"
        ],
        "summary": "Get Build",
        "description": "`GET /api/mosaic/builds/{build_id}` \u2014 a finished build's full result.\n\n\u26a0\ufe0f `per_tile` is a **\"go look here first\" list, not a verdict** \u2014 and \ud83d\udd34 **pass-1 tiles have no\nper-tile confidence at all** (t27's info is aggregate-only), while the worst tile in the shipped\n312/312 build (trial 127, at 9.94 px) **is a pass-1 tile.** The absence of a warning is not a\nclean bill of health, and the UI must say so.",
        "operationId": "get_build_api_mosaic_builds__build_id__get",
        "parameters": [
          {
            "name": "build_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Build Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/BuildResult"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/mosaic/seed": {
      "post": {
        "tags": [
          "mosaic"
        ],
        "summary": "Post Seed",
        "description": "`POST /api/mosaic/seed` \u2014 apply a build to a document. **The SERVER does this, not the JS.**\n\n\ud83d\udd34 **A RE-SOLVE MUST NOT DESTROY THE HUMAN'S WORK.** `document.seed_from_build` keeps every tile\nthat is `anchored` **or** flagged `human` and seeds everything else *around* them, translating\nthe build onto the human's field by the **MEDIAN** offset over the protected tiles \u2014 a median,\nnot a mean, because a tile the human corrected *because the solver was wrong* is precisely an\noutlier, and one 2,969 px correction would drag a mean into nonsense. If none of the human's\ntiles are in the build the two frames cannot be tied together, and the server **refuses to\nseed** rather than guess.\n\n(v1 reimplemented all of this in `sweep.js` and called `setState` unconditionally on every\nnon-excluded tile \u2014 so a 150-tile sweep with three catastrophic hand-corrections in it was wiped\nby taking the app's own advice to re-solve, and the autosave then wrote the wiped document over\nthe crash-recovery file.)",
        "operationId": "post_seed_api_mosaic_seed_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SeedRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SeedResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/mosaic/document/machine-evidence": {
      "post": {
        "tags": [
          "mosaic"
        ],
        "summary": "Post Machine Evidence",
        "description": "\u26a0\ufe0f **DERIVED FROM THE DOCUMENT'S HISTORY, NEVER FROM WHAT IT SAYS ABOUT ITSELF** \u2014 a `build`\nblock, or a single tile still carrying a `machine` position. `seeded_from` is writable, and\n\"Skip \u2014 place from scratch\" once *erased it* while every tile kept t33's answer.\n\n\ud83d\udd34 **This project has already destroyed one benchmark exactly this way**\n(`analysis/archive/challenge_2026-07/benchmark/ground_truth/260620d.json` **is T27's own\noutput**). Powers the provenance panel \u2014 which is a **live warning about current state**, and so\nit stays on the page rather than hiding behind a `?`.",
        "operationId": "post_machine_evidence_api_mosaic_document_machine_evidence_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/MachineEvidenceRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MachineEvidenceResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/mosaic/document/discard-machine": {
      "post": {
        "tags": [
          "mosaic"
        ],
        "summary": "Post Discard Machine",
        "description": "*\"Skip \u2014 place by hand\"*. \ud83d\udd34 **IT IS DESTRUCTIVE, OR IT IS NOTHING.**\n\nIf anything in the document came from a machine it discards **every position and the build**.\nOtherwise it launders a machine build into an \"independent ground truth\": v1 nulled `build`,\nnulled `seeded_from`, set `independent_of_method: true`, deleted the warning \u2014 and **did not\ntouch a single tile.** Every tile kept t33's position. Score t33 against that and it gets ~100 %\n**by construction.**\n\n`confirm` is `Literal[True]` in the schema: there is no path through this route by accident.",
        "operationId": "post_discard_machine_api_mosaic_document_discard_machine_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DiscardMachineRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DiscardMachineResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/mosaic/match/anchor": {
      "post": {
        "tags": [
          "mosaic"
        ],
        "summary": "Post Match Anchor",
        "description": "\u2b50\u2b50 **THE PRIMITIVE. ONE ENDPOINT, FOUR FEATURES**: place the next tile (`Space`), the ranked\nalternatives (`V`, `1`\u2013`9`), rescue an unplaced tile, and snap a drag (`mode: \"local\"`).\n\n*Why a composite is the right primitive and not merely a convenient one:* of 719 genuinely\noverlapping tile PAIRS on this data the exact-NCC argmax is **> 20 px wrong for 38 of them (5 %),\nat scores up to 0.760** \u2014 one 0.760 winner is **757 px wrong** and the truth is the runner-up at\n0.677. The **same tile against a composite** scores 0.654 vs 0.416 next-best and lands **1.7 px**\nfrom the human. **Aperture is everything**, and the anchor field the user is building is a big,\nhuman-certified one.\n\n\u26d4 **BLANK \u21d2 REFUSED, NOT SCORED.** 200, with `candidates: []`, `best: null` and a populated\n`refused`. **There is no `force` flag and there will not be one.** Two blank frames 136 trials\napart correlate **+0.43 at zero shift** (honest noise floor 0.115) because what they share is\n*fixed-pattern sensor structure*, which does not move with the stage \u2014 **they register\nconfidently and wrongly.** A blank *anchor*, by contrast, is **dropped from the composite and is\nnot fatal**: the rule that made it an error dead-ended the whole app, the moment the user\nanchored the first near-threshold trial the sweep refused **forever**, and it died one tile later.\n\n\u2b50 **A plain `def`, not `async def`** \u2014 Starlette runs it in its thread pool, which is what stops\nan in-flight prefetch from blocking the foreground request the user is actually waiting on.",
        "operationId": "post_match_anchor_api_mosaic_match_anchor_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/MatchAnchorRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MatchResult"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/mosaic/match/score": {
      "post": {
        "tags": [
          "mosaic"
        ],
        "summary": "Post Match Score",
        "description": "`POST /api/mosaic/match/score` \u2014 *\"you dropped it here; what do the pixels say?\"*\n\nOne `exact_ncc`, no search (~31 ms). Used live during a drag (debounced), to label the\nalternatives, and to **re-measure a diverted tile's NCC where it actually sits** \u2014 because\n`TileRecord.ncc` is defined as the NCC **at the tile's final position**, on every path, and\nwriting the rejected alias's score there would attribute it to the position that shipped.\n\n\u26a0\ufe0f `ncc` is **`null`** below `exact_ncc`'s overlap floor. The honest answer is *\"not\nmeasurable\"* \u2014 **never `0.0`**.\n\n\u2b50 Deliberately **NOT** serialised behind an in-flight `match/anchor`: it used to wait out a whole\nmatch, so the live NCC under the cursor was up to a full match out of date.",
        "operationId": "post_match_score_api_mosaic_match_score_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/MatchScoreRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ScoreResult"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/mosaic/recheck": {
      "post": {
        "tags": [
          "mosaic"
        ],
        "summary": "Post Recheck",
        "description": "`POST /api/mosaic/recheck` -> **202 `JobRef`**. It is N x ~1 s, so it never blocks.\n\n\ud83d\udd34 **THE RE-CHECK IS GLOBAL, AND IT IS ALLOWED TO SAY NO.** It **never moves a tile** \u2014 it only\nmeasures it. Anything that disagrees with where the tile sits by more than `tolerance_px`\n**STAYS FLAGGED**, and the caller clears only the flags it is told it may clear.\n\n\u26a0\ufe0f **IT MUST BE GLOBAL, AND THIS IS WHY.** v1 fired a *local* match (\u00b164 px) and then cleared\n`stale` unconditionally. But a tile knocked off by a moved anchor is off by **hundreds** of px \u2014\nfailure on this data is **binary**, sub-pixel-right or wildly wrong \u2014 so a \u00b164 px window is\n*structurally blind to the one error the panel exists for*. Measured: a tile **380 px** from the\ntruth re-checked locally to `ncc -0.0678`, was not moved, and had its flag **cleared** \u2014 the loud\npanel disappeared, the tile stayed 380 px wrong, and a negative NCC went into the document as its\nevidence.",
        "operationId": "post_recheck_api_mosaic_recheck_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RecheckRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "202": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JobRef"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/mosaic/recompute": {
      "post": {
        "tags": [
          "mosaic"
        ],
        "summary": "Post Recompute",
        "description": "`POST /api/mosaic/recompute` -> **202 `JobRef`**. \u2b50 **THE TOOL THE USER REACHES FOR.**\n\nFreeze the tiles the human has anchored and re-place every other tile against their combined\ncomposite \u2014 then he anchors more and recomputes again. It is `recheck`'s per-target\n`match_anchor(global)` loop (same `gpu` lease, same progress/cancel, N \u00d7 ~1 s so it never blocks),\nbut it **writes** the answer instead of only measuring it.\n\n\ud83d\udd34 **It never moves an `anchored`, `human`, or `excluded` tile** \u2014 those are the frozen reference\nand the human's own work (the re-solve-wipes-the-sweep bug `seed_from_build` exists to prevent).\nEvery placed tile lands `unverified` + `machine`; **nothing is auto-anchored** (I3), and the\nmachine fingerprint keeps provenance honest. A target with no measurable overlap against the\nanchors is **left untouched** \u2014 its previous placement stands; anchor a neighbour and recompute.\n\n\u26a1 The anchor set is FIXED across all targets, so `solve`'s incremental composite cache builds the\nreference composite **once** and reuses it for every match.",
        "operationId": "post_recompute_api_mosaic_recompute_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RecomputeRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "202": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JobRef"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/mosaic/export": {
      "post": {
        "tags": [
          "mosaic"
        ],
        "summary": "Post Export",
        "description": "`POST /api/mosaic/export` -> **202 `JobRef`**. Holds the `gpu` lease.\n\n\u2b50\u2b50 **TIFF AND PNG ARE TWO DIFFERENT RENDERS**, and `flat` decides what the pixels *are*:\n`flat=True` is a *picture* (per-tile gain); `flat=False` is **raw camera counts** = the TIFF. The\nTIFF was once rendered `flat=True` while its header said RAW \u2014 trial 11's median went 2111 ->\n3435 (x1.63).\n\n\u26a0\ufe0f\u26a0\ufe0f **THE COVERAGE MASK IS MANDATORY, NOT AN OPTION.** 13.1 % of the canvas is background\nencoded as exactly `0.0`, indistinguishable from a legitimately black pixel, and a TIFF has **no\nalpha channel**. Without the sidecar, \"empty\" and \"black\" merge forever. Asking for `tiff`\nimplies `coverage`; the exporter writes it either way.\n\n\ud83d\udd34 The export describes the document **AS EXPORTED** (the *stamped* one), never as posted. A\nlaundered doc once produced a GT JSON saying \"NOT AN INDEPENDENT GROUND TRUTH\" beside a TIFF\nheader saying \"hand-placed from scratch\".",
        "operationId": "post_export_api_mosaic_export_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ExportRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "202": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JobRef"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/mosaic/qc": {
      "post": {
        "tags": [
          "mosaic"
        ],
        "summary": "Post Qc",
        "description": "`POST /api/mosaic/qc` \u2014 what the human did vs what the machine said.\n\n\u2b50 **EVERY NUMBER STATES ITS DENOMINATOR**, and the denominator is the document's own `excluded`\nset and nothing else \u2014 the app has no list of its own. (A percentage without one is how the\n182-vs-156 confusion started.)\n\n\u26a0\ufe0f `diverted_to_solver` is counted **separately and named next to the number it contaminates**: a\ndiverted tile *is* sitting exactly on the machine's position, so it lands inside\n`accepted_unchanged` and reads as agreement. It is not. The app never gave the matcher a vote\nthere. In the defer flow this can be **302 of 311 tiles**.",
        "operationId": "post_qc_api_mosaic_qc_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/QcRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/QcReport"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/mosaic/electrodes/map": {
      "post": {
        "tags": [
          "mosaic"
        ],
        "summary": "Post Electrodes Map",
        "description": "`POST /api/mosaic/electrodes/map` -> **202 `JobRef`**. Optional post-build stage: render\nthe placed tiles (the export's own compositor) and fit the electrode lattice \u2014 pitch,\nrotation and phase MEASURED from this mosaic, never assumed (no dataset knowledge).\n\n\u2b50 Files land in the project's `outputs/` (R44), `<name>-electrodes.json/.csv`; the job result\ncarries the summary block for the CLIENT to merge into its document and save (this feature's\ndocument is client-owned, like export and unlike the videomosaic build).\n\n\u2b50 `array_coverage` is HIS declaration (R45.8), made before the button is even enabled:\n`\"full\"` holds the fit to the MaxOne/MaxTwo's 220 \u00d7 120 (a near miss corrected and reported,\na far miss refused by the fitter); `\"partial\"` enforces only the 17.5 \u00b5m scale and leaves\n`1-1` meaning the top-left of the IMAGED REGION. Defaulted `\"partial\"` here so an older\nclient that does not send it cannot accidentally claim a whole chip.\n\nHolds the `gpu` lease: the render walks the same frame store the export does.",
        "operationId": "post_electrodes_map_api_mosaic_electrodes_map_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ElectrodeMapRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "202": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JobRef"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/mosaic/electrodes/{analysis_id}": {
      "get": {
        "tags": [
          "mosaic"
        ],
        "summary": "Get Electrodes",
        "description": "The full electrode map of a project, plus `stale` \u2014 computed against the SAVED document's\ncurrent tile positions, so an edited/recomputed mosaic says \"re-run\" instead of silently\nhighlighting drifted centres. (Unsaved client edits are the client's own staleness to track.)",
        "operationId": "get_electrodes_api_mosaic_electrodes__analysis_id__get",
        "parameters": [
          {
            "name": "analysis_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Analysis Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ElectrodeMapPayload"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/videomosaic/probe": {
      "post": {
        "summary": "Post Probe",
        "description": "The receipt: does this path decode as video, and what does it claim to contain?\nProves a frame actually decodes \u2014 'probed OK' can be trusted by Create.",
        "operationId": "post_probe_api_videomosaic_probe_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/VideoProbeRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/VideoSource"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/videomosaic/projects": {
      "post": {
        "summary": "Post Video Project",
        "description": "\u2b50 THE SERVER CREATES THE DOCUMENT \u2014 `POST /api/projects` for a video source.\n\nNo session: the video is probed here (a real decode, not a header parse) and its receipt\nbecomes the document's `source`.\n\n\u2b50 **AND NO FOLDER (R44).** The project is made in Camea's store, listed on the home screen from\nthis moment, and its build writes into its own `outputs/`. There is nothing left to ask.",
        "operationId": "post_video_project_api_videomosaic_projects_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateVideoProjectRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AnalysisSummary"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/videomosaic/build": {
      "post": {
        "summary": "Post Build",
        "description": "The whole pipeline as one cancellable job: track \u2192 keyframes \u2192 register \u2192 solve \u2192\nrender \u2192 save.\n\n\u2b50 The artifacts land in the project's **`outputs/`, named after the project** (R44) \u2014 the one\nplace the outputs browser reads for every feature, and names that still say what they are when\nthe user copies one out to his desktop. The document is updated and saved by the JOB\n(server-side), so the result the UI polls out is already durable.",
        "operationId": "post_build_api_videomosaic_build_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/VideoBuildRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "202": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JobRef"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/videomosaic/{analysis_id}/outputs/{name}": {
      "get": {
        "summary": "Get Output",
        "description": "One built artifact, addressed by its LOGICAL name (`mosaic.png` / `preview.png` /\n`positions.csv` / `build.json`) and resolved to the real file through the document's\n`build.outputs` \u2014 on disk it lives in `outputs/`, named after the project (R44).\n\n`v` is the cache-buster the UI appends (the build's `built_at`); the response itself says\nno-store because a rebuild overwrites in place.",
        "operationId": "get_output_api_videomosaic__analysis_id__outputs__name__get",
        "parameters": [
          {
            "name": "analysis_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Analysis Id"
            }
          },
          {
            "name": "name",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Name"
            }
          },
          {
            "name": "v",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "V"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response"
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/videomosaic/electrodes/map": {
      "post": {
        "summary": "Post Electrodes Map",
        "description": "`POST /api/videomosaic/electrodes/map` -> **202 `JobRef`**. Fit the electrode lattice of\nthe built `mosaic.png` \u2014 its canvas is 1:1 with video pixels, so the map needs no offset.\n\n\u2b50 Same artifacts and block as the mosaic feature (`<name>-electrodes.json/.csv` in\n`outputs/`, R44), but here the DOCUMENT IS SERVER-OWNED: the job saves `electrodes` into it\nand returns the saved doc, exactly like the build job. `source_stamp` is the build's\n`built_at` \u2014 a rebuild makes the map stale, and the GET below says so.\n\n\u2b50 `array_coverage` is HIS declaration (R45.8), same contract as the snapshot route:\n`\"full\"` holds the fit to the MaxOne/MaxTwo's 220 \u00d7 120 (near miss corrected and reported,\nfar miss refused); `\"partial\"` enforces only the 17.5 \u00b5m scale and leaves `1-1` meaning the\ntop-left of the IMAGED REGION. Defaulted `\"partial\"` \u2014 the mode that assumes nothing.",
        "operationId": "post_electrodes_map_api_videomosaic_electrodes_map_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/VideoElectrodeMapRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "202": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JobRef"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/videomosaic/{analysis_id}/electrodes": {
      "get": {
        "summary": "Get Electrode Map",
        "description": "The full electrode map + `stale` (the map's `source_stamp` vs the current build's\n`built_at` \u2014 a rebuilt mosaic invalidates the map and the UI says so).",
        "operationId": "get_electrode_map_api_videomosaic__analysis_id__electrodes_get",
        "parameters": [
          {
            "name": "analysis_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Analysis Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ElectrodeMapPayload"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/videomosaic/regions/locate": {
      "post": {
        "summary": "Post Locate Region",
        "description": "`POST /api/videomosaic/regions/locate` -> **202 `JobRef`**. Add a fixed-field recording\nand find where on the built mosaic it was taken.\n\n\u2b50 The zoom is MEASURED, not searched: the electrode lattice appears in both pictures and the\nratio of the two pitches is the magnification ratio. Several stills (median / max / std) are\nbuilt from one decode and each is matched, because whether the neurons are bright at rest or\nonly when firing is a property of the preparation.\n\n\u26a0\ufe0f Refuses without a built mosaic, and without an electrode map \u2014 the pipeline is step by\nstep (his ruling), and a location whose electrodes cannot be named is half an answer.",
        "operationId": "post_locate_region_api_videomosaic_regions_locate_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/LocateRegionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "202": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JobRef"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/videomosaic/regions/snap": {
      "post": {
        "summary": "Post Snap Region",
        "description": "`POST /api/videomosaic/regions/snap` -> **202 `JobRef`**. *\"I dragged it here, now snap\nit\"* \u2014 a bounded local re-search seeded where the user dropped the rectangle.\n\n\u2b50 Correct beats fast (R23): the committed number is the server's masked NCC on the real\npixels, not a browser-side guess. About a second, and his ruling accepts that.",
        "operationId": "post_snap_region_api_videomosaic_regions_snap_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SnapRegionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "202": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JobRef"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/videomosaic/regions": {
      "patch": {
        "summary": "Patch Region",
        "description": "Rename a region, or confirm it. Both are the user's word, so both are synchronous.\n\n\u2b50 **Confirm is the human's signature.** A located region is `unconfirmed` until he says\notherwise; nothing here ever promotes itself, which is I3 (nothing is auto-anchored) applied\nto a rectangle instead of a tile.",
        "operationId": "patch_region_api_videomosaic_regions_patch",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RegionUpdateRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RegionRecord"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/videomosaic/{analysis_id}/regions/{region_id}": {
      "delete": {
        "summary": "Delete Region",
        "description": "Forget a located region. Its still goes with it, and so does the project's copy of the\nrecording \u2014 unless another region is still using that same file. The project holds the files,\nso removing the region removes what it brought in; but it must not remove what something else\nstill needs.",
        "operationId": "delete_region_api_videomosaic__analysis_id__regions__region_id__delete",
        "parameters": [
          {
            "name": "analysis_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Analysis Id"
            }
          },
          {
            "name": "region_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Region Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RegionsPayload"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/videomosaic/{analysis_id}/regions": {
      "get": {
        "summary": "Get Regions",
        "description": "Every located region + `stale` (rebuild the mosaic and every location is stale).",
        "operationId": "get_regions_api_videomosaic__analysis_id__regions_get",
        "parameters": [
          {
            "name": "analysis_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Analysis Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RegionsPayload"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/videomosaic/{analysis_id}/mea": {
      "get": {
        "summary": "Get Mea",
        "description": "What electrical data this project has. Empty-but-fine when nothing is attached yet.",
        "operationId": "get_mea_api_videomosaic__analysis_id__mea_get",
        "parameters": [
          {
            "name": "analysis_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Analysis Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MeaAttachment"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/videomosaic/mea/attach": {
      "post": {
        "summary": "Post Mea Attach",
        "description": "Find the recordings that belong to a project \u2014 and, on `confirm`, remember them.\n\n\u2b50 Two-step ON PURPOSE. Without `confirm` this only *reports* what it found, so the user sees\nthe actual paths before anything is written. Attaching the wrong plate would pair one culture's\nvoltages with another culture's neurons \u2014 silently, and in a dataset meant to be ground truth.",
        "operationId": "post_mea_attach_api_videomosaic_mea_attach_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/MeaAttachRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MeaAttachment"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/videomosaic/{analysis_id}/mea/trace": {
      "get": {
        "summary": "Get Mea Trace",
        "description": "One electrode's stored trace and its spikes, for the window `[t0, t1)`.\n\n`electrode` is the Camea grid id the user clicked (`\"col-row\"`). Resolving it to a MaxWell\nchannel needs the chip's seating, which is why `orientation` rides on the response: while it is\nunconfirmed the caller must present the identity as provisional.\n\n\u26a0\ufe0f `health` is not decoration. The published MaxWell decoder does not reconstruct this\nproject's files, and a railed window drawn without that caveat looks exactly like a real\nsilent electrode. See `core/mearecording.py`.",
        "operationId": "get_mea_trace_api_videomosaic__analysis_id__mea_trace_get",
        "parameters": [
          {
            "name": "analysis_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Analysis Id"
            }
          },
          {
            "name": "electrode",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Electrode"
            }
          },
          {
            "name": "run_id",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Run Id"
            }
          },
          {
            "name": "t0",
            "in": "query",
            "required": false,
            "schema": {
              "type": "number",
              "default": 0.0,
              "title": "T0"
            }
          },
          {
            "name": "t1",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "null"
                }
              ],
              "title": "T1"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ElectrodeTracePayload"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/videomosaic/mea/orientation": {
      "post": {
        "summary": "Post Mea Orientation",
        "description": "Score the four chip seatings against the located regions (202 + `JobRef`).\n\nA job because it reads every frame of each region recording and makes a full pass over the MEA\nspike table \u2014 minutes, not milliseconds.\n\n\u2b50 **EVERY SCORABLE REGION TAKES PART** (2026-08-15). Two located regions are two independent\nwitnesses to the same seating \u2014 the chip sat one way for the whole session \u2014 so the test scores\neach region against each seating and combines them (`orientation.combined_correlation`\ndocuments how). `region_id` still narrows it to one region, with the old single-region contract\nand its specific refusals intact.\n\n\u2b50 **IT PROPOSES; IT NEVER APPLIES.** The winning seating comes back in the result and a human\nconfirms it through `POST /mea/attach`. That is his rule for the whole app and it matters most\nhere: a wrong seating pairs every neuron with the wrong electrode, and would do so invisibly.",
        "operationId": "post_mea_orientation_api_videomosaic_mea_orientation_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/MeaOrientationRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "202": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JobRef"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/videomosaic/mea/footprint": {
      "get": {
        "summary": "Get Mea Footprint",
        "description": "Where the RECORDED pads would sit in the mosaic, under each of the four seatings.\n\n\u2b50 **CHEAP ON PURPOSE \u2014 the mapping only.** No video, no spikes, no raw stream: a UI can draw\nthe four candidate footprints the moment its panel opens. The per-region covered-pad counts\nare the same geometry the orientation job's coverage evidence rests on, computable without\nreading a single frame \u2014 the heavy evidence stays in the job.\n\nRefusals mirror the orientation route's: no MEA attached and no electrode map are both 409s,\nbecause without them there is no grid for a footprint to sit in.",
        "operationId": "get_mea_footprint_api_videomosaic_mea_footprint_get",
        "parameters": [
          {
            "name": "analysis_id",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Analysis Id"
            }
          },
          {
            "name": "run_id",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Run Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MeaFootprintPayload"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/mea/projects": {
      "post": {
        "summary": "Post Mea Project",
        "description": "\u2b50 **THE SERVER CREATES THE DOCUMENT** \u2014 `POST /api/projects` for the standalone MEA task.\nA name and the recordings the wizard picked go in; a project with those already on its shelf\ncomes out. No session, no probe, no folder.\n\n\u2b50 **EVERY PATH IS READ BEFORE THE PROJECT EXISTS.** That ordering is the whole reason this is\none call: if one of them is not a MaxLab recording, the refusal names it and **no project is\ncreated**, so there is nothing for him to clean up before trying again.\n\nThe project carries `dataset_key=\"\"` / `dataset=\"\"` / `data_dir=\"\"` \u2014 see the module\ndocstring for why an empty key beats a minted one, and what was read to decide it. \u26d4 A\nrecording's `source_path` does **not** become `data_dir`: the shelf may hold several, from\nanywhere, and none of them is \"the project's dataset\".",
        "operationId": "post_mea_project_api_mea_projects_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateMeaProjectRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AnalysisSummary"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/mea/browse": {
      "get": {
        "summary": "Get Mea Browse",
        "description": "\u2b50 **THE ONE ROUTE WITH NO PROJECT.** Every `data.raw.h5` under `path`, with enough of each\nfile's own facts that he can tell which ones he means before he ticks them.\n\n\u26a0\ufe0f **It has no `analysis_id`, and must never be given one \"for consistency\"** \u2014 the wizard calls\nit *before a project exists*. That is precisely what makes one import component mountable in two\nplaces: it browses, it lists, it hands back the paths he ticked, and it creates nothing.\n\n\u26d4 It reads and never writes. \u2b50 And it is the picker he will actually use: the native\nmulti-select dialog only exists with `--window`, and he drives Camea over VSCode remote where\nthat route is a 501.",
        "operationId": "get_mea_browse_api_mea_browse_get",
        "parameters": [
          {
            "name": "path",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "description": "The folder to look under.",
              "title": "Path"
            },
            "description": "The folder to look under."
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MeaBrowseResult"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/mea/{analysis_id}/recordings": {
      "get": {
        "summary": "Get Mea Recordings",
        "description": "Everything on this project's shelf, with **live** copy state.\n\nEvery number is read off the file on the way past \u2014 see `recordings.shelf_entry`. That is what\nmakes a recording whose original has moved say so, instead of showing a row of zeros.",
        "operationId": "get_mea_recordings_api_mea__analysis_id__recordings_get",
        "parameters": [
          {
            "name": "analysis_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Analysis Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MeaShelf"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      },
      "post": {
        "summary": "Post Mea Recordings",
        "description": "Add several recordings at once \u2014 *\"opens file explorer, can import multiple at a time\"*.\n\nEach one is read to confirm it is a MaxLab recording (\u26d4 refused **by name** if it is not, and\nthen none of them are added), recorded as `referenced`, and given a copy job. He can open any of\nthem the moment this returns; the copies land behind him.\n\n\u2b50 This is the same work `POST /api/mea/projects` does with its `paths`, through the same three\nfunctions \u2014 so the wizard's Files step and the in-project button cannot drift apart.",
        "operationId": "post_mea_recordings_api_mea__analysis_id__recordings_post",
        "parameters": [
          {
            "name": "analysis_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Analysis Id"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AddMeaRecordingsRequest"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MeaShelf"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/mea/{analysis_id}/recordings/envelopes": {
      "get": {
        "summary": "Get Mea Envelopes",
        "description": "Which recordings can be shown whole yet, and what is still being read.\n\n\u26a0\ufe0f `ready: false` never means a recording is broken \u2014 it means only the narrow windows the app\ncan read live are available for it, which is still a working trace panel.",
        "operationId": "get_mea_envelopes_api_mea__analysis_id__recordings_envelopes_get",
        "parameters": [
          {
            "name": "analysis_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Analysis Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MeaEnvelopeStatus"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      },
      "post": {
        "summary": "Post Mea Envelopes",
        "description": "\u2b50 **THE BACKFILL** \u2014 read every recording in this project end to end, once, so the trace\npanel can show the whole of any pad.\n\nHis instruction, 2026-08-15: *\"go ahead and run the loader on the MEAs I have already imported.\"*\nNew recordings get this at import time; this is how everything already in a project catches up.\n\nRecordings that already have one are skipped, so calling this twice costs nothing. Measured\n37-70 s per recording on his five, in the background, cancellable.",
        "operationId": "post_mea_envelopes_api_mea__analysis_id__recordings_envelopes_post",
        "parameters": [
          {
            "name": "analysis_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Analysis Id"
            }
          },
          {
            "name": "force",
            "in": "query",
            "required": false,
            "schema": {
              "type": "boolean",
              "default": false,
              "title": "Force"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MeaEnvelopeStatus"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/mea/{analysis_id}/recordings/{recording_id}": {
      "patch": {
        "summary": "Patch Mea Recording",
        "description": "Rename a recording \u2014 the row's `label`, and nothing else.\n\n\u2b50 **A RENAME IS A FACT ABOUT THE ROW, NEVER ABOUT THE BYTES** (the project rename's rule,\nrestated one level down: the manifest moves, the folder never does). His original `.h5` is\nread-only by hard rule; the project's copy lives at `recordings/<id>/`, and the id is forever.\n\u26d4 A blank name is refused \u2014 a nameless row is one he cannot pick out of a list.",
        "operationId": "patch_mea_recording_api_mea__analysis_id__recordings__recording_id__patch",
        "parameters": [
          {
            "name": "analysis_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Analysis Id"
            }
          },
          {
            "name": "recording_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Recording Id"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RenameMeaRecordingRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MeaShelf"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      },
      "delete": {
        "summary": "Delete Mea Recording",
        "description": "Forget a recording, and delete **Camea's copy** of it.\n\n\u26d4 **THE USER'S ORIGINAL IS NEVER TOUCHED**, and there is no confirm box, because there is\nnothing of his to lose: the only bytes removed are a copy Camea made itself, inside the project\nfolder (his ruling, 2026-08-14). A copy still in flight is cancelled first.",
        "operationId": "delete_mea_recording_api_mea__analysis_id__recordings__recording_id__delete",
        "parameters": [
          {
            "name": "analysis_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Analysis Id"
            }
          },
          {
            "name": "recording_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Recording Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MeaShelf"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/mea/{analysis_id}/recordings/{recording_id}/layout": {
      "get": {
        "summary": "Get Mea Layout",
        "description": "The chip as this recording describes it \u2014 every routed pad, at its own \u00b5m position.\n\n\u2b50 **ONLY THE ROUTED PADS.** A MaxOne routes ~1k channels out of tens of thousands of pads, so\nmost of the chip carries no data at all \u2014 and a pad that was never routed is not a measurement\nof silence, it is the *absence* of a measurement. Drawing them would invent data.\n\n\u2b50 `stride` and `pitch_um` are **derived from the file's own numbering and verified against every\nrouted pad**, never taken from a datasheet. \u26a0\ufe0f And never measured from the routed spacing: one of\nthis project's recordings routed every other pad, so the smallest routed gap is twice the truth.\n\n\u26d4 Needs no proprietary decoder \u2014 the mapping table is plain HDF5.",
        "operationId": "get_mea_layout_api_mea__analysis_id__recordings__recording_id__layout_get",
        "parameters": [
          {
            "name": "analysis_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Analysis Id"
            }
          },
          {
            "name": "recording_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Recording Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MeaChipLayout"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/mea/{analysis_id}/recordings/{recording_id}/activity": {
      "get": {
        "summary": "Get Mea Activity",
        "description": "The per-pad tally the chip map is coloured by \u2014 one row per routed pad, in `layout` order.\n\n\u2b50 **A GET, NOT A JOB, AND THAT WAS MEASURED** (plan 003 \u00a7 Open asked). One pass over the spike\ntable of a real 300 s recording: **2.3\u201321 ms** end to end including the layout, over all five\nreadable recordings in the mirror (the worst is 982 channels \u00d7 244,925 spikes at 21 ms). Nothing\nhere is slow enough to need a spinner, let alone a job \u2014 so it is a plain GET and the screen\ndraws on the first paint.\n\n\u2b50 **AND IT NEEDS NO PROPRIETARY DECODER**, because the spike table is written uncompressed by\nthe on-chip detector. This is the reason the chip map is worth building before the decoder\nproblem is solved: it is exactly right on a machine where the waveform is a rail.\n\n\u26d4 A count. Not a rate per neuron, not a burst, not a verdict on whether the culture is healthy\n(I1) \u2014 and `max_rate_hz` is the busiest pad *in this recording*, so the UI can scale its colours\nto the file in front of it. There is no number in Camea for how active a chip should be.",
        "operationId": "get_mea_activity_api_mea__analysis_id__recordings__recording_id__activity_get",
        "parameters": [
          {
            "name": "analysis_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Analysis Id"
            }
          },
          {
            "name": "recording_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Recording Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MeaChipActivity"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/mea/{analysis_id}/recordings/{recording_id}/trace": {
      "get": {
        "summary": "Get Mea Channel Trace",
        "description": "One pad's stored waveform and its spikes, for the window `[t0, t1)`.\n\n\u2b50 **BY CHANNEL, BECAUSE THE CLICK ALREADY KNOWS ITS CHANNEL.** The chip map was drawn from\n`layout`, so the dot he clicked carries its own channel \u2014 there is nothing to resolve and no\nseating to guess. \u26d4 That is the whole difference from the videomosaic trace route, and it is why\nthe two are separate rather than shared.\n\n\u2b50 **`max_points` IS HOW THE WHOLE RECORDING FITS DOWN THE WIRE.** Without it, this returns raw\nsamples and `MAX_TRACE_SECONDS` still applies \u2014 that is the old contract, unchanged, and the\nvideomosaic sibling keeps it too. With it, the answer is a **min/max envelope** of at most\n`max_points` columns, so a 300 s window costs the same as a 1 s one and **the cap does not\napply**. His ruling, 2026-08-15: *\"make it so it can go beyond the 30 sec limit\"* \u2014 the answer is\na smaller payload, not a bigger one.\n\n\u26a0\ufe0f **A WIDE ENVELOPE IS SERVED FROM THE PRECOMPUTED CACHE, AND MUST BE.** Reading one channel's\nfull length out of a MaxWell file costs 12-23 s (the raw stream is chunked across every channel),\nso a live read is only used when the window is narrow enough to be cheap. When the cache is\nmissing the route says so by name rather than blocking for half a minute.\n\n\u26a0\ufe0f **`health` IS NOT DECORATION.** On some of these files the decoder returns a rail, and a\nrailed window drawn without that caveat looks exactly like a genuinely silent electrode. \u2b50 The\nspikes are unaffected \u2014 the on-chip detector wrote them uncompressed \u2014 so they are returned and\ndrawn even when the waveform is refused. \u26a0\ufe0f On the envelope path the health number is the\n**cache's own, whole-recording** figure, never a window's: the rail fraction changes with window\nlength (measured 1.000 over 1 s but 0.827 over 30 s on 000690).",
        "operationId": "get_mea_channel_trace_api_mea__analysis_id__recordings__recording_id__trace_get",
        "parameters": [
          {
            "name": "analysis_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Analysis Id"
            }
          },
          {
            "name": "recording_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Recording Id"
            }
          },
          {
            "name": "channel",
            "in": "query",
            "required": true,
            "schema": {
              "type": "integer",
              "description": "The routed channel that was clicked on the chip map.",
              "title": "Channel"
            },
            "description": "The routed channel that was clicked on the chip map."
          },
          {
            "name": "t0",
            "in": "query",
            "required": false,
            "schema": {
              "type": "number",
              "default": 0.0,
              "title": "T0"
            }
          },
          {
            "name": "t1",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "null"
                }
              ],
              "title": "T1"
            }
          },
          {
            "name": "max_points",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "integer",
                  "exclusiveMinimum": 0
                },
                {
                  "type": "null"
                }
              ],
              "description": "Reduce to at most this many min/max columns instead of returning raw samples. Lifts the window cap entirely.",
              "title": "Max Points"
            },
            "description": "Reduce to at most this many min/max columns instead of returning raw samples. Lifts the window cap entirely."
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MeaChannelTrace"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "AddMeaRecordingsRequest": {
        "properties": {
          "paths": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Paths"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "title": "AddMeaRecordingsRequest",
        "description": "`POST /api/mea/{analysis_id}/recordings` \u2014 several at once, the way he asked for them.\n\n\u2b50 **ALL OR NOTHING, AND THE REFUSAL NAMES THE FILE.** If one path on the list does not read as\na MaxLab recording, none of them are added. The alternative \u2014 take the good ones and report the\nrest \u2014 cannot be offered at *creation* (the response there is the project, with nowhere to put a\nrefusal), and one rule he can state to himself beats two that differ by which door he came\nthrough."
      },
      "AnalysisListResponse": {
        "properties": {
          "analyses": {
            "items": {
              "$ref": "#/components/schemas/AnalysisSummary"
            },
            "type": "array",
            "title": "Analyses"
          },
          "unreadable": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Unreadable",
            "description": "Project folders in the store that could not be read (a corrupt manifest). Listed, never silently dropped \u2014 and never a failure of the whole home screen."
          },
          "migration": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/MigrationReport"
              },
              {
                "type": "null"
              }
            ],
            "description": "\u2b50 Set on the first launch after R44, when pre-R44 projects were brought into the store. The home screen states it once. null on every ordinary launch."
          }
        },
        "type": "object",
        "required": [
          "analyses"
        ],
        "title": "AnalysisListResponse"
      },
      "AnalysisRef": {
        "properties": {
          "analysis_id": {
            "type": "string",
            "title": "Analysis Id"
          },
          "feature": {
            "type": "string",
            "title": "Feature"
          },
          "name": {
            "type": "string",
            "title": "Name"
          },
          "modified": {
            "type": "string",
            "title": "Modified"
          },
          "n_anchored": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "N Anchored"
          },
          "n_excluded": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "N Excluded"
          }
        },
        "type": "object",
        "required": [
          "analysis_id",
          "feature",
          "name",
          "modified"
        ],
        "title": "AnalysisRef",
        "description": "A card on the dataset's tile in the browser: \"you already have work here\"."
      },
      "AnalysisSummary": {
        "properties": {
          "analysis_id": {
            "type": "string",
            "title": "Analysis Id"
          },
          "feature": {
            "type": "string",
            "title": "Feature",
            "description": "\"mosaic\". Features register their own name."
          },
          "name": {
            "type": "string",
            "title": "Name"
          },
          "dataset_key": {
            "type": "string",
            "title": "Dataset Key"
          },
          "dataset": {
            "type": "string",
            "title": "Dataset"
          },
          "path": {
            "type": "string",
            "title": "Path"
          },
          "folder": {
            "type": "string",
            "title": "Folder",
            "description": "Where the project lives in Camea's store. \u26a0\ufe0f Shown for support and diagnosis, not as somewhere to go: browsing a project means GET /api/projects/{id}/outputs.",
            "default": ""
          },
          "data_dir": {
            "type": "string",
            "title": "Data Dir",
            "description": "Where this project's dataset lives.",
            "default": ""
          },
          "created": {
            "type": "string",
            "title": "Created"
          },
          "modified": {
            "type": "string",
            "title": "Modified"
          },
          "bytes": {
            "type": "integer",
            "title": "Bytes"
          },
          "n_tiles": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "N Tiles"
          },
          "n_anchored": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "N Anchored"
          },
          "n_excluded": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "N Excluded"
          },
          "independent_of_method": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Independent Of Method",
            "description": "\u26a0\ufe0f Derived from the document's HISTORY, never from what it claims about itself. Shown on the card so a seeded document can never be mistaken for a truth."
          }
        },
        "type": "object",
        "required": [
          "analysis_id",
          "feature",
          "name",
          "dataset_key",
          "dataset",
          "path",
          "created",
          "modified",
          "bytes"
        ],
        "title": "AnalysisSummary",
        "description": "A project = a document + its outputs, in Camea's store.\n\n\u26a0\ufe0f **`dataset`/`dataset_key`/`data_dir` may all be empty, and that is not an error.** They were\nmandatory while every task was a wrapper around something on disk; since `Analyze MEA`\n(2026-08-14) a task may have no input at creation at all \u2014 its project is a shelf, filled from\ninside. A client must render the blank as a state (\"nothing added yet\"), never as a missing\nvalue it should go and look for."
      },
      "AppStamp": {
        "properties": {
          "name": {
            "type": "string",
            "title": "Name"
          },
          "version": {
            "type": "string",
            "title": "Version"
          }
        },
        "type": "object",
        "required": [
          "name",
          "version"
        ],
        "title": "AppStamp"
      },
      "AutosaveRequest": {
        "properties": {
          "doc": {
            "$ref": "#/components/schemas/Document"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "doc"
        ],
        "title": "AutosaveRequest",
        "description": "`POST /api/analyses/{analysis_id}/autosave`. Debounced 2 s, **plus unconditionally on every\n`A` and `E`**. \ud83d\udd34 **A FAILURE IS LOUD** \u2014 never swallowed. (`localStorage` failed *silently* in\nthe sandbox and nearly cost a day's work. Autosave is a server file.)\n\n\ud83d\udd34 **THE RANGE GUARD RUNS HERE**, and in v1 it did not: `autosave()` carried it and nothing\ncalled `autosave()`. Pass 2's autosave once silently overwrote pass 1's ground-truth records,\nand the autosave slot keys on the DIRECTORY, not the trial range \u2014 so the collision is still\nopen. A document whose range/dataset disagrees with the slot's is refused with\n`409 range_mismatch`. It is not merged, not renamed, not \"repaired\"."
      },
      "BlankProposal": {
        "properties": {
          "threshold": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Threshold"
          },
          "threshold_source": {
            "type": "string",
            "title": "Threshold Source"
          },
          "measure": {
            "type": "string",
            "title": "Measure"
          },
          "texture": {
            "additionalProperties": {
              "type": "number"
            },
            "type": "object",
            "title": "Texture"
          },
          "proposed": {
            "items": {
              "type": "integer"
            },
            "type": "array",
            "title": "Proposed"
          },
          "n_proposed": {
            "type": "integer",
            "title": "N Proposed"
          },
          "n_scanned": {
            "type": "integer",
            "title": "N Scanned"
          },
          "margin_warning": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Margin Warning"
          }
        },
        "type": "object",
        "required": [
          "threshold",
          "threshold_source",
          "measure",
          "texture",
          "proposed",
          "n_proposed",
          "n_scanned"
        ],
        "title": "BlankProposal",
        "description": "\u2b50 **IT RECOMMENDS. IT NEVER REJECTS.**\n\n\u26a0\ufe0f **ZERO MARGIN AT THE BOUNDARY.** Genuinely-blank frames and perfectly good ones *interleave*\nbelow the threshold. Measured with the refusal lifted, three of the four near-threshold trials\nland 0.24 / 0.18 / 2.07 px from the human truth \u2014 they are **ordinary tiles** \u2014 while the fourth\nlands **679 px wrong**. One of four. That is exactly why the measure may only ever propose.\n\n\u274c **NO BLUR JUDGEMENT. EVER.** Across 15 focus measures the best global blur threshold reaches\nF1 = 0.37, and variance-of-Laplacian scores **worse than chance**. No slider, no number, nowhere\nin the UI. Blur is his eye, in the sweep, with `E`.\n\n\u26d4 There is no `BLANK_THRESHOLD` fallback constant. The threshold is **computed from the frames\nin front of it, every time.** A measured number carried in the source would be dataset knowledge."
      },
      "BlankProposeRequest": {
        "properties": {
          "session_id": {
            "type": "string",
            "title": "Session Id"
          },
          "trials": {
            "items": {
              "type": "integer"
            },
            "type": "array",
            "title": "Trials"
          },
          "pass_split": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Pass Split"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "session_id",
          "trials"
        ],
        "title": "BlankProposeRequest",
        "description": "`POST /api/mosaic/screen/propose`. Needs `pass_split` because the threshold is defined in\nmosaic's terms: *the 2.0th percentile of PASS-1 texture*."
      },
      "BlankScanBlock": {
        "properties": {
          "threshold": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Threshold"
          },
          "measure": {
            "type": "string",
            "title": "Measure",
            "default": "std of DoG(sigma=3, sigma=30) of the frame as read"
          },
          "blank": {
            "items": {
              "type": "integer"
            },
            "type": "array",
            "title": "Blank"
          },
          "scanned": {
            "items": {
              "type": "integer"
            },
            "type": "array",
            "title": "Scanned"
          },
          "accepted": {
            "type": "boolean",
            "title": "Accepted",
            "default": false
          },
          "overruled_by_user": {
            "items": {
              "type": "integer"
            },
            "type": "array",
            "title": "Overruled By User",
            "description": "Frames the scan proposed and the human pressed Keep on."
          }
        },
        "additionalProperties": true,
        "type": "object",
        "title": "BlankScanBlock",
        "description": "\u2b50 **THE SCAN RECOMMENDS. THE USER TICKS. NOTHING IS AUTO-EXCLUDED.**\n\n`blank` is the DECISION \u2014 the list the human left standing, which the matcher will refuse. It\nlives **here, in the document**, and it travels to the matcher **in the request body**\n(`MatchAnchorRequest.refuse`). It is *not* server state. (In v1 it was, and that single field\nis what made the match endpoints impure.)"
      },
      "BuildBlock": {
        "properties": {
          "build_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Build Id"
          },
          "method": {
            "type": "string",
            "title": "Method",
            "default": "t33"
          },
          "created": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Created"
          },
          "seconds": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Seconds"
          },
          "gpu": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Gpu"
          },
          "n_placed": {
            "type": "integer",
            "title": "N Placed",
            "default": 0
          },
          "config": {
            "additionalProperties": {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "integer"
                },
                {
                  "type": "boolean"
                },
                {
                  "type": "string"
                },
                {
                  "additionalProperties": {
                    "anyOf": [
                      {
                        "type": "number"
                      },
                      {
                        "type": "integer"
                      },
                      {
                        "type": "boolean"
                      },
                      {
                        "type": "string"
                      },
                      {
                        "type": "null"
                      }
                    ]
                  },
                  "type": "object"
                },
                {
                  "items": {
                    "anyOf": [
                      {
                        "type": "number"
                      },
                      {
                        "type": "integer"
                      },
                      {
                        "type": "boolean"
                      },
                      {
                        "type": "string"
                      },
                      {
                        "type": "null"
                      }
                    ]
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ]
            },
            "type": "object",
            "title": "Config"
          },
          "info": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/BuildInfo"
              },
              {
                "type": "null"
              }
            ]
          },
          "positions": {
            "additionalProperties": {
              "prefixItems": [
                {
                  "type": "number"
                },
                {
                  "type": "number"
                }
              ],
              "type": "array",
              "maxItems": 2,
              "minItems": 2
            },
            "type": "object",
            "title": "Positions"
          },
          "trials": {
            "items": {
              "type": "integer"
            },
            "type": "array",
            "title": "Trials"
          },
          "gaps": {
            "items": {
              "prefixItems": [
                {
                  "type": "integer"
                },
                {
                  "type": "integer"
                }
              ],
              "type": "array",
              "maxItems": 2,
              "minItems": 2
            },
            "type": "array",
            "title": "Gaps"
          },
          "stale": {
            "type": "boolean",
            "title": "Stale",
            "default": false
          },
          "stale_reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Stale Reason"
          }
        },
        "additionalProperties": true,
        "type": "object",
        "title": "BuildBlock",
        "description": "The build, as recorded in the document.\n\n\u2b50 `trials` + `gaps` are **what the solver was actually given**. Without them the document can\nnever know its build was solved on a *different* input, and the staleness check degenerates into\ncomparing the current trial list with itself. **A build with no recorded `trials` is treated as\nSTALE** \u2014 the backend refuses to guess."
      },
      "BuildConfig": {
        "properties": {
          "pass_split": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Pass Split"
          },
          "anchor_ncc": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Anchor Ncc"
          },
          "split_px": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Split Px"
          },
          "look": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Look"
          },
          "min_side": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Min Side"
          },
          "t27": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/T27Config"
              },
              {
                "type": "null"
              }
            ]
          }
        },
        "additionalProperties": true,
        "type": "object",
        "title": "BuildConfig",
        "description": "The Advanced drawer. \u26a0\ufe0f The drawer must say: *\"Off the validated path. The shipped 312/312\nbuild used the defaults.\"*\n\n\u26a0\ufe0f **`pass_split` defaults to the session's DETECTED value, not to t33's literal 166.**\nValidation is done by **constructing** a real `t33.Config` \u2014 it raises `TypeError` on an unknown\nknob itself. \u26d4 Do not hard-code the knob list here; `extra=\"allow\"` is what lets a new t33 knob\nthrough without a schema change."
      },
      "BuildInfo": {
        "properties": {
          "seconds": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Seconds"
          },
          "gpu": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Gpu"
          }
        },
        "additionalProperties": true,
        "type": "object",
        "title": "BuildInfo",
        "description": "t33's own `info` dict, made JSON-safe. \u26a0\ufe0f `info[\"config\"]` holds a nested `t27.Config` and\n`json.dumps(info)` **crashes** without a coercer. And a **warm cache hit hands back a plain\ndict**, not a `t33.Config` \u2014 do not key the coercion on `hasattr(cfg, \"t27_config\")`, or it is a\nno-op on every cached build, which is the common case."
      },
      "BuildResult": {
        "properties": {
          "kind": {
            "type": "string",
            "const": "build",
            "title": "Kind",
            "default": "build"
          },
          "build_id": {
            "type": "string",
            "title": "Build Id"
          },
          "created": {
            "type": "string",
            "title": "Created"
          },
          "trials": {
            "items": {
              "type": "integer"
            },
            "type": "array",
            "title": "Trials",
            "description": "\u2b50 WHAT THE SOLVER WAS ACTUALLY GIVEN."
          },
          "gaps": {
            "items": {
              "prefixItems": [
                {
                  "type": "integer"
                },
                {
                  "type": "integer"
                }
              ],
              "type": "array",
              "maxItems": 2,
              "minItems": 2
            },
            "type": "array",
            "title": "Gaps",
            "description": "\u2b50 ditto \u2014 see `BuildBlock`."
          },
          "pass_split": {
            "type": "integer",
            "title": "Pass Split"
          },
          "positions": {
            "additionalProperties": {
              "prefixItems": [
                {
                  "type": "number"
                },
                {
                  "type": "number"
                }
              ],
              "type": "array",
              "maxItems": 2,
              "minItems": 2
            },
            "type": "object",
            "title": "Positions"
          },
          "n_placed": {
            "type": "integer",
            "title": "N Placed"
          },
          "unplaced": {
            "items": {
              "type": "integer"
            },
            "type": "array",
            "title": "Unplaced"
          },
          "seconds": {
            "type": "number",
            "title": "Seconds"
          },
          "gpu": {
            "type": "boolean",
            "title": "Gpu"
          },
          "info": {
            "$ref": "#/components/schemas/BuildInfo"
          },
          "per_tile": {
            "additionalProperties": {
              "$ref": "#/components/schemas/PerTileEvidence"
            },
            "type": "object",
            "title": "Per Tile"
          }
        },
        "type": "object",
        "required": [
          "build_id",
          "created",
          "trials",
          "gaps",
          "pass_split",
          "positions",
          "n_placed",
          "unplaced",
          "seconds",
          "gpu",
          "info",
          "per_tile"
        ],
        "title": "BuildResult",
        "description": "`Job.result` when `kind == \"build\"`. Also `GET /api/mosaic/builds/{build_id}`."
      },
      "BuildStartRequest": {
        "properties": {
          "session_id": {
            "type": "string",
            "title": "Session Id"
          },
          "trials": {
            "items": {
              "type": "integer"
            },
            "type": "array",
            "title": "Trials"
          },
          "pass_split": {
            "type": "integer",
            "title": "Pass Split"
          },
          "config": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/BuildConfig"
              },
              {
                "type": "null"
              }
            ]
          },
          "use_cache": {
            "type": "boolean",
            "title": "Use Cache",
            "default": true
          },
          "analysis_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Analysis Id"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "session_id",
          "trials",
          "pass_split"
        ],
        "title": "BuildStartRequest",
        "description": "`POST /api/mosaic/build` \u2192 202 `JobRef`. Holds the **`gpu` exclusive lease** for its whole\nrun; another job that wants it gets `409 busy`.\n\n\u2b50 **`trials` IS THE DOCUMENT'S ACTIVE LIST** \u2014 everything the user has *not* excluded. This is\nthe ONLY way a frame ever leaves a build. Hard-wiring the session's run list here meant pressing\n`E` and then \"re-solve\" re-solved the **identical problem** and put the excluded frame straight\nback into the chain.\n\n`config: null` = t33's shipped 312/312 defaults, and that is the one-button path.\n\n`use_cache` is a **speed** switch, not a correctness one (warm \u2248 25 s vs cold \u2248 3 min). t33's\ncache filename carries a hash of the trial list + config and a mismatch is **refused, not\nrepaired** \u2014 it cannot serve a stale answer for a different input."
      },
      "Candidate": {
        "properties": {
          "rank": {
            "type": "integer",
            "title": "Rank",
            "description": "0-indexed. **DISPLAYED as +1** \u2014 key `1` is rank 0."
          },
          "x": {
            "type": "number",
            "title": "X"
          },
          "y": {
            "type": "number",
            "title": "Y"
          },
          "ncc": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Ncc"
          },
          "npix": {
            "type": "integer",
            "title": "Npix"
          },
          "subpixel": {
            "type": "boolean",
            "title": "Subpixel"
          }
        },
        "type": "object",
        "required": [
          "rank",
          "x",
          "y",
          "ncc",
          "npix",
          "subpixel"
        ],
        "title": "Candidate",
        "description": "A peak. `x`/`y` are **world top-left positions** \u2014 the server has already done the\n`m0 + (dx, dy)` conversion; the front end never sees a composite coordinate.\n\n\u26a0\ufe0f `npix = 0` from a tier-A candidate means **\"not measured\"**, not \"no overlap\". Pass it\nthrough untouched."
      },
      "CompositeInfo": {
        "properties": {
          "w": {
            "type": "integer",
            "title": "W"
          },
          "h": {
            "type": "integer",
            "title": "H"
          },
          "valid_px": {
            "type": "integer",
            "title": "Valid Px"
          },
          "m0": {
            "prefixItems": [
              {
                "type": "number"
              },
              {
                "type": "number"
              }
            ],
            "type": "array",
            "maxItems": 2,
            "minItems": 2,
            "title": "M0"
          }
        },
        "type": "object",
        "required": [
          "w",
          "h",
          "valid_px",
          "m0"
        ],
        "title": "CompositeInfo"
      },
      "CopyOutputsRequest": {
        "properties": {
          "names": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Names",
            "description": "Which outputs to copy. Names from the listing."
          },
          "dest": {
            "type": "string",
            "title": "Dest",
            "description": "The folder to copy them into. Created if it does not exist."
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "names",
          "dest"
        ],
        "title": "CopyOutputsRequest",
        "description": "`POST /api/projects/{id}/outputs/copy` \u2014 **the one way work leaves Camea.**\n\n\u26d4 The destination is refused (409) inside a dataset or inside `data/`, exactly as every other\npath the app writes to is. \u26d4 A file already at the destination is **refused, never overwritten**\n\u2014 the user's folder is his, and this route is not allowed to be the reason something in it is\ngone."
      },
      "CopyOutputsResponse": {
        "properties": {
          "copied": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Copied",
            "description": "The full paths written."
          },
          "dest": {
            "type": "string",
            "title": "Dest"
          }
        },
        "type": "object",
        "required": [
          "dest"
        ],
        "title": "CopyOutputsResponse"
      },
      "CreateAnalysisRequest": {
        "properties": {
          "session_id": {
            "type": "string",
            "title": "Session Id"
          },
          "feature": {
            "type": "string",
            "title": "Feature"
          },
          "name": {
            "type": "string",
            "title": "Name"
          },
          "trials": {
            "anyOf": [
              {
                "items": {
                  "type": "integer"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "title": "Trials",
            "description": "The feature's selection. null = the session's whole trial list."
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "session_id",
          "feature",
          "name"
        ],
        "title": "CreateAnalysisRequest",
        "description": "\u2b50 **THE SERVER CREATES THE DOCUMENT**, via the feature's `new_document` hook.\n\n\ud83d\udd34 In v1 `project.new_doc()` and `project.seed_from_build()` were **dead code** \u2014 the front end\nreimplemented both in JS. That is how `human_edits`' divert counters were silently dropped on\nevery save, and how \"Skip \u2014 place by hand\" could erase `seeded_from` while every tile still sat\nexactly where t33 put it. The document is authored on the server. It is not negotiable."
      },
      "CreateMeaProjectRequest": {
        "properties": {
          "name": {
            "type": "string",
            "title": "Name",
            "description": "What he wants to call it. Blank is allowed and becomes a placeholder \u2014 there is no filename to fall back to.",
            "default": ""
          },
          "paths": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Paths",
            "description": "\u2b50 The recordings the wizard's Files step picked, if any. **Create-with-recordings is ONE call, not create-then-add**: a second call that failed would strand a project he can see on the home screen and cannot use. Omit it (or send `[]`) and this is exactly the empty-shelf create, which stays supported \u2014 that is the project whose recordings he has since removed."
          }
        },
        "additionalProperties": false,
        "type": "object",
        "title": "CreateMeaProjectRequest",
        "description": "\u2b50 THE SERVER CREATES THE DOCUMENT \u2014 `POST /api/mea/projects` -> 201 `AnalysisSummary`.\n\n\u2b50 **A NAME, AND NOTHING ELSE.** The sibling requests each carry the one input their task is\na wrapper around \u2014 `CreateAnalysisRequest` a session, `CreateVideoProjectRequest` a video\npath. This one carries none, because the project *is the shelf*: he names it, and puts\nrecordings on it from inside. Asking for the first `.h5` here was considered and rejected.\n\n\u2b50 **And no `folder`, like every create since R44** \u2014 the project is made in Camea's own\nstore. Between that and having no input file, this route asks **zero path questions**, which\nis one fewer than any other way to make a project."
      },
      "CreateVideoProjectRequest": {
        "properties": {
          "name": {
            "type": "string",
            "title": "Name"
          },
          "video_path": {
            "type": "string",
            "title": "Video Path"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "name",
          "video_path"
        ],
        "title": "CreateVideoProjectRequest",
        "description": "\u2b50 THE SERVER CREATES THE DOCUMENT \u2014 `POST /api/videomosaic/projects`. The video-source\nsibling of `CreateAnalysisRequest`: no session, the probed receipt becomes `doc.source`.\n\n\u2b50 **NO `folder` \u2014 and neither has the dataset task since R44 (2026-08-10).** Both create their\nproject in Camea's store. R43 got here first for this feature, by deferring the folder question\nuntil the mosaic existed; R44 removed the question instead."
      },
      "DatasetAtRequest": {
        "properties": {
          "path": {
            "type": "string",
            "title": "Path"
          },
          "depth": {
            "type": "integer",
            "maximum": 2.0,
            "minimum": 1.0,
            "title": "Depth",
            "description": "1 = this folder only. 2 (the default) = this folder, or the acquisitions DIRECTLY inside it \u2014 he may have typed the parent. Never deeper.",
            "default": 2
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "path"
        ],
        "title": "DatasetAtRequest",
        "description": "`POST /api/datasets/at` \u2014 *\"look at this folder.\"* (A POST, not a GET: Windows paths in query\nstrings are an encoding trap.)\n\n\u26d4 **Nothing is remembered and nothing is recommended.** The folder is either an acquisition, or\nit directly contains some \u2014 and that is as far as it looks. A deeper walk would be the app going\nlooking for the user's data, which is exactly what was removed on 2026-07-25."
      },
      "DatasetDetail": {
        "properties": {
          "summary": {
            "$ref": "#/components/schemas/DatasetSummary"
          },
          "trials": {
            "items": {
              "$ref": "#/components/schemas/TrialMeta"
            },
            "type": "array",
            "title": "Trials"
          },
          "blocks": {
            "items": {
              "$ref": "#/components/schemas/SnapshotBlock"
            },
            "type": "array",
            "title": "Blocks",
            "description": "Contiguous runs of Snapshot trials. A feature selects from these; core does not choose for it."
          }
        },
        "type": "object",
        "required": [
          "summary",
          "trials",
          "blocks"
        ],
        "title": "DatasetDetail",
        "description": "Everything the browser can say about a dataset **without loading a pixel**."
      },
      "DatasetListResponse": {
        "properties": {
          "path": {
            "type": "string",
            "title": "Path",
            "description": "The folder that was looked at, resolved."
          },
          "is_dataset": {
            "type": "boolean",
            "title": "Is Dataset",
            "description": "True when that folder IS one acquisition (log.txt + NNN.xml).",
            "default": false
          },
          "datasets": {
            "items": {
              "$ref": "#/components/schemas/DatasetSummary"
            },
            "type": "array",
            "title": "Datasets"
          },
          "skipped": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Skipped",
            "description": "Folders that looked like a dataset but could not be read."
          }
        },
        "type": "object",
        "required": [
          "path",
          "datasets"
        ],
        "title": "DatasetListResponse",
        "description": "What is at ONE folder the user just named. \u26d4 Not a registry \u2014 nothing here was remembered\nor scanned on the app's initiative."
      },
      "DatasetSummary": {
        "properties": {
          "key": {
            "type": "string",
            "title": "Key",
            "description": "Stable id: basename + sha1 of the RESOLVED directory. \u2b50 Two folders both named `260620d` under different parents must not share a cache file or an autosave slot."
          },
          "name": {
            "type": "string",
            "title": "Name",
            "description": "The folder's basename."
          },
          "path": {
            "type": "string",
            "title": "Path"
          },
          "experiment": {
            "type": "string",
            "title": "Experiment",
            "description": "The acquisition's own name (log.txt `New experiment:`), which may differ from the folder's. Recorded because a directory label can lie. Nothing branches on it.",
            "default": ""
          },
          "n_trials": {
            "type": "integer",
            "title": "N Trials",
            "description": "Every trial in log.txt, of any type."
          },
          "n_snapshots": {
            "type": "integer",
            "title": "N Snapshots"
          },
          "first_at": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "First At"
          },
          "last_at": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last At"
          },
          "shapes": {
            "items": {
              "$ref": "#/components/schemas/ShapeGroup"
            },
            "type": "array",
            "title": "Shapes"
          },
          "thumbnail_url": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Thumbnail Url",
            "description": "GET /api/datasets/{key}/thumbnail.png \u2014 one frame, no session."
          },
          "analyses": {
            "items": {
              "$ref": "#/components/schemas/AnalysisRef"
            },
            "type": "array",
            "title": "Analyses"
          },
          "last_opened": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Opened"
          },
          "warnings": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Warnings"
          }
        },
        "type": "object",
        "required": [
          "key",
          "name",
          "path",
          "n_trials",
          "n_snapshots"
        ],
        "title": "DatasetSummary",
        "description": "One card in the browser."
      },
      "DialogOpenDirectoryRequest": {
        "properties": {
          "title": {
            "type": "string",
            "title": "Title",
            "default": "Choose a directory"
          },
          "start": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Start"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "title": "DialogOpenDirectoryRequest"
      },
      "DialogOpenFileRequest": {
        "properties": {
          "title": {
            "type": "string",
            "title": "Title",
            "default": "Open a file"
          },
          "filters": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Filters"
          },
          "allow_multiple": {
            "type": "boolean",
            "title": "Allow Multiple",
            "description": "\u2b50 Several files in one gesture \u2014 what *'opens file explorer, can import multiple at a time'* means when there IS a desktop. Defaults False, which is what every existing caller wants and what this route did before. \u26a0\ufe0f Only reachable with `--window`; over VSCode remote this route is a 501 and Camea's own served picker is the real door.",
            "default": false
          }
        },
        "additionalProperties": false,
        "type": "object",
        "title": "DialogOpenFileRequest"
      },
      "DialogPathResponse": {
        "properties": {
          "path": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Path",
            "description": "null = the user cancelled. With `allow_multiple` this is the FIRST of `paths`, so every existing caller keeps working unchanged."
          },
          "paths": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Paths",
            "description": "Everything he chose, in order. Empty = cancelled. A single-file dialog returns a list of one."
          }
        },
        "type": "object",
        "title": "DialogPathResponse"
      },
      "DialogSaveFileRequest": {
        "properties": {
          "title": {
            "type": "string",
            "title": "Title",
            "default": "Save"
          },
          "default_name": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Default Name"
          },
          "filters": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Filters"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "title": "DialogSaveFileRequest"
      },
      "DiscardMachineRequest": {
        "properties": {
          "doc": {
            "$ref": "#/components/schemas/MosaicDocument"
          },
          "confirm": {
            "type": "boolean",
            "const": true,
            "title": "Confirm",
            "description": "Must be literally `true`. There is no path through this route by accident."
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "doc",
          "confirm"
        ],
        "title": "DiscardMachineRequest",
        "description": "`POST /api/mosaic/document/discard-machine` \u2014 *\"Skip \u2014 place by hand\"*.\n\n\ud83d\udd34 **IT IS DESTRUCTIVE, OR IT IS NOTHING.** If anything in the document came from a machine it\ndiscards **every position and the build** (and `machine`, `ncc`, `margin`, `seq`, `human`,\n`source`). Otherwise it launders a machine build into an \"independent ground truth\": v1 nulled\n`build`, nulled `seeded_from`, set `independent_of_method: true`, deleted the warning \u2014 and\n**did not touch a single tile.** Every tile kept t33's position. **Score t33 against that and it\ngets ~100 % by construction. That is the exact mechanism that already destroyed one benchmark in\nthis project.**\n\nCall `machine-evidence` first to name the count in the confirm."
      },
      "DiscardMachineResponse": {
        "properties": {
          "doc": {
            "$ref": "#/components/schemas/MosaicDocument"
          },
          "n_positions_discarded": {
            "type": "integer",
            "title": "N Positions Discarded"
          },
          "had_build": {
            "type": "boolean",
            "title": "Had Build"
          }
        },
        "type": "object",
        "required": [
          "doc",
          "n_positions_discarded",
          "had_build"
        ],
        "title": "DiscardMachineResponse"
      },
      "Document": {
        "properties": {
          "schema_version": {
            "type": "string",
            "title": "Schema Version"
          },
          "app": {
            "$ref": "#/components/schemas/AppStamp"
          },
          "id": {
            "type": "string",
            "title": "Id"
          },
          "feature": {
            "type": "string",
            "title": "Feature"
          },
          "dataset": {
            "type": "string",
            "title": "Dataset"
          },
          "experiment": {
            "type": "string",
            "title": "Experiment",
            "default": ""
          },
          "data_dir": {
            "type": "string",
            "title": "Data Dir"
          },
          "dataset_key": {
            "type": "string",
            "title": "Dataset Key"
          },
          "created": {
            "type": "string",
            "title": "Created"
          },
          "modified": {
            "type": "string",
            "title": "Modified"
          },
          "provenance": {
            "$ref": "#/components/schemas/Provenance"
          }
        },
        "additionalProperties": true,
        "type": "object",
        "required": [
          "schema_version",
          "app",
          "id",
          "feature",
          "dataset",
          "data_dir",
          "dataset_key",
          "created",
          "modified",
          "provenance"
        ],
        "title": "Document",
        "description": "The generic envelope. **What every analysis has, whatever the feature is.**\n\nUnknown top-level keys \u2014 including a whole feature payload \u2014 are preserved **verbatim**."
      },
      "DocumentProblem": {
        "properties": {
          "kind": {
            "type": "string",
            "title": "Kind"
          },
          "message": {
            "type": "string",
            "title": "Message"
          },
          "severity": {
            "type": "string",
            "enum": [
              "error",
              "warning"
            ],
            "title": "Severity",
            "default": "error"
          }
        },
        "type": "object",
        "required": [
          "kind",
          "message"
        ],
        "title": "DocumentProblem"
      },
      "DocumentResponse": {
        "properties": {
          "doc": {
            "$ref": "#/components/schemas/Document"
          }
        },
        "type": "object",
        "required": [
          "doc"
        ],
        "title": "DocumentResponse"
      },
      "DroppedTrial": {
        "properties": {
          "trial": {
            "type": "integer",
            "title": "Trial"
          },
          "reason": {
            "type": "string",
            "enum": [
              "off_shape",
              "not_snapshot"
            ],
            "title": "Reason"
          },
          "w": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "W"
          },
          "h": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "H"
          }
        },
        "type": "object",
        "required": [
          "trial",
          "reason"
        ],
        "title": "DroppedTrial"
      },
      "ElectrodeCells": {
        "properties": {
          "col": {
            "items": {
              "type": "integer"
            },
            "type": "array",
            "title": "Col"
          },
          "row": {
            "items": {
              "type": "integer"
            },
            "type": "array",
            "title": "Row"
          },
          "x": {
            "items": {
              "type": "number"
            },
            "type": "array",
            "title": "X"
          },
          "y": {
            "items": {
              "type": "number"
            },
            "type": "array",
            "title": "Y"
          },
          "kind": {
            "items": {
              "type": "integer"
            },
            "type": "array",
            "title": "Kind"
          },
          "x_um": {
            "items": {
              "type": "number"
            },
            "type": "array",
            "title": "X Um",
            "description": "\u00b5m along the column axis. Empty on maps written before R45.8 (and whenever no device spec applied) \u2014 never assume it is populated."
          },
          "y_um": {
            "items": {
              "type": "number"
            },
            "type": "array",
            "title": "Y Um",
            "description": "\u00b5m along the row axis."
          }
        },
        "type": "object",
        "required": [
          "col",
          "row",
          "x",
          "y",
          "kind"
        ],
        "title": "ElectrodeCells",
        "description": "Flat parallel arrays, one entry per EXISTING grid position (absent corners omitted).\n`kind`: 1 = detected on the pixels, 2 = inferred from the lattice (occluded/dead pad).\n\n`x`/`y` are pixels in the feature's own space; `x_um`/`y_um` are the SAME positions in the\nARRAY's frame \u2014 origin at electrode 1-1's lattice position, rotation taken out, so x grows\nalong columns and y along rows. They land near `(col-1) * 17.5` / `(row-1) * 17.5` but carry\nthe real measured deviation. Both are served: \u00b5m is added ALONGSIDE pixels, never instead."
      },
      "ElectrodeDevice": {
        "properties": {
          "name": {
            "type": "string",
            "title": "Name",
            "description": "Whose numbers these are, e.g. \"MaxWell MaxOne/MaxTwo\"."
          },
          "axes": {
            "prefixItems": [
              {
                "type": "integer"
              },
              {
                "type": "integer"
              }
            ],
            "type": "array",
            "maxItems": 2,
            "minItems": 2,
            "title": "Axes",
            "description": "The two array axis lengths, **ORIENTATION-FREE** \u2014 the chip can sit portrait or landscape in a mosaic, so neither the fitter nor the UI may assume which one is columns."
          },
          "pitch_um": {
            "type": "number",
            "title": "Pitch Um",
            "description": "Centre-to-centre electrode spacing, \u00b5m. The physical ruler that turns the MEASURED pixel pitch into a scale (`um_per_px = pitch_um / pitch_px`) \u2014 in BOTH coverage modes. It is never used to assume a pitch: R45.1 stands, the lattice is measured."
          },
          "electrodes": {
            "type": "integer",
            "title": "Electrodes",
            "description": "axes[0] * axes[1]. Derived by the spec, never typed in twice \u2014 and the number a 'whole chip imaged' map must contain, exactly."
          }
        },
        "type": "object",
        "required": [
          "name",
          "axes",
          "pitch_um",
          "electrodes"
        ],
        "title": "ElectrodeDevice",
        "description": "`GET /api/electrodes/device` \u2014 \u2b50 **THE CHIP, ON THE WIRE, so nothing has to retype it.**\n\n\ud83d\udd34 R45.8 put every device number in exactly one place (`core.electrodegrid.DeviceSpec` /\n`MAXWELL`) \u2014 and then the coverage question the UI asks *before* mapping wrote them out a second\ntime as button prose (\"220 \u00d7 120\", \"26,400\", \"17.5 \u00b5m\"), in TypeScript, where nothing keeps them\nhonest. Change `DeviceSpec.axes` and the panel goes on promising the old array while the fitter\nenforces the new one: **the UI describing a rule it is not the one enforcing.** This model is the\nfix \u2014 the spec is served, typed, and the UI reads it instead of repeating it.\n\n\u26d4 **No field here has a default.** A default would be a second copy of the device fact, which is\nthe very duplication this endpoint exists to close. The values are `MAXWELL`'s, whatever they are."
      },
      "ElectrodeMapBlock": {
        "properties": {
          "built_at": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Built At"
          },
          "cols": {
            "type": "integer",
            "title": "Cols",
            "default": 0
          },
          "rows": {
            "type": "integer",
            "title": "Rows",
            "default": 0
          },
          "pitch_px": {
            "type": "number",
            "title": "Pitch Px",
            "default": 0.0
          },
          "angle_deg": {
            "type": "number",
            "title": "Angle Deg",
            "default": 0.0
          },
          "hit_radius_px": {
            "type": "number",
            "title": "Hit Radius Px",
            "default": 0.0
          },
          "stats": {
            "additionalProperties": true,
            "type": "object",
            "title": "Stats",
            "description": "the fit's own numbers, plus `um_per_px` and \u2014 when the whole chip was declared imaged and the fit was a near miss \u2014 `shape_corrected` ({'cols': +1, 'rows': -1}), which the panel MUST show: a corrected shape is never silent."
          },
          "outputs": {
            "additionalProperties": {
              "type": "string"
            },
            "type": "object",
            "title": "Outputs",
            "description": "logical name ('map', 'csv') -> FILENAME in outputs/."
          },
          "source_stamp": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Source Stamp"
          },
          "coordinates": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Coordinates"
          },
          "array_coverage": {
            "type": "string",
            "title": "Array Coverage",
            "description": "what the user declared before mapping: 'full' or 'partial'. Recorded so a reopened project still knows whether 1-1 is the chip's corner or only the imaged region's (R45.8). Old blocks predate the field and read as 'partial'.",
            "default": "partial"
          },
          "um_per_px": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Um Per Px",
            "description": "device pitch / measured pitch_px. None when no device applied."
          },
          "device": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Device",
            "description": "the device NAME the spec came from ('MaxWell MaxOne/MaxTwo'). The full spec (axes, pitch, electrode count) rides on the payload, not on this summary."
          }
        },
        "additionalProperties": true,
        "type": "object",
        "title": "ElectrodeMapBlock",
        "description": "`doc[\"electrodes\"]` \u2014 the SUMMARY of a fitted grid (the full per-electrode table lives in\n`outputs/<name>-electrodes.json`, listed here by filename). `source_stamp` identifies the tile\npositions / build the map was computed from; when it no longer matches, the map is STALE and\nthe UI says so instead of silently highlighting drifted centres."
      },
      "ElectrodeMapPayload": {
        "properties": {
          "cols": {
            "type": "integer",
            "title": "Cols"
          },
          "rows": {
            "type": "integer",
            "title": "Rows"
          },
          "pitch_px": {
            "type": "number",
            "title": "Pitch Px"
          },
          "angle_deg": {
            "type": "number",
            "title": "Angle Deg"
          },
          "hit_radius_px": {
            "type": "number",
            "title": "Hit Radius Px"
          },
          "a1": {
            "prefixItems": [
              {
                "type": "number"
              },
              {
                "type": "number"
              }
            ],
            "type": "array",
            "maxItems": 2,
            "minItems": 2,
            "title": "A1",
            "description": "mean column step vector (one cell rightward), px"
          },
          "a2": {
            "prefixItems": [
              {
                "type": "number"
              },
              {
                "type": "number"
              }
            ],
            "type": "array",
            "maxItems": 2,
            "minItems": 2,
            "title": "A2",
            "description": "mean row step vector (one cell downward), px"
          },
          "canvas_offset": {
            "prefixItems": [
              {
                "type": "number"
              },
              {
                "type": "number"
              }
            ],
            "type": "array",
            "maxItems": 2,
            "minItems": 2,
            "title": "Canvas Offset",
            "description": "world = rendered-canvas px + this offset. (0,0) for the videomosaic."
          },
          "coordinates": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Coordinates"
          },
          "built_at": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Built At"
          },
          "stale": {
            "type": "boolean",
            "title": "Stale",
            "description": "True when the mosaic changed after this map was built (tile positions edited / rebuilt) \u2014 re-run the mapping."
          },
          "stats": {
            "additionalProperties": true,
            "type": "object",
            "title": "Stats"
          },
          "um_per_px": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Um Per Px",
            "description": "MEASURED scale: the device's known pitch (17.5 \u00b5m) over the pitch this image actually shows. None when the map was written without a device spec. \u26a0\ufe0f Not the provenance `Scale.um_per_px` (R30) \u2014 that one stays absent."
          },
          "device": {
            "anyOf": [
              {
                "additionalProperties": true,
                "type": "object"
              },
              {
                "type": "null"
              }
            ],
            "title": "Device",
            "description": "the spec that supplied the \u00b5m scale: {name, axes, pitch_um, electrodes}. None on maps written before R45.8."
          },
          "array_coverage": {
            "type": "string",
            "title": "Array Coverage",
            "description": "'full' = the user declared the whole chip imaged, so the shape was checked against the device (and any near miss corrected \u2014 see `stats.shape_corrected`). 'partial' = only part of the chip: **1-1 is the top-left of the IMAGED REGION, not of the chip**, and the UI must say so.",
            "default": "partial"
          },
          "cells": {
            "$ref": "#/components/schemas/ElectrodeCells"
          }
        },
        "type": "object",
        "required": [
          "cols",
          "rows",
          "pitch_px",
          "angle_deg",
          "hit_radius_px",
          "a1",
          "a2",
          "canvas_offset",
          "stale",
          "cells"
        ],
        "title": "ElectrodeMapPayload",
        "description": "The full electrode map, served typed so the client never re-derives geometry. A pixel\nresolves to electrode `col-row` only within `hit_radius_px` of that centre (his click rule:\nthe pad plus a slim margin; the gaps select nothing \u2014 arrow keys step to neighbours)."
      },
      "ElectrodeMapRequest": {
        "properties": {
          "session_id": {
            "type": "string",
            "title": "Session Id"
          },
          "doc": {
            "$ref": "#/components/schemas/MosaicDocument"
          },
          "include_unverified": {
            "type": "boolean",
            "title": "Include Unverified",
            "description": "Render (and therefore map) the same tile set the export defaults to: anchored plus unverified. False = certified field only.",
            "default": true
          },
          "array_coverage": {
            "type": "string",
            "enum": [
              "full",
              "partial"
            ],
            "title": "Array Coverage",
            "description": "Did the user say the WHOLE chip is in frame? 'full' enforces the device shape (220 x 120 = 26,400 electrodes, either orientation) and corrects a near miss; 'partial' enforces nothing but the 17.5 um scale, and 1-1 is the top-left of the imaged region. R45.8 \u2014 he picks before mapping; the server default is the assumption-free one.",
            "default": "partial"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "session_id",
          "doc"
        ],
        "title": "ElectrodeMapRequest",
        "description": "`POST /api/mosaic/electrodes/map` \u2014 fit the electrode grid of the built mosaic."
      },
      "ElectrodeMapResult": {
        "properties": {
          "kind": {
            "type": "string",
            "const": "electrode_map",
            "title": "Kind",
            "default": "electrode_map"
          },
          "analysis_id": {
            "type": "string",
            "title": "Analysis Id"
          },
          "electrodes": {
            "$ref": "#/components/schemas/ElectrodeMapBlock"
          },
          "doc": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/Document"
              },
              {
                "type": "null"
              }
            ]
          }
        },
        "type": "object",
        "required": [
          "analysis_id",
          "electrodes"
        ],
        "title": "ElectrodeMapResult",
        "description": "`job.result` of an `electrode_map` job (either feature). The videomosaic job saves its\ndocument server-side and returns it; the mosaic feature's document is CLIENT-owned, so the UI\nmerges `electrodes` into its doc and saves \u2014 same split as build vs export."
      },
      "ElectrodeTracePayload": {
        "properties": {
          "electrode": {
            "type": "string",
            "title": "Electrode",
            "description": "The Camea grid id that was clicked, e.g. \"42-117\"."
          },
          "recorded": {
            "type": "boolean",
            "title": "Recorded",
            "description": "False when this pad was never routed \u2014 the ordinary case. Render as a fact; there is no trace to draw."
          },
          "run_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Run Id"
          },
          "channel": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Channel",
            "description": "The MaxWell channel carrying it."
          },
          "chip_electrode": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Chip Electrode",
            "description": "MaxWell's own electrode id."
          },
          "t0_s": {
            "type": "number",
            "title": "T0 S",
            "default": 0.0
          },
          "t1_s": {
            "type": "number",
            "title": "T1 S",
            "default": 0.0
          },
          "sampling_hz": {
            "type": "number",
            "title": "Sampling Hz",
            "default": 0.0
          },
          "trace_uv": {
            "items": {
              "type": "number"
            },
            "type": "array",
            "title": "Trace Uv",
            "description": "The stored waveform in \u00b5V. \u26a0\ufe0f Judge it with `health` before believing it."
          },
          "health": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/TraceHealthPayload"
              },
              {
                "type": "null"
              }
            ]
          },
          "spikes": {
            "items": {
              "$ref": "#/components/schemas/MeaSpike"
            },
            "type": "array",
            "title": "Spikes",
            "description": "Detected spikes inside the window. Trustworthy independently of the trace."
          },
          "n_spikes_total": {
            "type": "integer",
            "title": "N Spikes Total",
            "description": "Across the whole recording, not the window.",
            "default": 0
          },
          "first_spike_s": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "First Spike S",
            "description": "When this electrode first fired. \u2b50 The UI opens its window HERE rather than at t=0: a 300 s recording stepped one second at a time would take 200 clicks to reach the activity, and a dead opening window makes a working electrode look broken."
          },
          "duration_s": {
            "type": "number",
            "title": "Duration S",
            "description": "The whole recording, so the UI can scrub.",
            "default": 0.0
          },
          "sync_episodes": {
            "items": {
              "$ref": "#/components/schemas/MeaSyncEpisode"
            },
            "type": "array",
            "title": "Sync Episodes"
          },
          "orientation": {
            "$ref": "#/components/schemas/MeaOrientation"
          }
        },
        "type": "object",
        "required": [
          "electrode",
          "recorded"
        ],
        "title": "ElectrodeTracePayload",
        "description": "Everything the panel needs for one clicked electrode."
      },
      "ExportRequest": {
        "properties": {
          "session_id": {
            "type": "string",
            "title": "Session Id"
          },
          "basename": {
            "type": "string",
            "title": "Basename",
            "description": "What the exported files are CALLED. \u2b50 Not where they go \u2014 since R44 they go into this project's outputs/, and the user is never asked for a folder."
          },
          "doc": {
            "$ref": "#/components/schemas/MosaicDocument"
          },
          "outputs": {
            "items": {
              "type": "string",
              "enum": [
                "tiff",
                "coverage",
                "png",
                "positions",
                "gt",
                "qc"
              ]
            },
            "type": "array",
            "title": "Outputs"
          },
          "render_mode": {
            "type": "string",
            "enum": [
              "feather",
              "median",
              "alpha"
            ],
            "title": "Render Mode",
            "default": "feather"
          },
          "include_unverified": {
            "type": "boolean",
            "title": "Include Unverified",
            "default": true
          },
          "um_per_px": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Um Per Px",
            "description": "\ud83d\udccf See `Scale`. Pixels only by default."
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "session_id",
          "basename",
          "doc"
        ],
        "title": "ExportRequest",
        "description": "`POST /api/mosaic/export` \u2192 202 `JobRef`. Holds the `gpu` lease.\n\n**What is rendered:** `anchored`, plus `unverified` **iff `include_unverified`**. `excluded` and\n`unplaced` are **never** rendered. Positions are normalised so `origin_trial` sits at exactly\n`(0, 0)`, matching `analysis/ground_truth/`.\n\n`render_mode` default **feather** \u2014 the only interactive one (feather 1.11 s; median 41.7 s;\nalpha 74 s).\n\n\u2b50\u2b50 **TIFF AND PNG ARE TWO DIFFERENT RENDERS.** The `flat` flag decides what the pixels *are*:\n`flat=True` is a *picture* (per-tile gain); `flat=False` is **raw camera counts** = the TIFF.\nThe TIFF was once rendered `flat=True` while its header said RAW \u2014 trial 11's median went\n2111 \u2192 3435 (\u00d71.63).\n\n\ud83d\udd34 The export must describe the document **AS EXPORTED** (the *stamped* one), not as posted. A\nlaundered doc once produced a GT JSON saying \"NOT AN INDEPENDENT GROUND TRUTH\" beside a TIFF\nheader saying \"hand-placed from scratch\"."
      },
      "ExportResult": {
        "properties": {
          "kind": {
            "type": "string",
            "const": "export",
            "title": "Kind",
            "default": "export"
          },
          "files": {
            "items": {
              "$ref": "#/components/schemas/ExportedFile"
            },
            "type": "array",
            "title": "Files"
          },
          "doc": {
            "$ref": "#/components/schemas/MosaicDocument",
            "description": "The document **as exported** \u2014 stamped. Not as posted."
          },
          "warnings": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Warnings"
          }
        },
        "type": "object",
        "required": [
          "files",
          "doc"
        ],
        "title": "ExportResult",
        "description": "`Job.result` when `kind == \"export\"`. **7 files.**\n\n\u26a0\ufe0f\u26a0\ufe0f **THE COVERAGE MASK IS MANDATORY**, not an option: **13.1 % of the canvas is background\nencoded as exactly `0.0`**, indistinguishable from a legitimately black pixel, and a TIFF has\n**no alpha channel**. Without the sidecar, \"empty\" and \"black\" merge forever. It is free\n(`wsum > 0` in the feather path). Asking for `tiff` implies `coverage`.\n\n`positions.csv`'s header is **exactly** `trial,x,y,state` \u2014 `score.load_positions` DictReads the\nfirst three. The GT JSON is asserted **scoreable** (`tiles[k].status`, `tolerance_px.region_default`)\nbefore it leaves the process."
      },
      "ExportedFile": {
        "properties": {
          "kind": {
            "type": "string",
            "enum": [
              "tiff",
              "coverage",
              "png",
              "positions",
              "gt",
              "qc"
            ],
            "title": "Kind"
          },
          "path": {
            "type": "string",
            "title": "Path"
          },
          "bytes": {
            "type": "integer",
            "title": "Bytes"
          }
        },
        "type": "object",
        "required": [
          "kind",
          "path",
          "bytes"
        ],
        "title": "ExportedFile"
      },
      "FailedMigration": {
        "properties": {
          "path": {
            "type": "string",
            "title": "Path"
          },
          "reason": {
            "type": "string",
            "title": "Reason",
            "description": "Said out loud, because the user's work is in that folder."
          }
        },
        "type": "object",
        "required": [
          "path",
          "reason"
        ],
        "title": "FailedMigration",
        "description": "One project that did **not** move. \u2b50 It is still exactly where it was."
      },
      "FsEntry": {
        "properties": {
          "name": {
            "type": "string",
            "title": "Name"
          },
          "path": {
            "type": "string",
            "title": "Path"
          },
          "is_dataset": {
            "type": "boolean",
            "title": "Is Dataset",
            "description": "It has a `log.txt` and at least one `NNN.xml`. \u26d4 Recognised by SHAPE, never by name \u2014 nothing in this app knows what a dataset is called."
          },
          "n_children": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "N Children",
            "description": "Sub-directories. null when the folder could not be read."
          }
        },
        "type": "object",
        "required": [
          "name",
          "path",
          "is_dataset"
        ],
        "title": "FsEntry",
        "description": "One row in the folder picker."
      },
      "FsListResponse": {
        "properties": {
          "path": {
            "type": "string",
            "title": "Path"
          },
          "parent": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Parent",
            "description": "null at a filesystem root."
          },
          "is_dataset": {
            "type": "boolean",
            "title": "Is Dataset"
          },
          "entries": {
            "items": {
              "$ref": "#/components/schemas/FsEntry"
            },
            "type": "array",
            "title": "Entries"
          },
          "roots": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Roots",
            "description": "The drives / mount points, so the picker can start from nothing."
          },
          "error": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Error",
            "description": "Set when `path` itself could not be listed. Not an HTTP error \u2014 the picker must still render its roots and its parent."
          }
        },
        "type": "object",
        "required": [
          "path",
          "is_dataset",
          "entries",
          "roots"
        ],
        "title": "FsListResponse",
        "description": "`GET /api/fs/list` \u2014 the served folder picker. See the section note above."
      },
      "GapsRequest": {
        "properties": {
          "trials": {
            "items": {
              "type": "integer"
            },
            "type": "array",
            "title": "Trials"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "trials"
        ],
        "title": "GapsRequest",
        "description": "`POST /api/mosaic/gaps` \u2014 a **pure function over a trial list**.\n\n\u2b50 `engine.excluded.gaps()` is the ONLY symbol the app may import from that module. Never\n`EXCLUDED` / `BLANK` / `BLURRY` / `usable_trials`. There is no toggle. This route is the single\nplace the app touches it, and it is called on every change to the excluded set."
      },
      "GapsResponse": {
        "properties": {
          "gaps": {
            "items": {
              "prefixItems": [
                {
                  "type": "integer"
                },
                {
                  "type": "integer"
                }
              ],
              "type": "array",
              "maxItems": 2,
              "minItems": 2
            },
            "type": "array",
            "title": "Gaps"
          }
        },
        "type": "object",
        "required": [
          "gaps"
        ],
        "title": "GapsResponse"
      },
      "GpuInfo": {
        "properties": {
          "available": {
            "type": "boolean",
            "title": "Available"
          },
          "backend": {
            "type": "string",
            "enum": [
              "cupy",
              "numpy"
            ],
            "title": "Backend"
          },
          "name": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Name"
          },
          "cupy": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Cupy"
          },
          "cuda_runtime": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Cuda Runtime"
          },
          "reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Reason",
            "description": "Why there is no GPU. Human-readable, and often actionable."
          },
          "note": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Note",
            "description": "What it costs the user, in real numbers."
          }
        },
        "type": "object",
        "required": [
          "available",
          "backend"
        ],
        "title": "GpuInfo",
        "description": "\ud83d\udd34 **DETECTION MUST EXECUTE A REAL OP.** `import cupy` *succeeds* on a broken CUDA install;\nit is `cupy.zeros(1) + 1` that raises. There is exactly **one** detector \u2014 `t27.xp()` \u2014 and\nthere must never be a second. `reason` is a *string for the human*, never a verdict: \"no GPU\"\nbecause a CUDA DLL is off the search path is **fixable** and is not the same thing as \"no card\"."
      },
      "HTTPValidationError": {
        "properties": {
          "detail": {
            "items": {
              "$ref": "#/components/schemas/ValidationError"
            },
            "type": "array",
            "title": "Detail"
          }
        },
        "type": "object",
        "title": "HTTPValidationError"
      },
      "HealthResponse": {
        "properties": {
          "ok": {
            "type": "boolean",
            "title": "Ok"
          },
          "version": {
            "type": "string",
            "title": "Version"
          },
          "python": {
            "type": "string",
            "title": "Python"
          },
          "uptime_s": {
            "type": "number",
            "title": "Uptime S"
          }
        },
        "type": "object",
        "required": [
          "ok",
          "version",
          "python",
          "uptime_s"
        ],
        "title": "HealthResponse"
      },
      "HumanEdits": {
        "properties": {
          "accepted_unchanged": {
            "type": "integer",
            "title": "Accepted Unchanged",
            "default": 0
          },
          "moved": {
            "type": "integer",
            "title": "Moved",
            "default": 0
          },
          "excluded": {
            "type": "integer",
            "title": "Excluded",
            "default": 0
          },
          "unverified": {
            "type": "integer",
            "title": "Unverified",
            "default": 0
          },
          "rescued": {
            "type": "integer",
            "title": "Rescued",
            "default": 0
          },
          "median_move_px": {
            "type": "number",
            "title": "Median Move Px",
            "default": 0.0
          },
          "max_move_px": {
            "type": "number",
            "title": "Max Move Px",
            "default": 0.0
          },
          "diverted_to_solver": {
            "type": "integer",
            "title": "Diverted To Solver",
            "default": 0
          },
          "diverted_trials": {
            "items": {
              "type": "integer"
            },
            "type": "array",
            "title": "Diverted Trials"
          },
          "diverted_note": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Diverted Note"
          }
        },
        "additionalProperties": true,
        "type": "object",
        "title": "HumanEdits",
        "description": "The honest record of what the human actually did. Recomputed on every save. **Every number\nstates its denominator.**\n\n\u26a0\ufe0f `diverted_to_solver` is counted **separately and named next to the number it contaminates**:\na diverted tile *is* sitting exactly on the machine's position, so it lands inside\n`accepted_unchanged` and reads as agreement. It is not. The app never gave the matcher a vote\nthere. (In the defer flow this can be 302 of 311 tiles.)"
      },
      "Job": {
        "properties": {
          "job_id": {
            "type": "string",
            "title": "Job Id"
          },
          "kind": {
            "type": "string",
            "title": "Kind"
          },
          "state": {
            "type": "string",
            "enum": [
              "queued",
              "running",
              "done",
              "failed",
              "cancelled"
            ],
            "title": "State"
          },
          "phase": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Phase"
          },
          "phase_index": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Phase Index"
          },
          "n_phases": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "N Phases"
          },
          "pct": {
            "anyOf": [
              {
                "type": "number",
                "maximum": 100.0,
                "minimum": 0.0
              },
              {
                "type": "null"
              }
            ],
            "title": "Pct"
          },
          "message": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Message"
          },
          "started_at": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Started At"
          },
          "elapsed_s": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Elapsed S"
          },
          "eta_s": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Eta S",
            "description": "Seconds remaining, or null when the phase is silent. May JUMP UP."
          },
          "log_tail": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Log Tail",
            "description": "The last 200 lines the job narrated (the UI shows ~8)."
          },
          "cancellable": {
            "type": "boolean",
            "title": "Cancellable",
            "default": true
          },
          "exclusive": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Exclusive",
            "description": "The exclusive resource lease this job holds, e.g. \"gpu\". While one is held, another job wanting the same lease gets 409 busy."
          },
          "result": {
            "anyOf": [
              {
                "oneOf": [
                  {
                    "$ref": "#/components/schemas/OpenJobResult"
                  },
                  {
                    "$ref": "#/components/schemas/BuildResult"
                  },
                  {
                    "$ref": "#/components/schemas/ExportResult"
                  },
                  {
                    "$ref": "#/components/schemas/RecheckResult"
                  },
                  {
                    "$ref": "#/components/schemas/RecomputeResult"
                  },
                  {
                    "$ref": "#/components/schemas/VideoMosaicBuildResult"
                  },
                  {
                    "$ref": "#/components/schemas/ElectrodeMapResult"
                  },
                  {
                    "$ref": "#/components/schemas/LocateRegionResult"
                  },
                  {
                    "$ref": "#/components/schemas/OrientationTestResult"
                  },
                  {
                    "$ref": "#/components/schemas/MeaCopyResult"
                  },
                  {
                    "$ref": "#/components/schemas/MeaEnvelopeResult"
                  }
                ],
                "discriminator": {
                  "propertyName": "kind",
                  "mapping": {
                    "build": "#/components/schemas/BuildResult",
                    "electrode_map": "#/components/schemas/ElectrodeMapResult",
                    "export": "#/components/schemas/ExportResult",
                    "locate_region": "#/components/schemas/LocateRegionResult",
                    "mea_copy": "#/components/schemas/MeaCopyResult",
                    "mea_envelope": "#/components/schemas/MeaEnvelopeResult",
                    "mea_orientation": "#/components/schemas/OrientationTestResult",
                    "open": "#/components/schemas/OpenJobResult",
                    "recheck": "#/components/schemas/RecheckResult",
                    "recompute": "#/components/schemas/RecomputeResult",
                    "videomosaic_build": "#/components/schemas/VideoMosaicBuildResult"
                  }
                }
              },
              {
                "type": "null"
              }
            ],
            "title": "Result",
            "description": "null until state == 'done'. Discriminated on `kind`."
          },
          "error": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/JobError"
              },
              {
                "type": "null"
              }
            ],
            "description": "Set iff state == 'failed'."
          }
        },
        "type": "object",
        "required": [
          "job_id",
          "kind",
          "state"
        ],
        "title": "Job",
        "description": "Poll at 500 ms. There is no websocket \u2014 it is one client on localhost and a poll is 0.6 ms.\n\n\u26a0\ufe0f **`eta_s` is only recomputed when the child prints a recognised line.** The front end must\nre-anchor its countdown **only when this raw number changes** (BEHAVIOUR R8.2) \u2014 re-anchoring on\nevery 500 ms tick resets the clock and the countdown never moves. And there is deliberately **no\nserver-side ETA heartbeat** (R8b): with a global-linear estimator, re-emitting during a silent\nphase makes the ETA count *up*."
      },
      "JobCancelResponse": {
        "properties": {
          "job_id": {
            "type": "string",
            "title": "Job Id"
          },
          "state": {
            "type": "string",
            "enum": [
              "queued",
              "running",
              "done",
              "failed",
              "cancelled"
            ],
            "title": "State"
          }
        },
        "type": "object",
        "required": [
          "job_id",
          "state"
        ],
        "title": "JobCancelResponse"
      },
      "JobError": {
        "properties": {
          "code": {
            "type": "string",
            "title": "Code"
          },
          "message": {
            "type": "string",
            "title": "Message"
          },
          "traceback": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Traceback"
          }
        },
        "type": "object",
        "required": [
          "code",
          "message"
        ],
        "title": "JobError"
      },
      "JobListResponse": {
        "properties": {
          "jobs": {
            "items": {
              "$ref": "#/components/schemas/Job"
            },
            "type": "array",
            "title": "Jobs",
            "description": "Newest first."
          }
        },
        "type": "object",
        "required": [
          "jobs"
        ],
        "title": "JobListResponse"
      },
      "JobRef": {
        "properties": {
          "job_id": {
            "type": "string",
            "title": "Job Id"
          },
          "kind": {
            "type": "string",
            "title": "Kind"
          }
        },
        "type": "object",
        "required": [
          "job_id",
          "kind"
        ],
        "title": "JobRef",
        "description": "The 202 body of every long operation."
      },
      "LoadDocumentRequest": {
        "properties": {
          "path": {
            "type": "string",
            "title": "Path"
          },
          "session_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Session Id"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "path"
        ],
        "title": "LoadDocumentRequest",
        "description": "`POST /api/documents/load` \u2014 the Load screen's *\"Load a project\u2026\"*, which must be reachable\n**cold** (no session). The server bootstraps a session from the file's own `data_dir`, then\nre-reads the file so the range guard actually runs against the session it belongs to."
      },
      "LoadDocumentResponse": {
        "properties": {
          "doc": {
            "$ref": "#/components/schemas/Document"
          },
          "session": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/SessionResponse"
              },
              {
                "type": "null"
              }
            ],
            "description": "Set when the load had to open the dataset itself."
          },
          "warnings": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Warnings"
          },
          "migrated_from": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Migrated From",
            "description": "The old schema_version, if the file was migrated on the way in."
          }
        },
        "type": "object",
        "required": [
          "doc"
        ],
        "title": "LoadDocumentResponse"
      },
      "LocateRegionRequest": {
        "properties": {
          "analysis_id": {
            "type": "string",
            "title": "Analysis Id"
          },
          "path": {
            "type": "string",
            "title": "Path",
            "description": "The video file to add. \u2b50 The ONE path question allowed inside a project (R44): where the recording came from. It is COPIED into the project, which then owns it."
          },
          "name": {
            "type": "string",
            "title": "Name",
            "description": "Optional label; defaults to the file's name.",
            "default": ""
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "analysis_id",
          "path"
        ],
        "title": "LocateRegionRequest",
        "description": "`POST /api/videomosaic/regions/locate` \u2014 add a recording and place it."
      },
      "LocateRegionResult": {
        "properties": {
          "kind": {
            "type": "string",
            "const": "locate_region",
            "title": "Kind",
            "default": "locate_region"
          },
          "analysis_id": {
            "type": "string",
            "title": "Analysis Id"
          },
          "region": {
            "$ref": "#/components/schemas/RegionRecord"
          },
          "doc": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/Document"
              },
              {
                "type": "null"
              }
            ]
          }
        },
        "type": "object",
        "required": [
          "analysis_id",
          "region"
        ],
        "title": "LocateRegionResult",
        "description": "`job.result` of a `locate_region` job. The videomosaic document is SERVER-owned, so it is\nalready saved when this appears \u2014 the UI adopts `doc` or refetches; it never authors."
      },
      "LogEntry": {
        "properties": {
          "trial": {
            "type": "integer",
            "title": "Trial"
          },
          "type": {
            "type": "string",
            "title": "Type"
          },
          "time": {
            "type": "string",
            "title": "Time"
          },
          "gap_s": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Gap S"
          }
        },
        "type": "object",
        "required": [
          "trial",
          "type",
          "time"
        ],
        "title": "LogEntry"
      },
      "LogResponse": {
        "properties": {
          "experiment": {
            "type": "string",
            "title": "Experiment"
          },
          "entries": {
            "items": {
              "$ref": "#/components/schemas/LogEntry"
            },
            "type": "array",
            "title": "Entries"
          },
          "n_snapshot": {
            "type": "integer",
            "title": "N Snapshot"
          },
          "n_other": {
            "type": "integer",
            "title": "N Other"
          }
        },
        "type": "object",
        "required": [
          "experiment",
          "entries",
          "n_snapshot",
          "n_other"
        ],
        "title": "LogResponse",
        "description": "\u26a0\ufe0f `log.txt` prints the date **only** on `New experiment:` lines; every other line carries\n`HH:MM:SS` alone. The parser carries the date forward and handles midnight rollover."
      },
      "MachineEvidenceRequest": {
        "properties": {
          "doc": {
            "$ref": "#/components/schemas/MosaicDocument"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "doc"
        ],
        "title": "MachineEvidenceRequest"
      },
      "MachineEvidenceResponse": {
        "properties": {
          "seeded_from": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/SeededFrom"
              },
              {
                "type": "null"
              }
            ]
          },
          "has_build": {
            "type": "boolean",
            "title": "Has Build"
          },
          "n_machine_tiles": {
            "type": "integer",
            "title": "N Machine Tiles"
          },
          "independent_of_method": {
            "type": "boolean",
            "title": "Independent Of Method"
          },
          "warning": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Warning",
            "description": "`PROVENANCE_WARNING`, verbatim, or null."
          }
        },
        "type": "object",
        "required": [
          "seeded_from",
          "has_build",
          "n_machine_tiles",
          "independent_of_method"
        ],
        "title": "MachineEvidenceResponse",
        "description": "Powers the provenance panel (a **live warning**, and it stays on the page) and the confirm\ntext of \"Skip \u2014 place by hand\". Derived from HISTORY, never from what the document claims."
      },
      "MatchAnchorRequest": {
        "properties": {
          "session_id": {
            "type": "string",
            "title": "Session Id"
          },
          "target": {
            "type": "integer",
            "title": "Target"
          },
          "anchors": {
            "items": {
              "type": "integer"
            },
            "type": "array",
            "title": "Anchors",
            "description": "The reference field. Order is irrelevant (the server sorts). Must be non-empty and must not contain `target`."
          },
          "positions": {
            "additionalProperties": {
              "prefixItems": [
                {
                  "type": "number"
                },
                {
                  "type": "number"
                }
              ],
              "type": "array",
              "maxItems": 2,
              "minItems": 2
            },
            "type": "object",
            "title": "Positions",
            "description": "World top-left of **every** trial in `anchors`. Extras ignored; a missing anchor is a 400."
          },
          "mode": {
            "type": "string",
            "enum": [
              "global",
              "local"
            ],
            "title": "Mode",
            "default": "global"
          },
          "near": {
            "anyOf": [
              {
                "prefixItems": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "number"
                  }
                ],
                "type": "array",
                "maxItems": 2,
                "minItems": 2
              },
              {
                "type": "null"
              }
            ],
            "title": "Near",
            "description": "REQUIRED when mode == 'local': where the user dropped the tile."
          },
          "radius": {
            "type": "integer",
            "maximum": 256.0,
            "minimum": 8.0,
            "title": "Radius",
            "description": "local only. \u26a0\ufe0f **Never widen past 128 in the UI**: the electrode grid repeats every **256 px** and a wide *local* search will confidently lock onto a grid alias. To search wide, use `mode: 'global'` \u2014 the FFT + margin is what survives the aliases.",
            "default": 64
          },
          "max_candidates": {
            "type": "integer",
            "maximum": 16.0,
            "minimum": 1.0,
            "title": "Max Candidates",
            "description": "\u2b50 9, or key `9` can never fire.",
            "default": 9
          },
          "refuse": {
            "items": {
              "type": "integer"
            },
            "type": "array",
            "title": "Refuse",
            "description": "\ud83d\udd34 **THE REFUSAL SET, IN THE BODY** \u2014 the blank list the human left standing (`MosaicDocument.blank_scan.blank`). It is part of `cache_key`. In v1 this was hidden server state that `PUT /api/scan/blank` mutated, which is precisely what made this endpoint impure. **That endpoint no longer exists.**"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "session_id",
          "target",
          "anchors",
          "positions"
        ],
        "title": "MatchAnchorRequest",
        "description": "\u2b50\u2b50 **A PURE FUNCTION OF THIS BODY.** The server holds no tile state to be out of sync with.\n\n\ud83d\udd34 **THE PREFETCH, AND THE CORRECTNESS TRAP IN IT.** Every `Space` costs ~1 s, so the front end\nfires the *next* tile's match the instant the current one is **displayed** \u2014 and that prefetch\n**MUST INCLUDE THE TILE CURRENTLY UNDER JUDGEMENT IN `anchors`**, i.e. it must assume the user\nwill press `A`. Prefetching from a composite **without** it **disagrees with the truth in 18 % of\npresses and is catastrophically wrong (up to 1,143 px) in 6 %**.\n\nThe server memoises on\n\n    cache_key = sha1({session nonce, target, [(t, x, y) for t in sorted(anchors)],\n                      mode, near, radius, max_candidates, sorted(refuse)})\n\n\u2014 i.e. **the key IS the anchor set and their positions.** Press `E` instead of `A` and the set\ngenuinely differs \u21d2 the key differs \u21d2 the memo misses \u21d2 the server recomputes honestly. **The\ntrap is structurally impossible to fall into, as long as the server holds no tile state.**\n\n\u21d2 \u26d4 **NEVER add a client-side prefetch cache keyed on the trial number.** The prefetch is\nliterally the same POST, fired early, and its answer is thrown away.\n\u21d2 \u26d4 **Never invent a second server cache keyed on the trial number.**"
      },
      "MatchResult": {
        "properties": {
          "target": {
            "type": "integer",
            "title": "Target"
          },
          "mode": {
            "type": "string",
            "enum": [
              "global",
              "local"
            ],
            "title": "Mode"
          },
          "n_anchors": {
            "type": "integer",
            "title": "N Anchors"
          },
          "composite": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/CompositeInfo"
              },
              {
                "type": "null"
              }
            ]
          },
          "candidates": {
            "items": {
              "$ref": "#/components/schemas/Candidate"
            },
            "type": "array",
            "title": "Candidates"
          },
          "best": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/Candidate"
              },
              {
                "type": "null"
              }
            ]
          },
          "margin": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Margin",
            "description": "candidates[0].ncc \u2212 candidates[1].ncc. null if there is only one."
          },
          "margin_thin": {
            "type": "boolean",
            "title": "Margin Thin",
            "description": "margin < 0.10. \u26a0\ufe0f **THE UI MUST FLAG IT LOUDLY** \u2014 this is what a surviving grid alias looks like. The shipped build's worst run margin is 0.081 vs a ~0.47 typical.",
            "default": false
          },
          "refused": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/Refusal"
              },
              {
                "type": "null"
              }
            ]
          },
          "dropped_anchors": {
            "items": {
              "type": "integer"
            },
            "type": "array",
            "title": "Dropped Anchors",
            "description": "Blank anchors dropped from the composite. Not an error."
          },
          "gpu": {
            "type": "boolean",
            "title": "Gpu"
          },
          "elapsed_ms": {
            "type": "number",
            "title": "Elapsed Ms"
          },
          "cached": {
            "type": "boolean",
            "title": "Cached"
          },
          "cache_key": {
            "type": "string",
            "title": "Cache Key"
          }
        },
        "type": "object",
        "required": [
          "target",
          "mode",
          "n_anchors",
          "gpu",
          "elapsed_ms",
          "cached",
          "cache_key"
        ],
        "title": "MatchResult",
        "description": "`candidates` is sorted by `ncc` descending, peaks \u2265 24 px apart (t33's NMS). It may be empty.\n\n\u26a0\ufe0f **The aperture is small at the start.** With one anchor down this call *is* a tile-pair \u2014 the\nweak case above. It works anyway because consecutive snapshots overlap ~78 %. **But surface the\nevidence** (`n_anchors`, `composite.valid_px`, `ncc`, `margin`) on every placement, so the user\nwatches the evidence strengthen instead of taking it on faith."
      },
      "MatchScoreRequest": {
        "properties": {
          "session_id": {
            "type": "string",
            "title": "Session Id"
          },
          "target": {
            "type": "integer",
            "title": "Target"
          },
          "anchors": {
            "items": {
              "type": "integer"
            },
            "type": "array",
            "title": "Anchors"
          },
          "positions": {
            "additionalProperties": {
              "prefixItems": [
                {
                  "type": "number"
                },
                {
                  "type": "number"
                }
              ],
              "type": "array",
              "maxItems": 2,
              "minItems": 2
            },
            "type": "object",
            "title": "Positions"
          },
          "at": {
            "prefixItems": [
              {
                "type": "number"
              },
              {
                "type": "number"
              }
            ],
            "type": "array",
            "maxItems": 2,
            "minItems": 2,
            "title": "At"
          },
          "refuse": {
            "items": {
              "type": "integer"
            },
            "type": "array",
            "title": "Refuse"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "session_id",
          "target",
          "anchors",
          "positions",
          "at"
        ],
        "title": "MatchScoreRequest",
        "description": "`POST /api/mosaic/match/score` \u2014 *\"you dropped it here; what do the pixels say?\"* One\n`exact_ncc`, no search (~31 ms). Used live during a drag (debounced 150 ms), to label the\nalternatives, and to **re-measure a diverted tile's NCC where it actually sits**.\n\n\u2b50 Deliberately **NOT** serialised behind an in-flight `match/anchor`: it used to wait out a\nwhole match, so the live NCC under the cursor was up to a full match out of date."
      },
      "MeaAttachRequest": {
        "properties": {
          "analysis_id": {
            "type": "string",
            "title": "Analysis Id"
          },
          "mea_dir": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Mea Dir",
            "description": "Where to look. When omitted the server searches beside the project's dataset folder and reports what it found \u2014 the user still confirms before it is saved."
          },
          "confirm": {
            "type": "boolean",
            "title": "Confirm",
            "description": "False = discover and report only. True = save this attachment onto the project.",
            "default": false
          },
          "orientation": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/MeaOrientation"
              },
              {
                "type": "null"
              }
            ]
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "analysis_id"
        ],
        "title": "MeaAttachRequest"
      },
      "MeaAttachment": {
        "properties": {
          "attached": {
            "type": "boolean",
            "title": "Attached"
          },
          "mea_dir": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Mea Dir",
            "description": "The folder the recordings were found in. \u26d4 Read-only, outside the project, never written to."
          },
          "recordings": {
            "items": {
              "$ref": "#/components/schemas/MeaRecordingSummary"
            },
            "type": "array",
            "title": "Recordings"
          },
          "stride": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Stride",
            "description": "The array's long-axis length, DERIVED from the recording's own numbering and verified against every routed electrode \u2014 never a datasheet number."
          },
          "pitch_um": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Pitch Um",
            "description": "Also derived from the numbering, not from the routed spacing (sparse routing would double it)."
          },
          "orientation": {
            "$ref": "#/components/schemas/MeaOrientation"
          },
          "decoder_present": {
            "type": "boolean",
            "title": "Decoder Present",
            "description": "A MaxWell decode plug-in is on this machine. Says NOTHING about whether it decodes correctly \u2014 only a trace's `health` can say that.",
            "default": false
          }
        },
        "type": "object",
        "required": [
          "attached"
        ],
        "title": "MeaAttachment",
        "description": "What electrical data a project has, and whether its trace can be believed."
      },
      "MeaBrowseResult": {
        "properties": {
          "path": {
            "type": "string",
            "title": "Path"
          },
          "recordings": {
            "items": {
              "$ref": "#/components/schemas/MeaRecordingCandidate"
            },
            "type": "array",
            "title": "Recordings"
          },
          "truncated": {
            "type": "boolean",
            "title": "Truncated",
            "description": "The walk hit its ceiling. The UI says so rather than quietly showing a prefix of what is really down there.",
            "default": false
          }
        },
        "type": "object",
        "required": [
          "path"
        ],
        "title": "MeaBrowseResult",
        "description": "`GET /api/mea/browse?path=` \u2014 every recording under a folder. \u26d4 Reads; never writes."
      },
      "MeaChannelTrace": {
        "properties": {
          "analysis_id": {
            "type": "string",
            "title": "Analysis Id"
          },
          "recording_id": {
            "type": "string",
            "title": "Recording Id"
          },
          "channel": {
            "type": "integer",
            "title": "Channel"
          },
          "electrode": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Electrode",
            "description": "MaxWell's own electrode id for this channel, when it is routed."
          },
          "recorded": {
            "type": "boolean",
            "title": "Recorded",
            "description": "\u2b50 False when this channel is not in the recording's routed set \u2014 *'never recorded'* is the ordinary answer, and it must render as a fact about the experiment rather than as an error or an empty chart."
          },
          "x_um": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "X Um"
          },
          "y_um": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Y Um"
          },
          "t0_s": {
            "type": "number",
            "title": "T0 S",
            "default": 0.0
          },
          "t1_s": {
            "type": "number",
            "title": "T1 S",
            "default": 0.0
          },
          "duration_s": {
            "type": "number",
            "title": "Duration S",
            "description": "The whole recording, so the UI can scrub.",
            "default": 0.0
          },
          "sampling_hz": {
            "type": "number",
            "title": "Sampling Hz",
            "default": 0.0
          },
          "trace_uv": {
            "items": {
              "type": "number"
            },
            "type": "array",
            "title": "Trace Uv",
            "description": "The stored waveform in \u00b5V. \u26a0\ufe0f Judge it with `health` before believing it; empty when the raw stream could not be decoded at all (`decode_error` says so)."
          },
          "resolution": {
            "type": "string",
            "enum": [
              "samples",
              "envelope"
            ],
            "title": "Resolution",
            "description": "\u2b50 Which picture came back. `samples` \u2014 `trace_uv` holds real samples, one per stored sample. `envelope` \u2014 `min_uv`/`max_uv` hold the lowest and highest value in each column, and `trace_uv` is empty. \u26d4 Read this field; do not infer it from which array is non-empty, because a genuinely empty window populates neither.",
            "default": "samples"
          },
          "min_uv": {
            "items": {
              "type": "number"
            },
            "type": "array",
            "title": "Min Uv",
            "description": "Envelope only: the lowest \u00b5V in each column, evenly spaced across `[t0_s, t1_s)`. Same length as `max_uv`."
          },
          "max_uv": {
            "items": {
              "type": "number"
            },
            "type": "array",
            "title": "Max Uv",
            "description": "Envelope only: the highest \u00b5V in each column. \u2b50 Min/max, never every n-th sample \u2014 a spike is 5-16 samples wide at 20 kHz and sub-sampling would draw a calm line over a burst. Whatever the eye sees here, a spike is never invisible."
          },
          "max_window_s": {
            "type": "number",
            "title": "Max Window S",
            "description": "\u26a0\ufe0f The widest window `resolution: samples` can return \u2014 a transport limit of this build (30 s at 20 kHz is 600k JSON numbers), **not** a fact about anyone's recording. Ask for anything wider with `max_points` and you get an envelope instead, at any width. Sent so the client never writes the number down.",
            "default": 0.0
          },
          "health": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/TraceHealthPayload"
              },
              {
                "type": "null"
              }
            ]
          },
          "health_scope": {
            "type": "string",
            "enum": [
              "window",
              "recording"
            ],
            "title": "Health Scope",
            "description": "\u26a0\ufe0f **WHAT `health` WAS MEASURED OVER, AND IT CHANGES.** A window read live off the file reports that window (`window`); a wide view served from the precomputed cache reports the **whole recording** (`recording`), because the cache carries its own figure and may not borrow a window's. That matters: the rail fraction genuinely differs with window length (measured 1.000 over 1 s but 0.827 over 30 s on one of his recordings), so `flat` can legitimately differ either side of that boundary. \u26d4 A UI quoting the percentage MUST say which it is quoting \u2014 *'of this stretch'* or *'of this recording'* \u2014 or the number changes as he zooms with nothing on screen to explain it.",
            "default": "window"
          },
          "spikes": {
            "items": {
              "$ref": "#/components/schemas/MeaSpike"
            },
            "type": "array",
            "title": "Spikes",
            "description": "Detected spikes inside the window. \u2b50 Trustworthy independently of the trace."
          },
          "n_spikes_total": {
            "type": "integer",
            "title": "N Spikes Total",
            "description": "On this channel, whole recording.",
            "default": 0
          },
          "first_spike_s": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "First Spike S",
            "description": "When this pad first fired. \u26a0\ufe0f Server-supplied and free, but the `Analyze MEA` screen no longer opens here \u2014 it opens at the start, showing the whole recording, so there is no empty stretch to skip past (his ruling, 2026-08-15)."
          },
          "decode_error": {
            "type": "string",
            "title": "Decode Error",
            "description": "\u26d4 Set when HDF5 could not run MaxWell's filter at all \u2014 the decoder library is absent. Said on the page; the spikes above are still returned and still correct.",
            "default": ""
          }
        },
        "type": "object",
        "required": [
          "analysis_id",
          "recording_id",
          "channel",
          "recorded"
        ],
        "title": "MeaChannelTrace",
        "description": "`GET /api/mea/{analysis_id}/recordings/{recording_id}/trace?channel=&t0=&t1=` \u2014 one pad's\nstored waveform and its spikes, for the window `[t0, t1)`.\n\n\u2b50 **ASKED FOR BY CHANNEL, BECAUSE THE CLICK ALREADY KNOWS ITS CHANNEL.** The videomosaic\n`ElectrodeTracePayload` above answers the same question for a pad clicked in a *mosaic*, where\ntwo thirds of the work is resolving a `col-row` id through the chip's seating. None of that\nexists here, so none of it is on this model \u2014 \u26d4 in particular there is no `orientation` and no\n`chip_electrode`, because the electrode is not in doubt.\n\n\u26a0\ufe0f **`health` IS NOT DECORATION.** On some of this project's files the published MaxWell decoder\nreturns a rail (measured 2026-08-15 across his five imported recordings: 000690/691/692 come back\n94-100% one repeated value, while 000688 and 000689 decode cleanly at 1.1% and 4.4%). A railed\nwindow drawn without that caveat looks **exactly** like a genuinely silent electrode. The spike\nticks stay correct either way and are drawn either way.\n\n\u2b50 **TWO RESOLUTIONS, AND THE CALLER SAYS WHICH IT CAN DRAW.** Without `max_points` this is raw\nsamples and `MAX_TRACE_SECONDS` still caps the window. With `max_points` the server reduces to a\nmin/max envelope \u2014 `min_uv`/`max_uv` instead of `trace_uv` \u2014 and **the cap does not apply**, so a\nwhole 300 s recording is one small response. `resolution` says which arrived; never guess from\nwhich array is populated."
      },
      "MeaChipActivity": {
        "properties": {
          "analysis_id": {
            "type": "string",
            "title": "Analysis Id"
          },
          "recording_id": {
            "type": "string",
            "title": "Recording Id"
          },
          "pads": {
            "items": {
              "$ref": "#/components/schemas/MeaPadActivity"
            },
            "type": "array",
            "title": "Pads"
          },
          "duration_s": {
            "type": "number",
            "title": "Duration S",
            "description": "What `rate_hz` was divided by.",
            "default": 0.0
          },
          "n_spikes": {
            "type": "integer",
            "title": "N Spikes",
            "description": "Every spike in the recording.",
            "default": 0
          },
          "n_pads": {
            "type": "integer",
            "title": "N Pads",
            "default": 0
          },
          "n_silent": {
            "type": "integer",
            "title": "N Silent",
            "description": "\u2b50 Routed pads with **zero** spikes. Silence is a MEASUREMENT \u2014 the amplifier sat on that pad for the whole recording and detected nothing \u2014 which is a different fact from a pad that was never routed, and the chip map must not let the two look alike.",
            "default": 0
          },
          "max_rate_hz": {
            "type": "number",
            "title": "Max Rate Hz",
            "description": "The busiest pad in THIS recording. \u26d4 Derived every time, never a constant: see the class docstring.",
            "default": 0.0
          }
        },
        "type": "object",
        "required": [
          "analysis_id",
          "recording_id"
        ],
        "title": "MeaChipActivity",
        "description": "`GET /api/mea/{analysis_id}/recordings/{recording_id}/activity` \u2014 the per-pad tally the chip\nmap is coloured by, one row per routed pad, in the same order as `layout`'s `pads`.\n\n\u2b50 **NO PROPRIETARY DECODER IS INVOLVED.** These come from MaxWell's spike table, which is\nwritten uncompressed by the on-chip detector at acquisition. So the chip map is **trustworthy on\nevery machine**, including all of the ones where the raw waveform decodes to a rail\n(`utils/knowledge/mea-recordings.md`). That is what makes this screen worth building before the\ndecoder problem is solved.\n\n\u26d4 **A COUNT, AND THE LEGEND SAYS SO.** No sorting, no bursts, no rates-per-neuron, no verdict.\nEach of those is a project on its own and none was asked for.\n\n\u26d4 **AND NOTHING HERE IS A JUDGEMENT ABOUT THE CHIP** (I1). `max_rate_hz` is the largest number\n*in this recording*, reported so the UI can scale its colours to the file in front of it. It is\nnot a maximum a chip is expected to reach, and there is no such number anywhere in Camea."
      },
      "MeaChipLayout": {
        "properties": {
          "analysis_id": {
            "type": "string",
            "title": "Analysis Id"
          },
          "recording_id": {
            "type": "string",
            "title": "Recording Id"
          },
          "label": {
            "type": "string",
            "title": "Label",
            "default": ""
          },
          "run_id": {
            "type": "string",
            "title": "Run Id",
            "default": ""
          },
          "assay": {
            "type": "string",
            "title": "Assay",
            "default": ""
          },
          "pads": {
            "items": {
              "$ref": "#/components/schemas/MeaChipPad"
            },
            "type": "array",
            "title": "Pads"
          },
          "stride": {
            "type": "integer",
            "title": "Stride",
            "description": "\u2b50 The array's long-axis length, **derived from this file's own numbering and then verified against every routed pad** (`derive_geometry`) \u2014 never a datasheet number. A file that does not satisfy the relation is refused rather than guessed at.",
            "default": 0
          },
          "pitch_um": {
            "type": "number",
            "title": "Pitch Um",
            "description": "Centre-to-centre pad spacing, derived the same way. \u26a0\ufe0f **Not** measured from the spacing of the routed pads: one of this project's recordings routed every OTHER pad, so the smallest routed gap is twice the truth.",
            "default": 0.0
          },
          "chip_cols": {
            "type": "integer",
            "title": "Chip Cols",
            "description": "The chip's full width in pads \u2014 always `stride`, which the file states and `derive_geometry` verifies against every routed pad.",
            "default": 0
          },
          "chip_rows": {
            "type": "integer",
            "title": "Chip Rows",
            "description": "\u2b50 The chip's full height in pads, so the map can draw **the whole chip** with the recorded pads in their real place on it (his answer, 2026-08-14). \u26a0\ufe0f Unlike the width, this is NOT in the file: a recording that routed a corner evidences only that corner. See `chip_extent` for which of the two answers this is.",
            "default": 0
          },
          "chip_extent": {
            "type": "string",
            "enum": [
              "device",
              "recorded"
            ],
            "title": "Chip Extent",
            "description": "\u2b50 **HOW `chip_rows` WAS ARRIVED AT, said out loud rather than assumed.** `device` = the file's own derived `stride` matches an axis of the one device Camea knows (`core.electrodegrid.MAXWELL`, R45.8's single place for a device number), so the other axis is the height. `recorded` = it does not, so the height is only as much as this file evidences and the outline is the recorded extent rather than the true chip. \u26d4 The device is CONSULTED, never assumed: a file whose stride disagrees is drawn as what it shows.",
            "default": "recorded"
          },
          "n_channels": {
            "type": "integer",
            "title": "N Channels",
            "default": 0
          },
          "n_samples": {
            "type": "integer",
            "title": "N Samples",
            "default": 0
          },
          "duration_s": {
            "type": "number",
            "title": "Duration S",
            "default": 0.0
          },
          "sampling_hz": {
            "type": "number",
            "title": "Sampling Hz",
            "default": 0.0
          },
          "n_spikes": {
            "type": "integer",
            "title": "N Spikes",
            "description": "Across the whole recording, every channel.",
            "default": 0
          }
        },
        "type": "object",
        "required": [
          "analysis_id",
          "recording_id"
        ],
        "title": "MeaChipLayout",
        "description": "`GET /api/mea/{analysis_id}/recordings/{recording_id}/layout` \u2014 the chip, as this file\ndescribes it: every pad that was actually recorded, at its real position.\n\n\u2b50 **ONLY THE ROUTED PADS ARE HERE, AND THAT IS THE HONEST SET.** A MaxOne routes ~1k channels\nof tens of thousands of pads, so *most of the chip has no data at all* \u2014 and a pad that was\nnever routed is not a measurement of silence, it is an absence of measurement. Drawing them\nwould invent data.\n\n\u26d4 Costs no proprietary decoder: the mapping table is plain HDF5."
      },
      "MeaChipPad": {
        "properties": {
          "channel": {
            "type": "integer",
            "title": "Channel",
            "description": "The amplifier channel this pad was routed to \u2014 what the trace route is asked for."
          },
          "electrode": {
            "type": "integer",
            "title": "Electrode",
            "description": "MaxWell's own electrode id, straight from the file."
          },
          "x_um": {
            "type": "number",
            "title": "X Um"
          },
          "y_um": {
            "type": "number",
            "title": "Y Um"
          }
        },
        "type": "object",
        "required": [
          "channel",
          "electrode",
          "x_um",
          "y_um"
        ],
        "title": "MeaChipPad",
        "description": "One routed pad, at the position the file itself states for it.\n\n\u26d4 **`electrode` IS MAXWELL'S OWN ID, NOT A CAMEA `col-row` GRID ID.** The mosaic pipeline's\n`col-row` ids exist because that pipeline has to *guess* how the chip was seated under a\nmicroscope; here the file says where every pad is, so the two must never be confused."
      },
      "MeaCopyResult": {
        "properties": {
          "kind": {
            "type": "string",
            "const": "mea_copy",
            "title": "Kind",
            "default": "mea_copy"
          },
          "analysis_id": {
            "type": "string",
            "title": "Analysis Id"
          },
          "recording_id": {
            "type": "string",
            "title": "Recording Id"
          },
          "stored_path": {
            "type": "string",
            "title": "Stored Path"
          },
          "bytes": {
            "type": "integer",
            "title": "Bytes",
            "default": 0
          }
        },
        "type": "object",
        "required": [
          "analysis_id",
          "recording_id",
          "stored_path"
        ],
        "title": "MeaCopyResult",
        "description": "`job.result` of a `mea_copy` job \u2014 one recording's copy has landed in the project."
      },
      "MeaEnvelopeResult": {
        "properties": {
          "kind": {
            "type": "string",
            "const": "mea_envelope",
            "title": "Kind",
            "default": "mea_envelope"
          },
          "analysis_id": {
            "type": "string",
            "title": "Analysis Id"
          },
          "recording_id": {
            "type": "string",
            "title": "Recording Id"
          },
          "built": {
            "type": "boolean",
            "title": "Built",
            "default": false
          },
          "n_buckets": {
            "type": "integer",
            "title": "N Buckets",
            "default": 0
          }
        },
        "type": "object",
        "required": [
          "analysis_id",
          "recording_id"
        ],
        "title": "MeaEnvelopeResult",
        "description": "`job.result` of a `mea_envelope` job \u2014 one recording has been read end to end and reduced.\n\n\u2b50 **WHY THIS IS A JOB.** MaxWell chunks the raw stream across every channel at once, so reading\none channel's full length decompresses all of them: measured 12-23 s for one channel against\n19-32 s for all of them, and 37-70 s once the exact per-channel health tally is included. Doing\nevery channel in one pass is therefore 1.4x the cost of doing one, and it is paid once per\nrecording rather than once per click.\n\n`built` is False \u2014 with no error \u2014 when the recording stores no continuous trace at all (an\nActivityScan). That is a fact about the assay, not a failure."
      },
      "MeaEnvelopeRow": {
        "properties": {
          "recording_id": {
            "type": "string",
            "title": "Recording Id"
          },
          "label": {
            "type": "string",
            "title": "Label",
            "default": ""
          },
          "ready": {
            "type": "boolean",
            "title": "Ready",
            "description": "\u2b50 True when the whole recording can be drawn at once. False means only the narrow windows the app can read live are available \u2014 never that the recording is broken."
          },
          "job_id": {
            "type": "string",
            "title": "Job Id",
            "description": "The build running for it, if one is.",
            "default": ""
          },
          "pct": {
            "type": "number",
            "title": "Pct",
            "description": "0-100 while a build is running.",
            "default": 0.0
          }
        },
        "type": "object",
        "required": [
          "recording_id",
          "ready"
        ],
        "title": "MeaEnvelopeRow",
        "description": "Whether one recording can be shown whole yet."
      },
      "MeaEnvelopeStatus": {
        "properties": {
          "analysis_id": {
            "type": "string",
            "title": "Analysis Id"
          },
          "recordings": {
            "items": {
              "$ref": "#/components/schemas/MeaEnvelopeRow"
            },
            "type": "array",
            "title": "Recordings"
          },
          "started": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Started",
            "description": "The jobs now reading a recording end to end as a result of this call \u2014 poll these, or poll `recordings[].ready`. Empty on a `GET`, and empty on a `POST` when everything was already built. \u26a0\ufe0f **Not strictly 'started by this call':** a `POST` issued while an earlier backfill is still running reports that earlier job's id rather than starting a duplicate, which is what a caller waiting on it actually wants."
          }
        },
        "type": "object",
        "required": [
          "analysis_id"
        ],
        "title": "MeaEnvelopeStatus",
        "description": "`GET`/`POST /api/mea/{analysis_id}/recordings/envelopes` \u2014 the state of the one-off read that\nlets the trace panel show a whole recording, and the way to start it for the ones that lack it.\n\n\u2b50 **THE BACKFILL EXISTS BECAUSE HE ASKED FOR IT BY NAME** (2026-08-15): *\"go ahead and run the\nloader on the MEAs I have already imported.\"* New recordings get this at import; everything\nalready in a project gets it from here."
      },
      "MeaFootprintPayload": {
        "properties": {
          "analysis_id": {
            "type": "string",
            "title": "Analysis Id"
          },
          "run_id": {
            "type": "string",
            "title": "Run Id"
          },
          "cols": {
            "type": "integer",
            "title": "Cols",
            "description": "The project's electrode grid, as mapped from the mosaic."
          },
          "rows": {
            "type": "integer",
            "title": "Rows"
          },
          "stride": {
            "type": "integer",
            "title": "Stride",
            "description": "The chip's long-axis length, derived from the recording itself (never a datasheet number)."
          },
          "n_routed": {
            "type": "integer",
            "title": "N Routed",
            "description": "How many electrodes the recording routed at all."
          },
          "orientation": {
            "$ref": "#/components/schemas/MeaOrientation",
            "description": "The seating the project currently holds \u2014 so the UI can mark which footprint is the saved one."
          },
          "seatings": {
            "items": {
              "$ref": "#/components/schemas/MeaFootprintSeating"
            },
            "type": "array",
            "title": "Seatings"
          }
        },
        "type": "object",
        "required": [
          "analysis_id",
          "run_id",
          "cols",
          "rows",
          "stride",
          "n_routed"
        ],
        "title": "MeaFootprintPayload",
        "description": "`GET /api/videomosaic/mea/footprint` \u2014 the four seatings' recorded-pad footprints.\n\n\u2b50 **GEOMETRY ONLY, CHEAP ON PURPOSE**: reads the recording's routed mapping and nothing else\n(no video, no spikes, no raw stream), so a UI can draw where each candidate seating would put\nthe recorded block the moment its panel opens. \u26d4 It ranks nothing and proposes nothing \u2014 the\nevidence lives in the orientation job."
      },
      "MeaFootprintRegion": {
        "properties": {
          "region_id": {
            "type": "string",
            "title": "Region Id"
          },
          "region_name": {
            "type": "string",
            "title": "Region Name",
            "default": ""
          },
          "n_region": {
            "type": "integer",
            "title": "N Region",
            "description": "How many pads the region covers at all."
          },
          "n_covered": {
            "type": "integer",
            "title": "N Covered",
            "description": "How many of them this seating maps onto RECORDED electrodes. The same coverage number the orientation test reports, computed without reading a frame or a spike."
          }
        },
        "type": "object",
        "required": [
          "region_id",
          "n_region",
          "n_covered"
        ],
        "title": "MeaFootprintRegion",
        "description": "How many of one located region's pads a seating actually covers \u2014 pure geometry."
      },
      "MeaFootprintSeating": {
        "properties": {
          "flip_x": {
            "type": "boolean",
            "title": "Flip X"
          },
          "flip_y": {
            "type": "boolean",
            "title": "Flip Y"
          },
          "electrodes": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Electrodes",
            "description": "The recorded pads as Camea \"col-row\" grid ids under this seating. Pads that fall outside the mapped grid are absent \u2014 they have no id in this mosaic."
          },
          "n_recorded": {
            "type": "integer",
            "title": "N Recorded",
            "description": "len(electrodes) \u2014 how many recorded pads land inside the mapped grid under this seating.",
            "default": 0
          },
          "regions": {
            "items": {
              "$ref": "#/components/schemas/MeaFootprintRegion"
            },
            "type": "array",
            "title": "Regions"
          }
        },
        "type": "object",
        "required": [
          "flip_x",
          "flip_y"
        ],
        "title": "MeaFootprintSeating",
        "description": "Where the recorded pads would sit in the mosaic under one candidate seating."
      },
      "MeaOrientation": {
        "properties": {
          "flip_x": {
            "type": "boolean",
            "title": "Flip X",
            "default": false
          },
          "flip_y": {
            "type": "boolean",
            "title": "Flip Y",
            "default": false
          },
          "confirmed": {
            "type": "boolean",
            "title": "Confirmed",
            "description": "False until something ESTABLISHED this seating. While false the UI must mark electrode identity provisional.",
            "default": false
          },
          "source": {
            "type": "string",
            "title": "Source",
            "description": "How it was established, for provenance.",
            "default": ""
          }
        },
        "type": "object",
        "title": "MeaOrientation",
        "description": "How the chip's own axes sit in the mosaic \u2014 the half no file records (see header)."
      },
      "MeaOrientationRequest": {
        "properties": {
          "analysis_id": {
            "type": "string",
            "title": "Analysis Id"
          },
          "region_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Region Id",
            "description": "Which located region to test against. Defaults to the first one the project has."
          },
          "run_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Run Id"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "analysis_id"
        ],
        "title": "MeaOrientationRequest"
      },
      "MeaPadActivity": {
        "properties": {
          "channel": {
            "type": "integer",
            "title": "Channel"
          },
          "n_spikes": {
            "type": "integer",
            "title": "N Spikes"
          },
          "rate_hz": {
            "type": "number",
            "title": "Rate Hz",
            "description": "Spikes per second of the recording. The unit the chip map's legend names."
          }
        },
        "type": "object",
        "required": [
          "channel",
          "n_spikes",
          "rate_hz"
        ],
        "title": "MeaPadActivity",
        "description": "How much happened on one routed pad. \u26d4 A count, and nothing more \u2014 see `MeaChipActivity`."
      },
      "MeaRecordingCandidate": {
        "properties": {
          "path": {
            "type": "string",
            "title": "Path"
          },
          "label": {
            "type": "string",
            "title": "Label",
            "description": "What a human calls it \u2014 `Network/000690`.",
            "default": ""
          },
          "run_id": {
            "type": "string",
            "title": "Run Id",
            "default": ""
          },
          "assay": {
            "type": "string",
            "title": "Assay",
            "default": ""
          },
          "bytes": {
            "type": "integer",
            "title": "Bytes",
            "default": 0
          },
          "duration_s": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Duration S"
          },
          "n_channels": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "N Channels"
          },
          "n_spikes": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "N Spikes"
          },
          "readable": {
            "type": "boolean",
            "title": "Readable",
            "description": "\u2b50 False = this file is named `data.raw.h5` but does not read as a MaxLab recording. It is still LISTED, greyed and un-tickable, with `problem` saying why \u2014 a file that silently vanished from the list would look like a folder Camea could not see into.",
            "default": true
          },
          "problem": {
            "type": "string",
            "title": "Problem",
            "default": ""
          }
        },
        "type": "object",
        "required": [
          "path"
        ],
        "title": "MeaRecordingCandidate",
        "description": "One row of the import tick-list \u2014 a `data.raw.h5` found under the folder he pointed at."
      },
      "MeaRecordingSummary": {
        "properties": {
          "run_id": {
            "type": "string",
            "title": "Run Id",
            "description": "The acquisition run, e.g. \"000690\"."
          },
          "assay": {
            "type": "string",
            "title": "Assay",
            "description": "The assay folder that holds it, e.g. \"Network\"."
          },
          "label": {
            "type": "string",
            "title": "Label",
            "description": "What a human calls it: \"Network/000690\"."
          },
          "path": {
            "type": "string",
            "title": "Path"
          },
          "n_channels": {
            "type": "integer",
            "title": "N Channels",
            "description": "Electrodes actually routed and recorded \u2014 NOT the chip's electrode count. The rest of the array has no trace at all."
          },
          "n_samples": {
            "type": "integer",
            "title": "N Samples"
          },
          "sampling_hz": {
            "type": "number",
            "title": "Sampling Hz"
          },
          "duration_s": {
            "type": "number",
            "title": "Duration S"
          },
          "lsb_uv": {
            "type": "number",
            "title": "Lsb Uv",
            "description": "Microvolts per stored count."
          },
          "gain": {
            "type": "number",
            "title": "Gain"
          },
          "hpf_hz": {
            "type": "number",
            "title": "Hpf Hz"
          },
          "n_spikes": {
            "type": "integer",
            "title": "N Spikes",
            "description": "Spikes MaxWell's on-chip detector wrote into the file. Needs no proprietary decoder, so this number is always trustworthy."
          }
        },
        "type": "object",
        "required": [
          "run_id",
          "assay",
          "label",
          "path",
          "n_channels",
          "n_samples",
          "sampling_hz",
          "duration_s",
          "lsb_uv",
          "gain",
          "hpf_hz",
          "n_spikes"
        ],
        "title": "MeaRecordingSummary",
        "description": "One `data.raw.h5` found for a project. Header facts only \u2014 no raw decode needed."
      },
      "MeaShelf": {
        "properties": {
          "analysis_id": {
            "type": "string",
            "title": "Analysis Id"
          },
          "recordings": {
            "items": {
              "$ref": "#/components/schemas/MeaShelfEntry"
            },
            "type": "array",
            "title": "Recordings"
          }
        },
        "type": "object",
        "required": [
          "analysis_id"
        ],
        "title": "MeaShelf",
        "description": "`GET|POST|DELETE /api/mea/{analysis_id}/recordings` \u2014 everything on one project's shelf."
      },
      "MeaShelfEntry": {
        "properties": {
          "id": {
            "type": "string",
            "title": "Id",
            "description": "Minted, stable, and never the path: he may add the same file twice, and a path is not an identity."
          },
          "label": {
            "type": "string",
            "title": "Label",
            "default": ""
          },
          "run_id": {
            "type": "string",
            "title": "Run Id",
            "default": ""
          },
          "assay": {
            "type": "string",
            "title": "Assay",
            "default": ""
          },
          "source_path": {
            "type": "string",
            "title": "Source Path",
            "description": "Where it came from. \u26d4 A path, not dataset knowledge \u2014 same standing as the manifest's `data_dir`. Read-only, always."
          },
          "stored_path": {
            "type": "string",
            "title": "Stored Path",
            "description": "The project's own copy, project-relative (`recordings/<id>/data.raw.h5`). Blank until the copy lands.",
            "default": ""
          },
          "copy_state": {
            "type": "string",
            "enum": [
              "referenced",
              "copying",
              "stored",
              "failed"
            ],
            "title": "Copy State",
            "description": "\u2b50 Where the app is reading this recording from RIGHT NOW. His answer,2026-08-14: *'reference it until the copy is finished'* \u2014 usable the instant it is added, read from where it sits, with a copy pulled in behind it. \u26a0\ufe0f **Derived from the disk and the job registry, not just from the document**, so a copy interrupted by a restart reports as `referenced` (which it is) instead of as a `copying` that will never finish."
          },
          "copy_pct": {
            "type": "number",
            "title": "Copy Pct",
            "description": "0\u2013100 while `copying`.",
            "default": 0.0
          },
          "copy_error": {
            "type": "string",
            "title": "Copy Error",
            "description": "Why the copy failed. The recording still works \u2014 it is read from the original \u2014 and this says what did not.",
            "default": ""
          },
          "added": {
            "type": "string",
            "title": "Added",
            "default": ""
          },
          "bytes": {
            "type": "integer",
            "title": "Bytes",
            "default": 0
          },
          "missing": {
            "type": "boolean",
            "title": "Missing",
            "description": "\u26d4 **There is no file to read at either address** \u2014 his original is not where he left it and no copy has landed. Said on the shelf as a live warning; the row refuses to open rather than showing empty numbers. Every field below is null in this state.",
            "default": false
          },
          "source_present": {
            "type": "boolean",
            "title": "Source Present",
            "description": "\u2b50 **Is HIS OWN file at `source_path` right now?** Stat'ed on every read, never cached from import time, because a drive that was unplugged can come back. \u26a0\ufe0f It exists for exactly one decision: `stored` **and not** `source_present` is the one case where removing a recording destroys the last copy of it, and the only case that asks him first (his ruling, 2026-08-14). \u26d4 Do not read it as `missing` \u2014 a `referenced` recording with no source has no copy either, so removing THAT destroys nothing.",
            "default": true
          },
          "duration_s": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Duration S"
          },
          "n_channels": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "N Channels"
          },
          "n_samples": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "N Samples"
          },
          "n_spikes": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "N Spikes"
          }
        },
        "type": "object",
        "required": [
          "id",
          "source_path",
          "copy_state"
        ],
        "title": "MeaShelfEntry",
        "description": "One recording a project holds \u2014 the row on the shelf."
      },
      "MeaSpike": {
        "properties": {
          "t_s": {
            "type": "number",
            "title": "T S"
          },
          "amplitude_uv": {
            "type": "number",
            "title": "Amplitude Uv"
          }
        },
        "type": "object",
        "required": [
          "t_s",
          "amplitude_uv"
        ],
        "title": "MeaSpike"
      },
      "MeaSyncEpisode": {
        "properties": {
          "start_s": {
            "type": "number",
            "title": "Start S"
          },
          "end_s": {
            "type": "number",
            "title": "End S"
          },
          "duration_s": {
            "type": "number",
            "title": "Duration S"
          },
          "peak_channels": {
            "type": "integer",
            "title": "Peak Channels"
          }
        },
        "type": "object",
        "required": [
          "start_s",
          "end_s",
          "duration_s",
          "peak_channels"
        ],
        "title": "MeaSyncEpisode",
        "description": "A stretch where hundreds of channels left the rail together \u2014 a 2P lamp artefact.\n\n\u2b50 Deliberate, not noise: the experimenters switched the lamp to stamp the MEA record with\nevents also visible optically, so the two clocks can be aligned. Render them, never remove them."
      },
      "MigratedProject": {
        "properties": {
          "name": {
            "type": "string",
            "title": "Name"
          },
          "from": {
            "type": "string",
            "title": "From",
            "description": "The folder the user had saved it into."
          },
          "to": {
            "type": "string",
            "title": "To",
            "description": "Where it is now, inside Camea's store."
          }
        },
        "type": "object",
        "required": [
          "name",
          "from",
          "to"
        ],
        "title": "MigratedProject",
        "description": "One project that came home to the store."
      },
      "MigrationReport": {
        "properties": {
          "migrated": {
            "items": {
              "$ref": "#/components/schemas/MigratedProject"
            },
            "type": "array",
            "title": "Migrated"
          },
          "failed": {
            "items": {
              "$ref": "#/components/schemas/FailedMigration"
            },
            "type": "array",
            "title": "Failed"
          }
        },
        "type": "object",
        "title": "MigrationReport",
        "description": "`core.migrate`'s account of the one-time move into the store (R44).\n\n\u26a0\ufe0f Reported, never silent. A project that moved without saying so would be indistinguishable\nfrom one that vanished, and these are folders the user chose and can still find in Explorer."
      },
      "MosaicDocument": {
        "properties": {
          "schema_version": {
            "type": "string",
            "title": "Schema Version"
          },
          "app": {
            "$ref": "#/components/schemas/AppStamp"
          },
          "id": {
            "type": "string",
            "title": "Id"
          },
          "feature": {
            "type": "string",
            "title": "Feature"
          },
          "dataset": {
            "type": "string",
            "title": "Dataset"
          },
          "experiment": {
            "type": "string",
            "title": "Experiment",
            "default": ""
          },
          "data_dir": {
            "type": "string",
            "title": "Data Dir"
          },
          "dataset_key": {
            "type": "string",
            "title": "Dataset Key"
          },
          "created": {
            "type": "string",
            "title": "Created"
          },
          "modified": {
            "type": "string",
            "title": "Modified"
          },
          "provenance": {
            "$ref": "#/components/schemas/Provenance"
          },
          "tiles": {
            "additionalProperties": {
              "$ref": "#/components/schemas/TileRecord"
            },
            "type": "object",
            "title": "Tiles"
          },
          "trial_range": {
            "prefixItems": [
              {
                "type": "integer"
              },
              {
                "type": "integer"
              }
            ],
            "type": "array",
            "maxItems": 2,
            "minItems": 2,
            "title": "Trial Range"
          },
          "pass_split": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Pass Split"
          },
          "origin_trial": {
            "type": "integer",
            "title": "Origin Trial"
          },
          "tile_px": {
            "type": "integer",
            "title": "Tile Px",
            "default": 512
          },
          "coordinates": {
            "type": "string",
            "title": "Coordinates",
            "description": "\u26a0\ufe0f Built from `SessionResponse.frame_note` \u2014 i.e. from THIS acquisition's XML. Never the unconditional literal \"180deg-flipped\" the v1 constant carried."
          },
          "tolerance_px": {
            "$ref": "#/components/schemas/TolerancePx"
          },
          "gaps": {
            "items": {
              "prefixItems": [
                {
                  "type": "integer"
                },
                {
                  "type": "integer"
                }
              ],
              "type": "array",
              "maxItems": 2,
              "minItems": 2
            },
            "type": "array",
            "title": "Gaps",
            "description": "\u26a0\ufe0f **DERIVED. Recomputed on EVERY change to the excluded set.** Trial numbers are acquisition ORDER but are NOT contiguous once anything is excluded, and a 'consecutive' pair across a gap is a multi-step jump where the serpentine one-axis step prior does NOT hold. Miss this and the next solve is silently poisoned."
          },
          "unusable_tiles": {
            "items": {
              "type": "integer"
            },
            "type": "array",
            "title": "Unusable Tiles"
          },
          "cursor": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Cursor",
            "description": "The sweep position, saved so a load resumes there. \ud83d\udd34 An **integer**, never null-on-Escape: `Esc` deselects, it does not abandon the tile you are judging."
          },
          "blank_scan": {
            "$ref": "#/components/schemas/BlankScanBlock"
          },
          "run": {
            "$ref": "#/components/schemas/RunBlock"
          },
          "build": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/BuildBlock"
              },
              {
                "type": "null"
              }
            ]
          },
          "tone": {
            "$ref": "#/components/schemas/ToneSnapshot"
          },
          "electrodes": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/ElectrodeMapBlock"
              },
              {
                "type": "null"
              }
            ],
            "description": "The fitted electrode-grid summary (2026-08-11); the full per-electrode table is an outputs/ file it names. None until Map electrodes runs."
          }
        },
        "additionalProperties": true,
        "type": "object",
        "required": [
          "schema_version",
          "app",
          "id",
          "feature",
          "dataset",
          "data_dir",
          "dataset_key",
          "created",
          "modified",
          "provenance",
          "tiles",
          "trial_range",
          "origin_trial",
          "coordinates",
          "tolerance_px"
        ],
        "title": "MosaicDocument",
        "description": "\u2b50 **THE MOSAIC PAYLOAD, FLAT, BESIDE THE ENVELOPE.** (See the `Document` note: nesting it\nunder `payload` would make it unreadable to the scorer, and a project the scorer cannot read is\na project that cannot be checked.)\n\n\u26d4 **NOTHING STARTS `excluded`.** Every trial in the run starts `unplaced`. A frame becomes\n`excluded` only when the human presses `E`, or when a project file he loaded says so."
      },
      "OkResponse": {
        "properties": {
          "ok": {
            "type": "boolean",
            "title": "Ok",
            "default": true
          }
        },
        "type": "object",
        "title": "OkResponse"
      },
      "OpenJobResult": {
        "properties": {
          "kind": {
            "type": "string",
            "const": "open",
            "title": "Kind",
            "default": "open"
          },
          "session": {
            "$ref": "#/components/schemas/SessionResponse"
          }
        },
        "type": "object",
        "required": [
          "session"
        ],
        "title": "OpenJobResult",
        "description": "`Job.result` when `kind == \"open\"`."
      },
      "OpenSessionRequest": {
        "properties": {
          "path": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Path"
          },
          "dataset_key": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Dataset Key"
          },
          "trials": {
            "anyOf": [
              {
                "items": {
                  "type": "integer"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "title": "Trials"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "title": "OpenSessionRequest",
        "description": "\u26a0\ufe0f `trials` is the caller's selection. A FrameStore holds one shape.\n\n* `trials: null` \u21d2 every snapshot trial. If they are **not** all one shape the open fails with\n  `409 mixed_shape`, listing the groups \u2014 core **refuses to guess** which shape you meant.\n* `trials: [...]` \u21d2 exactly those. The mosaic feature passes its 512\u00d7512 selection here.\n\n\u26d4 Nothing is ever dropped by TRIAL NUMBER. Only by shape, and only with a reason, reported in\n`SessionResponse.skipped`."
      },
      "OrientationRegionScore": {
        "properties": {
          "region_id": {
            "type": "string",
            "title": "Region Id"
          },
          "region_name": {
            "type": "string",
            "title": "Region Name",
            "default": ""
          },
          "n_region": {
            "type": "integer",
            "title": "N Region",
            "description": "How many pads this region covers.",
            "default": 0
          },
          "offset_s": {
            "type": "number",
            "title": "Offset S",
            "description": "Clock shift applied for THIS region's video, MEA onto video. Each region's recording has its own clock, so each gets its own alignment.",
            "default": 0.0
          },
          "alignment_quality": {
            "type": "number",
            "title": "Alignment Quality",
            "description": "Jaccard overlap of the mark trains at that offset, for this region. Same sceptical reading as the top-level number.",
            "default": 0.0
          },
          "seatings": {
            "items": {
              "$ref": "#/components/schemas/OrientationRegionSeating"
            },
            "type": "array",
            "title": "Seatings"
          }
        },
        "type": "object",
        "required": [
          "region_id"
        ],
        "title": "OrientationRegionScore",
        "description": "One located region's own evidence inside the orientation test.\n\n\u2b50 Regions are combined for the verdict, but each is also reported alone: a seating that two\nindependent fields both point at is different evidence from one field shouting and one silent,\nand only the breakdown can show which happened."
      },
      "OrientationRegionSeating": {
        "properties": {
          "flip_x": {
            "type": "boolean",
            "title": "Flip X"
          },
          "flip_y": {
            "type": "boolean",
            "title": "Flip Y"
          },
          "n_recorded": {
            "type": "integer",
            "title": "N Recorded",
            "description": "Of THIS region's pads, how many this seating maps onto recorded electrodes."
          },
          "correlation": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Correlation",
            "description": "This region's own spike-rate vs calcium correlation under this seating. null = untestable here (no recorded pad under this field)."
          }
        },
        "type": "object",
        "required": [
          "flip_x",
          "flip_y",
          "n_recorded"
        ],
        "title": "OrientationRegionSeating",
        "description": "One seating's numbers against ONE region \u2014 a row of the per-region breakdown."
      },
      "OrientationTestResult": {
        "properties": {
          "kind": {
            "type": "string",
            "const": "mea_orientation",
            "title": "Kind",
            "default": "mea_orientation"
          },
          "analysis_id": {
            "type": "string",
            "title": "Analysis Id"
          },
          "run_id": {
            "type": "string",
            "title": "Run Id"
          },
          "region_id": {
            "type": "string",
            "title": "Region Id",
            "description": "The region tested, when ONE was \u2014 its old meaning. Blank when several regions took part; `regions` carries each of them."
          },
          "region_name": {
            "type": "string",
            "title": "Region Name",
            "default": ""
          },
          "scores": {
            "items": {
              "$ref": "#/components/schemas/SeatingScorePayload"
            },
            "type": "array",
            "title": "Scores"
          },
          "regions": {
            "items": {
              "$ref": "#/components/schemas/OrientationRegionScore"
            },
            "type": "array",
            "title": "Regions",
            "description": "\u2b50 The per-region breakdown behind `scores` \u2014 every region that was scored, each with its own clock alignment and its own four seating rows. Additive: a one-region run has a one-entry list."
          },
          "best": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/MeaOrientation"
              },
              {
                "type": "null"
              }
            ],
            "description": "The top-ranked seating \u2014 a PROPOSAL, and only when `decisive`. Never applied by the job, and null when the seatings could not be told apart."
          },
          "decisive": {
            "type": "boolean",
            "title": "Decisive",
            "description": "\u2b50 Whether the data actually separated the seatings. False means the UI must say **cannot tell** and offer no winner \u2014 crowning the top of four near-identical numbers would manufacture an answer out of noise.",
            "default": false
          },
          "decided_by": {
            "type": "string",
            "title": "Decided By",
            "description": "What separated them: 'coverage' (only one seating puts any recorded electrode under the field \u2014 a geometric fact, independent of the decoder and the clock, so the strongest evidence available here), 'correlation', or '' when nothing did.",
            "default": ""
          },
          "margin": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Margin",
            "description": "Top correlation minus runner-up, among testable seatings. Null when fewer than two were testable."
          },
          "chance_level": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Chance Level",
            "description": "\u2b50 The empirical chance bar: the 95th percentile of the |combined correlation| the WINNING seating's own series produce when re-scored at ~200 deliberately-wrong clock offsets (circular shifts, deterministic seed \u2014 the same number on every re-run). What luck alone can reach on this data. Null when nothing was scorable."
          },
          "beats_chance": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Beats Chance",
            "description": "Whether the winning correlation clears `chance_level`. \u26d4 `decided_by: 'correlation'` requires BOTH the margin AND this \u2014 a correlation a wrong clock could produce never wins, however clear its margin over the runner-up. Null when not computed."
          },
          "offset_s": {
            "type": "number",
            "title": "Offset S",
            "description": "Clock shift applied, MEA onto video (the first region's, when several took part \u2014 see `regions`).",
            "default": 0.0
          },
          "alignment_quality": {
            "type": "number",
            "title": "Alignment Quality",
            "description": "Jaccard overlap of the two mark trains at that offset. \u26a0\ufe0f Read it sceptically: two mostly-on signals overlap substantially at ANY shift. On this project's data the best was 0.65 between signals of 60% and 80% duty \u2014 i.e. chance.",
            "default": 0.0
          },
          "alignment_source": {
            "type": "string",
            "title": "Alignment Source",
            "description": "\u2b50 What the clock alignment actually rested on: 'ttl' = the digital time-stamp the rig itself wrote (the file's top-level `bits` \u2014 sample-accurate, no decoder involved, preferred whenever present), 'lamp' = the 2P-lamp episodes found in the electrical trace (the distrusted signal of issue 003), 'none' = the clocks were not aligned at all. The caveat is computed from this and must be shown with the result.",
            "default": ""
          },
          "caveat": {
            "type": "string",
            "title": "Caveat",
            "description": "Why this result cannot be taken at face value \u2014 computed from `alignment_source`, one wording per grounds. The UI shows it verbatim, never behind a `?`.",
            "default": ""
          }
        },
        "type": "object",
        "required": [
          "analysis_id",
          "run_id",
          "region_id"
        ],
        "title": "OrientationTestResult",
        "description": "`job.result` of an `mea_orientation` job \u2014 the four seatings, ranked.\n\n\ud83d\udd34 **UNVALIDATED, AND THE UI MUST SAY SO** (issue 003). The clock alignment rests on the 2P-lamp\nmarks, and those did not survive checking against the calcium video: 70 MEA episodes against 5\nvideo dark stretches, no consistent offset, and a confound where the \"episodes\" may be where the\nbroken decoder emitted anything rather than where the lamp fired. He asked for it built anyway\n(2026-08-13). It ranks; it never confirms. `Orientation.confirmed` is set by a human only."
      },
      "OutputEntry": {
        "properties": {
          "name": {
            "type": "string",
            "title": "Name",
            "description": "Its filename. \u26d4 A name, never a path \u2014 there are no subfolders."
          },
          "bytes": {
            "type": "integer",
            "title": "Bytes"
          },
          "modified": {
            "type": "string",
            "title": "Modified"
          },
          "media_type": {
            "type": "string",
            "title": "Media Type",
            "description": "Guessed from the extension. What GET .../outputs/{name} serves."
          },
          "previewable": {
            "type": "boolean",
            "title": "Previewable",
            "description": "True for images the browser can show inline.",
            "default": false
          }
        },
        "type": "object",
        "required": [
          "name",
          "bytes",
          "modified",
          "media_type"
        ],
        "title": "OutputEntry",
        "description": "One file in a project's `outputs/`."
      },
      "OutputListResponse": {
        "properties": {
          "outputs": {
            "items": {
              "$ref": "#/components/schemas/OutputEntry"
            },
            "type": "array",
            "title": "Outputs"
          }
        },
        "type": "object",
        "title": "OutputListResponse",
        "description": "`GET /api/projects/{id}/outputs` \u2014 everything this project has built, newest first."
      },
      "PassSplit": {
        "properties": {
          "value": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Value"
          },
          "detected": {
            "type": "boolean",
            "title": "Detected"
          },
          "why": {
            "type": "string",
            "title": "Why"
          },
          "gap_s": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Gap S"
          },
          "median_gap_s": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Median Gap S"
          },
          "runner_up": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/RunnerUpSplit"
              },
              {
                "type": "null"
              }
            ]
          },
          "n_pass1": {
            "type": "integer",
            "title": "N Pass1",
            "default": 0
          },
          "n_pass2": {
            "type": "integer",
            "title": "N Pass2",
            "default": 0
          }
        },
        "type": "object",
        "required": [
          "value",
          "detected",
          "why"
        ],
        "title": "PassSplit",
        "description": "\u2b50 `value` is the **LAST TRIAL OF PASS 1** (166 on 260620d), never 167.\n\n\u26a0\ufe0f Keep the `MIN_SIDE_FRAC = 0.20` guard: without it a naive argmax returns **11**, because\n11\u219212 is *also* 20.0 s (settling right after `Settings loaded`) and ties the true boundary."
      },
      "PerTileEvidence": {
        "properties": {
          "anchor_ncc": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Anchor Ncc",
            "description": "PASS 2 ONLY. The best signal there is."
          },
          "anchor_residual_px": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Anchor Residual Px",
            "description": "Probably THE number to sort a worklist on (it caught a tile at 2,706 px) \u2014 but it has fired exactly once and its false-positive rate is unmeasured. 'Go look', never a verdict."
          },
          "run": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Run"
          },
          "run_margin": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Run Margin"
          },
          "pass": {
            "type": "integer",
            "title": "Pass"
          }
        },
        "type": "object",
        "required": [
          "pass"
        ],
        "title": "PerTileEvidence",
        "description": "**A \"go look here first\" list, NOT a verdict.**\n\n\ud83d\udd34 **PASS-1 TILES HAVE NO PER-TILE CONFIDENCE AT ALL** \u2014 t27's info is aggregate-only, and the\nworst tile in the shipped 312/312 build (trial 127, at 9.94 px) **is a pass-1 tile**. Say so,\nloudly, on the screen: **the absence of a warning is not a clean bill of health.**\n\n\u26d4 Do **not** rebuild this on `quality.score_positions`: on the ground-truth-perfect 312/312\nbuild it flags 11 tiles and **all 11 are false positives (precision 0/11)**. And\n`quality.score_build()` / `quality.leaderboard()` are **dead** \u2014 they raise."
      },
      "Provenance": {
        "properties": {
          "authored_by": {
            "type": "string",
            "title": "Authored By"
          },
          "app_version": {
            "type": "string",
            "title": "App Version"
          },
          "workflow": {
            "type": "string",
            "title": "Workflow"
          },
          "seeded_from": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/SeededFrom"
              },
              {
                "type": "null"
              }
            ]
          },
          "independent_of_method": {
            "type": "boolean",
            "title": "Independent Of Method"
          },
          "warning": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Warning"
          },
          "human_edits": {
            "$ref": "#/components/schemas/HumanEdits"
          },
          "scale": {
            "$ref": "#/components/schemas/Scale"
          }
        },
        "additionalProperties": true,
        "type": "object",
        "required": [
          "authored_by",
          "app_version",
          "workflow",
          "independent_of_method"
        ],
        "title": "Provenance",
        "description": "\u26a0\ufe0f **NOT DECORATION.**\n\n`warning` is present **iff** `independent_of_method` is false, and it is the\n`PROVENANCE_WARNING` constant **verbatim, never paraphrased**. The verdict is derived from the\ndocument's **HISTORY** \u2014 a `build` block, or a single tile still carrying a `machine` position \u2014\nand **never** from what the document says about itself, because `seeded_from` is writable and\n\"Skip \u2014 place from scratch\" once erased it while every tile kept t33's answer.\n\n\ud83d\udd34 **This project has already destroyed one benchmark exactly this way**\n(`analysis/archive/challenge_2026-07/benchmark/ground_truth/260620d.json` **is T27's own\noutput**), and pass 1's ground truth got tiles 128/129/130/148 wrong for the same reason."
      },
      "QcDenominator": {
        "properties": {
          "trials_in_document": {
            "type": "integer",
            "title": "Trials In Document"
          },
          "not_excluded": {
            "type": "integer",
            "title": "Not Excluded"
          },
          "excluded": {
            "type": "integer",
            "title": "Excluded"
          }
        },
        "additionalProperties": true,
        "type": "object",
        "required": [
          "trials_in_document",
          "not_excluded",
          "excluded"
        ],
        "title": "QcDenominator",
        "description": "\u2b50 **EVERY NUMBER STATES ITS DENOMINATOR.** A percentage without one is how the 182-vs-156\nconfusion started. The denominator is the document's own `excluded` set, and nothing else \u2014 the\napp has no list of its own.\n\n\u26a0\ufe0f **`not_excluded` was called `usable_trials` in v1's `qc.json`, and it is RENAMED here on\npurpose.** The name collided, character for character, with `excluded.usable_trials()` \u2014 the one\nsymbol the standing ruling forbids the app to import. A key in a report is not a function, but\nthe whole point of that ruling is that nobody should ever have to make that distinction. The\nnumber is unchanged: **every tile the human did not exclude.** (Nothing machine-readable depends\non the old name: `score.py` reads `gt.json` and `positions.csv`, never `qc.json`.)"
      },
      "QcMovedRow": {
        "properties": {
          "trial": {
            "type": "integer",
            "title": "Trial"
          },
          "moved_px": {
            "type": "number",
            "title": "Moved Px"
          },
          "from": {
            "prefixItems": [
              {
                "type": "number"
              },
              {
                "type": "number"
              }
            ],
            "type": "array",
            "maxItems": 2,
            "minItems": 2,
            "title": "From"
          },
          "to": {
            "prefixItems": [
              {
                "type": "number"
              },
              {
                "type": "number"
              }
            ],
            "type": "array",
            "maxItems": 2,
            "minItems": 2,
            "title": "To"
          },
          "state": {
            "type": "string",
            "enum": [
              "unplaced",
              "unverified",
              "anchored",
              "excluded"
            ],
            "title": "State"
          },
          "ncc": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Ncc"
          },
          "margin": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Margin"
          },
          "n_anchors": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "N Anchors"
          },
          "note": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Note"
          }
        },
        "additionalProperties": true,
        "type": "object",
        "required": [
          "trial",
          "moved_px",
          "from",
          "to",
          "state"
        ],
        "title": "QcMovedRow"
      },
      "QcReport": {
        "properties": {
          "dataset": {
            "type": "string",
            "title": "Dataset"
          },
          "trial_range": {
            "prefixItems": [
              {
                "type": "integer"
              },
              {
                "type": "integer"
              }
            ],
            "type": "array",
            "maxItems": 2,
            "minItems": 2,
            "title": "Trial Range"
          },
          "pass_split": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Pass Split"
          },
          "generated": {
            "type": "string",
            "title": "Generated"
          },
          "app": {
            "$ref": "#/components/schemas/AppStamp"
          },
          "denominator": {
            "$ref": "#/components/schemas/QcDenominator"
          },
          "by_state": {
            "additionalProperties": {
              "items": {
                "type": "integer"
              },
              "type": "array"
            },
            "propertyNames": {
              "enum": [
                "unplaced",
                "unverified",
                "anchored",
                "excluded"
              ]
            },
            "type": "object",
            "title": "By State"
          },
          "moved": {
            "items": {
              "$ref": "#/components/schemas/QcMovedRow"
            },
            "type": "array",
            "title": "Moved"
          },
          "thin_margin": {
            "items": {
              "type": "integer"
            },
            "type": "array",
            "title": "Thin Margin"
          },
          "rescued": {
            "items": {
              "type": "integer"
            },
            "type": "array",
            "title": "Rescued"
          },
          "build_stale": {
            "type": "boolean",
            "title": "Build Stale"
          },
          "build_stale_reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Build Stale Reason"
          },
          "provenance": {
            "$ref": "#/components/schemas/Provenance"
          }
        },
        "additionalProperties": true,
        "type": "object",
        "required": [
          "dataset",
          "trial_range",
          "pass_split",
          "generated",
          "app",
          "denominator",
          "by_state",
          "moved",
          "thin_margin",
          "rescued",
          "build_stale",
          "provenance"
        ],
        "title": "QcReport",
        "description": "What the human did vs what the machine said. This report **is** the honest record of the\nanchoring hazard, and it is what makes the output safe to hand to somebody else."
      },
      "QcRequest": {
        "properties": {
          "doc": {
            "$ref": "#/components/schemas/MosaicDocument"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "doc"
        ],
        "title": "QcRequest"
      },
      "RecheckRequest": {
        "properties": {
          "session_id": {
            "type": "string",
            "title": "Session Id"
          },
          "doc": {
            "$ref": "#/components/schemas/MosaicDocument"
          },
          "trials": {
            "anyOf": [
              {
                "items": {
                  "type": "integer"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "title": "Trials",
            "description": "null = every tile flagged stale."
          },
          "tolerance_px": {
            "type": "number",
            "title": "Tolerance Px",
            "default": 5.0
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "session_id",
          "doc"
        ],
        "title": "RecheckRequest",
        "description": "`POST /api/mosaic/recheck` \u2192 202 `JobRef` (it is N \u00d7 ~1 s \u2014 it never blocks).\n\n\ud83d\udd34 **THE RE-CHECK IS GLOBAL, AND IT IS ALLOWED TO SAY NO.** It **never moves a tile** \u2014 it only\nmeasures it. Anything that disagrees with where the tile sits by more than `tolerance_px`\n**STAYS FLAGGED**.\n\nv1 fired a *local* match (\u00b164 px) and then cleared `stale` unconditionally. But a tile knocked\noff by a moved anchor is off by **hundreds** of px \u2014 failure on this data is **binary**,\nsub-pixel-right or wildly wrong \u2014 so a \u00b164 px window is *structurally blind* to the one error the\npanel exists for. Measured: a tile **380 px** from the truth re-checked locally to `ncc \u22120.0678`,\nwas not moved, and had its flag **cleared** \u2014 the loud panel disappeared and the tile stayed 380\npx wrong, with a negative NCC recorded in the document as its evidence."
      },
      "RecheckResult": {
        "properties": {
          "kind": {
            "type": "string",
            "const": "recheck",
            "title": "Kind",
            "default": "recheck"
          },
          "n_checked": {
            "type": "integer",
            "title": "N Checked"
          },
          "cleared": {
            "items": {
              "type": "integer"
            },
            "type": "array",
            "title": "Cleared"
          },
          "disagree": {
            "items": {
              "$ref": "#/components/schemas/RecheckRow"
            },
            "type": "array",
            "title": "Disagree",
            "description": "**Go and look.** These are still wrong."
          }
        },
        "type": "object",
        "required": [
          "n_checked",
          "cleared",
          "disagree"
        ],
        "title": "RecheckResult",
        "description": "`Job.result` when `kind == \"recheck\"`. \u26a0\ufe0f It returns **rows, not a document** \u2014 the re-check\ndoes not move anything; the front end clears the flags it is told it may clear."
      },
      "RecheckRow": {
        "properties": {
          "trial": {
            "type": "integer",
            "title": "Trial"
          },
          "at": {
            "prefixItems": [
              {
                "type": "number"
              },
              {
                "type": "number"
              }
            ],
            "type": "array",
            "maxItems": 2,
            "minItems": 2,
            "title": "At"
          },
          "best": {
            "anyOf": [
              {
                "prefixItems": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "number"
                  }
                ],
                "type": "array",
                "maxItems": 2,
                "minItems": 2
              },
              {
                "type": "null"
              }
            ],
            "title": "Best"
          },
          "disagree_px": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Disagree Px"
          },
          "ncc": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Ncc"
          },
          "margin": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Margin"
          },
          "still_stale": {
            "type": "boolean",
            "title": "Still Stale"
          }
        },
        "type": "object",
        "required": [
          "trial",
          "at",
          "best",
          "disagree_px",
          "ncc",
          "margin",
          "still_stale"
        ],
        "title": "RecheckRow"
      },
      "RecomputeRequest": {
        "properties": {
          "session_id": {
            "type": "string",
            "title": "Session Id"
          },
          "doc": {
            "$ref": "#/components/schemas/MosaicDocument"
          },
          "trials": {
            "anyOf": [
              {
                "items": {
                  "type": "integer"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "title": "Trials",
            "description": "null = every non-anchored, non-human, non-excluded, loaded tile."
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "session_id",
          "doc"
        ],
        "title": "RecomputeRequest",
        "description": "`POST /api/mosaic/recompute` \u2192 202 `JobRef`. \u2b50 **The tool the user reaches for.** Freeze the\ntiles he has anchored and re-place every other tile against their combined composite \u2014 then he\nanchors more and recomputes again.\n\nIt is `recheck`'s per-target `match_anchor(global)` loop, but it **writes** the answer instead of\nonly measuring it: every placed tile lands `unverified` + `machine` (never `anchored` \u2014 I3), and\nan `anchored`, `human`, or `excluded` tile is never touched (the frozen reference / his own work).\nA target with no measurable overlap against the anchors is **left untouched** \u2014 anchor a neighbour\nand recompute again."
      },
      "RecomputeResult": {
        "properties": {
          "kind": {
            "type": "string",
            "const": "recompute",
            "title": "Kind",
            "default": "recompute"
          },
          "doc": {
            "$ref": "#/components/schemas/MosaicDocument"
          },
          "n_placed": {
            "type": "integer",
            "title": "N Placed",
            "description": "tiles re-placed against the anchor composite."
          },
          "n_unmeasurable": {
            "type": "integer",
            "title": "N Unmeasurable",
            "description": "targets with no measurable overlap with the anchors \u2014 left untouched."
          },
          "n_reference": {
            "type": "integer",
            "title": "N Reference",
            "description": "anchored tiles used as the frozen ground truth."
          }
        },
        "type": "object",
        "required": [
          "doc",
          "n_placed",
          "n_unmeasurable",
          "n_reference"
        ],
        "title": "RecomputeResult",
        "description": "`Job.result` when `kind == \"recompute\"`. \u26a0\ufe0f Unlike `recheck`, it **returns the DOCUMENT** \u2014 the\nfront end adopts it exactly as it adopts a `/seed` result (and re-hydrates the sweep)."
      },
      "Refusal": {
        "properties": {
          "reason": {
            "type": "string",
            "enum": [
              "blank",
              "no_anchors"
            ],
            "title": "Reason"
          },
          "trials": {
            "items": {
              "type": "integer"
            },
            "type": "array",
            "title": "Trials"
          },
          "texture": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Texture"
          },
          "threshold": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Threshold"
          },
          "message": {
            "type": "string",
            "title": "Message"
          }
        },
        "type": "object",
        "required": [
          "reason",
          "trials",
          "message"
        ],
        "title": "Refusal",
        "description": "\u26d4 **BLANK FRAMES ARE REFUSED, NOT SCORED. There is no `force` flag and there will not be\none.** Two blank frames 136 trials apart correlate **+0.43 at zero shift** (honest noise floor\n0.115) because what they share is *fixed-pattern sensor structure*, which does not move with the\nstage. **They register confidently and wrongly.**\n\n* **`target` is blank** \u2192 REFUSE. `candidates: []`, `best: null`. This is the real hazard.\n* **an `anchor` is blank** \u2192 **DROPPED from the composite** (`MatchResult.dropped_anchors`).\n  **Not fatal.** \ud83d\udd34 The rule that made a blank anchor an error dead-ended the app: the moment the\n  user anchored the first near-threshold trial, every subsequent `Space` and snap refused\n  **forever** and the sweep died at the next tile.\n* **every anchor is blank** \u2192 `reason: \"no_anchors\"`.\n\n**A human may still place a blank tile by hand** \u2014 a human eye is allowed to do what the\ncorrelator must not. It gets **no NCC**, because there is no honest one."
      },
      "RegionCandidate": {
        "properties": {
          "rank": {
            "type": "integer",
            "title": "Rank"
          },
          "x": {
            "type": "number",
            "title": "X"
          },
          "y": {
            "type": "number",
            "title": "Y"
          },
          "ncc": {
            "type": "number",
            "title": "Ncc"
          },
          "npix": {
            "type": "integer",
            "title": "Npix"
          },
          "subpixel": {
            "type": "boolean",
            "title": "Subpixel",
            "default": false
          }
        },
        "type": "object",
        "required": [
          "rank",
          "x",
          "y",
          "ncc",
          "npix"
        ],
        "title": "RegionCandidate",
        "description": "One place the recording could sit. `x`/`y` are TOP-LEFT corners (R19), never centres."
      },
      "RegionElectrodes": {
        "properties": {
          "count": {
            "type": "integer",
            "title": "Count"
          },
          "detected": {
            "type": "integer",
            "title": "Detected"
          },
          "inferred": {
            "type": "integer",
            "title": "Inferred"
          },
          "ids": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Ids",
            "description": "`col-row`, the electrode map's own ids."
          },
          "cols": {
            "items": {
              "type": "integer"
            },
            "type": "array",
            "title": "Cols"
          },
          "rows": {
            "items": {
              "type": "integer"
            },
            "type": "array",
            "title": "Rows"
          },
          "map": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Map",
            "description": "The map FILENAME these ids came from."
          },
          "array_coverage": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Array Coverage"
          },
          "um_per_px": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Um Per Px"
          }
        },
        "type": "object",
        "required": [
          "count",
          "detected",
          "inferred",
          "ids"
        ],
        "title": "RegionElectrodes",
        "description": "The electrodes whose centres fall inside the rectangle \u2014 the point of the whole feature."
      },
      "RegionRecord": {
        "properties": {
          "id": {
            "type": "string",
            "title": "Id"
          },
          "name": {
            "type": "string",
            "title": "Name",
            "description": "Editable label; defaults to the video's file name."
          },
          "source": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/VideoSource"
              },
              {
                "type": "null"
              }
            ]
          },
          "still": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Still",
            "description": "FILENAME in outputs/ of the winning still, already resampled to MOSAIC SCALE \u2014 the picture the UI fades into the rectangle, and the template a re-snap matches with. One file, both jobs."
          },
          "x": {
            "type": "number",
            "title": "X",
            "default": 0.0
          },
          "y": {
            "type": "number",
            "title": "Y",
            "default": 0.0
          },
          "w": {
            "type": "number",
            "title": "W",
            "default": 0.0
          },
          "h": {
            "type": "number",
            "title": "H",
            "default": 0.0
          },
          "still_kind": {
            "type": "string",
            "title": "Still Kind",
            "description": "Which projection of the recording won: median / max / std.",
            "default": ""
          },
          "zoom": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/RegionZoom"
              },
              {
                "type": "null"
              }
            ]
          },
          "ncc": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Ncc"
          },
          "margin": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Margin",
            "description": "best - runner-up on the GLOBAL surface. The only evidence that the aperture killed the electrode-comb aliases rather than outscoring them."
          },
          "margin_thin": {
            "type": "boolean",
            "title": "Margin Thin",
            "default": false
          },
          "confident": {
            "type": "boolean",
            "title": "Confident",
            "default": false
          },
          "candidates": {
            "items": {
              "$ref": "#/components/schemas/RegionCandidate"
            },
            "type": "array",
            "title": "Candidates"
          },
          "tried": {
            "items": {
              "additionalProperties": true,
              "type": "object"
            },
            "type": "array",
            "title": "Tried",
            "description": "Every still x scale that was attempted, with its score \u2014 so a poor result can be diagnosed instead of merely disbelieved."
          },
          "electrodes": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/RegionElectrodes"
              },
              {
                "type": "null"
              }
            ]
          },
          "status": {
            "type": "string",
            "enum": [
              "unconfirmed",
              "confirmed"
            ],
            "title": "Status",
            "default": "unconfirmed"
          },
          "placed_by": {
            "type": "string",
            "title": "Placed By",
            "description": "machine | hand | hand+snap",
            "default": "machine"
          },
          "located_at": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Located At"
          },
          "source_stamp": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Source Stamp",
            "description": "The build's `built_at` this was placed against. Rebuild the mosaic and the location is STALE \u2014 it never silently drifts."
          },
          "moved_px": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Moved Px"
          },
          "snap_margin": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Snap Margin"
          },
          "elapsed_ms": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Elapsed Ms"
          }
        },
        "additionalProperties": true,
        "type": "object",
        "required": [
          "id",
          "name"
        ],
        "title": "RegionRecord",
        "description": "`doc[\"regions\"][i]` \u2014 one located recording, with its evidence."
      },
      "RegionUpdateRequest": {
        "properties": {
          "analysis_id": {
            "type": "string",
            "title": "Analysis Id"
          },
          "region_id": {
            "type": "string",
            "title": "Region Id"
          },
          "name": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Name"
          },
          "status": {
            "anyOf": [
              {
                "type": "string",
                "enum": [
                  "unconfirmed",
                  "confirmed"
                ]
              },
              {
                "type": "null"
              }
            ],
            "title": "Status"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "analysis_id",
          "region_id"
        ],
        "title": "RegionUpdateRequest",
        "description": "`PATCH /api/videomosaic/regions` \u2014 rename, or confirm. Both are the user's word."
      },
      "RegionZoom": {
        "properties": {
          "scale": {
            "type": "number",
            "title": "Scale",
            "description": "Multiply recording px by this to get mosaic px."
          },
          "measured": {
            "type": "boolean",
            "title": "Measured",
            "description": "True when the ratio was MEASURED from the electrode lattice in both pictures. False means no lattice could be read and a ladder of zoom factors was searched instead \u2014 a guess the UI must not present as a measurement."
          },
          "pitch_recording_px": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Pitch Recording Px"
          },
          "pitch_mosaic_px": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Pitch Mosaic Px"
          },
          "angle_recording_deg": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Angle Recording Deg"
          },
          "angle_mosaic_deg": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Angle Mosaic Deg"
          },
          "angle_delta_deg": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Angle Delta Deg",
            "description": "Degrees the recording's lattice is turned relative to the mosaic's, folded into (-45, 45]. Rotation is NOT solved (same rig, same orientation) \u2014 this is reported so a reseated chip is visible instead of silently mis-placed."
          },
          "note": {
            "type": "string",
            "title": "Note",
            "default": ""
          }
        },
        "type": "object",
        "required": [
          "scale",
          "measured"
        ],
        "title": "RegionZoom",
        "description": "How much the recording was shrunk/grown to sit on the mosaic, and where that came from."
      },
      "RegionsPayload": {
        "properties": {
          "analysis_id": {
            "type": "string",
            "title": "Analysis Id"
          },
          "regions": {
            "items": {
              "$ref": "#/components/schemas/RegionRecord"
            },
            "type": "array",
            "title": "Regions"
          },
          "built_from": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Built From"
          },
          "stale": {
            "type": "boolean",
            "title": "Stale",
            "description": "True when the mosaic was rebuilt after these were placed.",
            "default": false
          },
          "outputs": {
            "additionalProperties": {
              "type": "string"
            },
            "type": "object",
            "title": "Outputs"
          }
        },
        "type": "object",
        "required": [
          "analysis_id"
        ],
        "title": "RegionsPayload",
        "description": "`GET /api/videomosaic/{id}/regions`."
      },
      "RejectedMatch": {
        "properties": {
          "x": {
            "type": "number",
            "title": "X"
          },
          "y": {
            "type": "number",
            "title": "Y"
          },
          "ncc": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Ncc"
          },
          "margin": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Margin"
          },
          "npix": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Npix"
          },
          "rank": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Rank"
          },
          "n_anchors": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "N Anchors"
          }
        },
        "additionalProperties": true,
        "type": "object",
        "required": [
          "x",
          "y"
        ],
        "title": "RejectedMatch",
        "description": "What the matcher wanted, on a tile the app **diverted** to the solver's answer. It is the\n*evidence for* the divert and the QC report needs it in full."
      },
      "RenameAnalysisRequest": {
        "properties": {
          "name": {
            "type": "string",
            "title": "Name"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "name"
        ],
        "title": "RenameAnalysisRequest",
        "description": "Rename a project (`PATCH /api/projects/{id}`). Rewrites the manifest only \u2014 **the folder\nnever moves; the id is forever.** This is what the project manager's rename does."
      },
      "RenameMeaRecordingRequest": {
        "properties": {
          "label": {
            "type": "string",
            "title": "Label"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "label"
        ],
        "title": "RenameMeaRecordingRequest",
        "description": "`PATCH /api/mea/{analysis_id}/recordings/{recording_id}` \u2014 call the row something else.\n\n\u2b50 **A RENAME IS A FACT ABOUT THE ROW, NEVER ABOUT THE BYTES.** It rewrites the document's\n`label` only: his original `.h5` is read-only by hard rule, and the project's copy lives at a\npath keyed by the recording's id, which is forever. A blank name is refused (400) \u2014 a nameless\nrow is one he cannot pick out of a list."
      },
      "RescopeRequest": {
        "properties": {
          "session_id": {
            "type": "string",
            "title": "Session Id"
          },
          "doc": {
            "$ref": "#/components/schemas/MosaicDocument"
          },
          "lo": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Lo"
          },
          "hi": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Hi"
          },
          "pass_split": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Pass Split",
            "description": "Override. Omit and the split is re-detected for the new range."
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "session_id",
          "doc"
        ],
        "title": "RescopeRequest",
        "description": "`POST /api/mosaic/document/rescope` \u2014 the Range step's `Apply`, applied to the DOCUMENT.\n\n\u2b50 **THE SERVER RE-AUTHORS THE TILE SET.** `POST /api/mosaic/run` only *measures* the run; it\nleaves the document alone, so a project opened on every square snapshot in the dataset kept the\nstray pre-scan snapshots (`1`, `5-7` on 260620d) as tiles of the mosaic. This is the route that\nmakes the answer stick \u2014 and it is a route, not four edits in the browser, because a changed\ntrial list changes the **gaps** and makes the build **stale**.\n\n\ud83d\udd34 It keeps every surviving tile's work byte-for-byte; only trials that leave the range are\ndropped. `n_placed_removed` in the reply is what the caller should have confirmed first."
      },
      "RescopeResponse": {
        "properties": {
          "doc": {
            "$ref": "#/components/schemas/MosaicDocument"
          },
          "run": {
            "$ref": "#/components/schemas/RunDetection",
            "description": "The re-detection the new tile set was authored from."
          },
          "n_trials": {
            "type": "integer",
            "title": "N Trials"
          },
          "added": {
            "items": {
              "type": "integer"
            },
            "type": "array",
            "title": "Added"
          },
          "removed": {
            "items": {
              "type": "integer"
            },
            "type": "array",
            "title": "Removed",
            "description": "Trials that left the mosaic. \u26d4 NOT 'excluded' \u2014 they were never tiles of it, and they do not appear in `unusable_tiles` or in the human-edit counters."
          },
          "n_added": {
            "type": "integer",
            "title": "N Added"
          },
          "n_removed": {
            "type": "integer",
            "title": "N Removed"
          },
          "n_placed_removed": {
            "type": "integer",
            "title": "N Placed Removed",
            "description": "How many of `removed` carried a position. The work that was thrown away."
          }
        },
        "type": "object",
        "required": [
          "doc",
          "run",
          "n_trials",
          "added",
          "removed",
          "n_added",
          "n_removed",
          "n_placed_removed"
        ],
        "title": "RescopeResponse"
      },
      "RunBlock": {
        "properties": {
          "detected": {
            "type": "boolean",
            "title": "Detected",
            "default": true
          },
          "why": {
            "type": "string",
            "title": "Why",
            "default": ""
          },
          "pass_split_detected": {
            "type": "boolean",
            "title": "Pass Split Detected",
            "default": true
          },
          "pass_split_why": {
            "type": "string",
            "title": "Pass Split Why",
            "default": ""
          },
          "n_trials": {
            "type": "integer",
            "title": "N Trials",
            "default": 0
          }
        },
        "additionalProperties": true,
        "type": "object",
        "title": "RunBlock",
        "description": "What the run detection said, recorded so the document can explain itself later."
      },
      "RunDetectRequest": {
        "properties": {
          "session_id": {
            "type": "string",
            "title": "Session Id"
          },
          "lo": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Lo"
          },
          "hi": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Hi"
          },
          "pass_split": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Pass Split"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "session_id"
        ],
        "title": "RunDetectRequest",
        "description": "`POST /api/mosaic/run` \u2014 detect, or override. **A pure function of the session's inventory.**\n\n\u2b50 It is NOT a session reload. v1 made `PATCH /api/session/run` a full re-open (5 s, and it\ninvalidated the tone and the scan) because the run lived *on* the session. It does not any more."
      },
      "RunDetection": {
        "properties": {
          "lo": {
            "type": "integer",
            "title": "Lo"
          },
          "hi": {
            "type": "integer",
            "title": "Hi"
          },
          "trials": {
            "items": {
              "type": "integer"
            },
            "type": "array",
            "title": "Trials",
            "description": "\u26d4 The run, gated **by shape only**. Nothing is dropped by trial number, ever. Every snapshot on disk in range is here."
          },
          "n": {
            "type": "integer",
            "title": "N"
          },
          "n_in_range": {
            "type": "integer",
            "title": "N In Range"
          },
          "detected": {
            "type": "boolean",
            "title": "Detected"
          },
          "why": {
            "type": "string",
            "title": "Why",
            "description": "MEASURED. Goes behind the `?`, verbatim."
          },
          "blocks": {
            "items": {
              "$ref": "#/components/schemas/SnapshotBlock"
            },
            "type": "array",
            "title": "Blocks"
          },
          "dropped": {
            "items": {
              "$ref": "#/components/schemas/DroppedTrial"
            },
            "type": "array",
            "title": "Dropped"
          },
          "pass_split": {
            "$ref": "#/components/schemas/PassSplit"
          },
          "gaps": {
            "items": {
              "prefixItems": [
                {
                  "type": "integer"
                },
                {
                  "type": "integer"
                }
              ],
              "type": "array",
              "maxItems": 2,
              "minItems": 2
            },
            "type": "array",
            "title": "Gaps"
          }
        },
        "type": "object",
        "required": [
          "lo",
          "hi",
          "trials",
          "n",
          "n_in_range",
          "detected",
          "why",
          "pass_split"
        ],
        "title": "RunDetection"
      },
      "RunnerUpSplit": {
        "properties": {
          "after_trial": {
            "type": "integer",
            "title": "After Trial"
          },
          "gap_s": {
            "type": "number",
            "title": "Gap S"
          }
        },
        "type": "object",
        "required": [
          "after_trial",
          "gap_s"
        ],
        "title": "RunnerUpSplit"
      },
      "SaveDocumentRequest": {
        "properties": {
          "path": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Path",
            "description": "save-as only. Refused inside `data/`, the repo, or the dataset."
          },
          "doc": {
            "$ref": "#/components/schemas/Document"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "doc"
        ],
        "title": "SaveDocumentRequest",
        "description": "`PUT /api/analyses/{analysis_id}/document` (into the workspace) or\n`POST /api/documents/save-as` (a file the user names \u2014 `Save\u2026`, reachable from EVERY screen).\n\n\u26a0\ufe0f **ORDER, on the server:** structural-validate \u2192 normalise \u2192 stamp \u2192 full-validate \u2192 write,\n**atomically** (temp + `os.replace`, under a lock, with fsync and a retry). Not\n`validate \u2192 normalise`: the derived fields are exactly the ones that drift when the user\nexcludes a tile, and `normalise` is what repairs them. \ud83d\udd34 On Windows two concurrent `os.replace`\non one target give `WinError 5` and the loser's document was **silently lost**."
      },
      "SaveResult": {
        "properties": {
          "path": {
            "type": "string",
            "title": "Path"
          },
          "bytes": {
            "type": "integer",
            "title": "Bytes"
          },
          "saved_at": {
            "type": "string",
            "title": "Saved At"
          },
          "warnings": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Warnings"
          }
        },
        "type": "object",
        "required": [
          "path",
          "bytes",
          "saved_at"
        ],
        "title": "SaveResult"
      },
      "Scale": {
        "properties": {
          "um_per_px": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Um Per Px"
          },
          "source": {
            "type": "string",
            "enum": [
              "unknown",
              "user-supplied by hand, not measured"
            ],
            "title": "Source",
            "default": "unknown"
          }
        },
        "type": "object",
        "title": "Scale",
        "description": "\ud83d\udccf **PIXELS ONLY.** `um_per_px` is `null` by default and the exporter writes no scale bar and\nno OME-TIFF `PhysicalSizeX/Y`. If the user types a number in, it is written **and stamped\n\"user-supplied by hand, not measured\"**. \u274c Never 1.237 \u00b5m/px \u2014 that came from a broken\ninference. (There is no magnification difference between the passes: cross-pass tissue scale is\n1.0000 \u00b1 0.0002. The MEA grid pitch tracks *focus*, not magnification.)"
      },
      "ScoreResult": {
        "properties": {
          "target": {
            "type": "integer",
            "title": "Target"
          },
          "at": {
            "prefixItems": [
              {
                "type": "number"
              },
              {
                "type": "number"
              }
            ],
            "type": "array",
            "maxItems": 2,
            "minItems": 2,
            "title": "At"
          },
          "ncc": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Ncc"
          },
          "npix": {
            "type": "integer",
            "title": "Npix"
          },
          "refused": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/Refusal"
              },
              {
                "type": "null"
              }
            ]
          },
          "dropped_anchors": {
            "items": {
              "type": "integer"
            },
            "type": "array",
            "title": "Dropped Anchors"
          },
          "elapsed_ms": {
            "type": "number",
            "title": "Elapsed Ms"
          }
        },
        "type": "object",
        "required": [
          "target",
          "at",
          "ncc",
          "npix",
          "elapsed_ms"
        ],
        "title": "ScoreResult",
        "description": "\u26a0\ufe0f `ncc` is **`null`** when the overlap is below `exact_ncc`'s floor (< 3000 valid px, or\n< 64 px on a side). The honest answer is *\"not measurable\"* \u2014 **never `0.0`**."
      },
      "SeatingScorePayload": {
        "properties": {
          "flip_x": {
            "type": "boolean",
            "title": "Flip X"
          },
          "flip_y": {
            "type": "boolean",
            "title": "Flip Y"
          },
          "n_recorded": {
            "type": "integer",
            "title": "N Recorded",
            "description": "Of the scored regions' pads, how many this seating maps onto electrodes that were actually recorded \u2014 totalled across regions when several took part."
          },
          "n_region": {
            "type": "integer",
            "title": "N Region",
            "description": "How many pads the scored regions cover \u2014 the denominator, totalled across regions when several took part."
          },
          "coverage": {
            "type": "number",
            "title": "Coverage"
          },
          "n_spikes": {
            "type": "integer",
            "title": "N Spikes"
          },
          "correlation": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Correlation",
            "description": "Spike rate vs calcium activity. \u2b50 **null means UNTESTABLE** \u2014 this seating puts no recorded electrode under any scored field \u2014 which is a different answer from a low correlation and must never be rendered as one. Across several regions it is the per-region correlations combined, weighted by each region's recorded-pad count under this seating (`orientation.combined_correlation` documents why a weighted mean: the regions' videos share no clock, so their series cannot be laid on one axis)."
          },
          "scorable": {
            "type": "boolean",
            "title": "Scorable"
          }
        },
        "type": "object",
        "required": [
          "flip_x",
          "flip_y",
          "n_recorded",
          "n_region",
          "coverage",
          "n_spikes",
          "scorable"
        ],
        "title": "SeatingScorePayload",
        "description": "One candidate seating and how well it explains the data \u2014 summed over every region the\ntest scored (one region behaves exactly as it always did)."
      },
      "SeedRequest": {
        "properties": {
          "doc": {
            "$ref": "#/components/schemas/MosaicDocument"
          },
          "build_id": {
            "type": "string",
            "title": "Build Id"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "doc",
          "build_id"
        ],
        "title": "SeedRequest",
        "description": "`POST /api/mosaic/seed` \u2014 apply a build to a document. **The SERVER does this**, not the\nfront end (see `CreateAnalysisRequest`).\n\n\ud83d\udd34 **A RE-SOLVE MUST NOT DESTROY THE HUMAN'S WORK.** It **keeps** every tile that is `anchored`\n**or** flagged `human`, and seeds everything else *around* them \u2014 translating the build onto the\nhuman's field by the **MEDIAN** offset over the protected tiles (a median, not a mean: a tile the\nhuman corrected *because t33 was wrong* is precisely an outlier, and one 2,969 px correction\nwould drag a mean into nonsense). If **none** of the human's tiles are in the build, the two\nframes cannot be tied together and the server **refuses to seed** (`409 refused`) rather than\nguess.\n\n(v1 called `setState` unconditionally on every non-excluded tile, so a 150-tile sweep with three\ncatastrophic hand-corrections in it was wiped by taking the app's own advice to re-solve \u2014 and\nthe autosave then wrote the wiped document over the crash-recovery file.)"
      },
      "SeedResponse": {
        "properties": {
          "doc": {
            "$ref": "#/components/schemas/MosaicDocument"
          },
          "n_seeded": {
            "type": "integer",
            "title": "N Seeded"
          },
          "n_protected": {
            "type": "integer",
            "title": "N Protected",
            "description": "anchored + human tiles the build was NOT allowed to touch."
          },
          "seed_translation": {
            "prefixItems": [
              {
                "type": "number"
              },
              {
                "type": "number"
              }
            ],
            "type": "array",
            "maxItems": 2,
            "minItems": 2,
            "title": "Seed Translation"
          }
        },
        "type": "object",
        "required": [
          "doc",
          "n_seeded",
          "n_protected",
          "seed_translation"
        ],
        "title": "SeedResponse"
      },
      "SeededFrom": {
        "properties": {
          "method": {
            "type": "string",
            "title": "Method"
          },
          "build_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Build Id"
          },
          "config": {
            "additionalProperties": {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "integer"
                },
                {
                  "type": "boolean"
                },
                {
                  "type": "string"
                },
                {
                  "additionalProperties": {
                    "anyOf": [
                      {
                        "type": "number"
                      },
                      {
                        "type": "integer"
                      },
                      {
                        "type": "boolean"
                      },
                      {
                        "type": "string"
                      },
                      {
                        "type": "null"
                      }
                    ]
                  },
                  "type": "object"
                },
                {
                  "items": {
                    "anyOf": [
                      {
                        "type": "number"
                      },
                      {
                        "type": "integer"
                      },
                      {
                        "type": "boolean"
                      },
                      {
                        "type": "string"
                      },
                      {
                        "type": "null"
                      }
                    ]
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ]
            },
            "type": "object",
            "title": "Config"
          },
          "detected_from": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Detected From",
            "description": "HOW the machine was detected. Evidence, not a self-declaration."
          }
        },
        "additionalProperties": true,
        "type": "object",
        "required": [
          "method"
        ],
        "title": "SeededFrom"
      },
      "SessionListResponse": {
        "properties": {
          "sessions": {
            "items": {
              "$ref": "#/components/schemas/SessionResponse"
            },
            "type": "array",
            "title": "Sessions"
          }
        },
        "type": "object",
        "required": [
          "sessions"
        ],
        "title": "SessionListResponse"
      },
      "SessionResponse": {
        "properties": {
          "session_id": {
            "type": "string",
            "title": "Session Id"
          },
          "dataset_key": {
            "type": "string",
            "title": "Dataset Key"
          },
          "dataset": {
            "type": "string",
            "title": "Dataset"
          },
          "experiment": {
            "type": "string",
            "title": "Experiment"
          },
          "path": {
            "type": "string",
            "title": "Path"
          },
          "opened_at": {
            "type": "string",
            "title": "Opened At"
          },
          "nonce": {
            "type": "string",
            "title": "Nonce",
            "description": "\u2b50 A FRESH identity per open. Cache-bust every pixel URL with `?v={nonce}.{tone.version}`. **NOT the tone version alone** \u2014 that is a dataclass default and resets to 1 on every open, while the pixels behind an `immutable, max-age=1y` URL change. Open a second acquisition whose trial numbers overlap and the browser serves the FIRST dataset's pixels. (And `id()` is recycled \u2014 do not use it.)"
          },
          "w": {
            "type": "integer",
            "title": "W"
          },
          "h": {
            "type": "integer",
            "title": "H"
          },
          "trials": {
            "items": {
              "type": "integer"
            },
            "type": "array",
            "title": "Trials",
            "description": "What is actually loaded, in order."
          },
          "n": {
            "type": "integer",
            "title": "N"
          },
          "skipped": {
            "items": {
              "$ref": "#/components/schemas/SkippedFrame"
            },
            "type": "array",
            "title": "Skipped"
          },
          "tone": {
            "$ref": "#/components/schemas/Tone"
          },
          "frame_note": {
            "type": "string",
            "title": "Frame Note",
            "description": "\u2b50 What the reader ACTUALLY did to these pixels, derived from THIS acquisition's XML \u2014 never asserted. It goes into the TIFF header. Do not hard-code \"180-degree-flipped\" anywhere: the reader flips CONDITIONALLY on ax/ay, and a header that lies about its own coordinate frame is the one thing this project has been burned by."
          },
          "gpu": {
            "$ref": "#/components/schemas/GpuInfo"
          },
          "n_log_entries": {
            "type": "integer",
            "title": "N Log Entries"
          }
        },
        "type": "object",
        "required": [
          "session_id",
          "dataset_key",
          "dataset",
          "experiment",
          "path",
          "opened_at",
          "nonce",
          "w",
          "h",
          "trials",
          "n",
          "tone",
          "frame_note",
          "gpu",
          "n_log_entries"
        ],
        "title": "SessionResponse"
      },
      "Settings": {
        "properties": {
          "recent_datasets": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Recent Datasets",
            "description": "Data folders recently opened. Offered back as completions."
          }
        },
        "type": "object",
        "title": "Settings",
        "description": "Everything the app remembers between launches. \u26d4 **No dataset knowledge lives here.**\nA remembered *path* is not knowledge about the data at that path.\n\n\u2b50 **ONE KEY, since R44 (2026-08-10).** `projects` went with the folders it indexed: projects\nlive in Camea's own store and the store IS the index. (`workspace` and `dataset_roots` went on\n2026-07-25.) What is left is the one thing the app cannot re-derive \u2014 where the user keeps his\ndata, which is his to decide and nobody else's to remember."
      },
      "SettingsUpdate": {
        "properties": {
          "recent_datasets": {
            "anyOf": [
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "title": "Recent Datasets"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "title": "SettingsUpdate"
      },
      "ShapeGroup": {
        "properties": {
          "w": {
            "type": "integer",
            "title": "W"
          },
          "h": {
            "type": "integer",
            "title": "H"
          },
          "n": {
            "type": "integer",
            "title": "N"
          },
          "trials": {
            "items": {
              "type": "integer"
            },
            "type": "array",
            "title": "Trials"
          }
        },
        "type": "object",
        "required": [
          "w",
          "h",
          "n",
          "trials"
        ],
        "title": "ShapeGroup",
        "description": "Snapshot trials sharing one frame shape. \u26d4 The browser reports shapes; it does not *gate*\non one. The 512\u00d7512 gate is the mosaic feature's policy (512 is `t33.TILE`), not core's."
      },
      "SkippedFrame": {
        "properties": {
          "trial": {
            "type": "integer",
            "title": "Trial"
          },
          "reason": {
            "type": "string",
            "enum": [
              "off_shape",
              "unreadable",
              "not_snapshot"
            ],
            "title": "Reason"
          },
          "w": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "W"
          },
          "h": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "H"
          },
          "message": {
            "type": "string",
            "title": "Message"
          }
        },
        "type": "object",
        "required": [
          "trial",
          "reason",
          "message"
        ],
        "title": "SkippedFrame"
      },
      "SnapRegionRequest": {
        "properties": {
          "analysis_id": {
            "type": "string",
            "title": "Analysis Id"
          },
          "region_id": {
            "type": "string",
            "title": "Region Id"
          },
          "x": {
            "type": "number",
            "title": "X",
            "description": "Where the user dropped the rectangle's TOP-LEFT, mosaic px."
          },
          "y": {
            "type": "number",
            "title": "Y"
          },
          "radius": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Radius",
            "description": "Local search half-width in mosaic px. Clamped server-side: a local search has no whole-plane view, so a wide one is an alias generator."
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "analysis_id",
          "region_id",
          "x",
          "y"
        ],
        "title": "SnapRegionRequest",
        "description": "`POST /api/videomosaic/regions/snap` \u2014 *\"I dragged it here, now snap it.\"*"
      },
      "SnapshotBlock": {
        "properties": {
          "lo": {
            "type": "integer",
            "title": "Lo"
          },
          "hi": {
            "type": "integer",
            "title": "Hi"
          },
          "n": {
            "type": "integer",
            "title": "N"
          }
        },
        "type": "object",
        "required": [
          "lo",
          "hi",
          "n"
        ],
        "title": "SnapshotBlock",
        "description": "A contiguous run of Snapshot trials in `log.txt`."
      },
      "T27Config": {
        "properties": {
          "conf": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Conf"
          },
          "run_conf": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Run Conf"
          },
          "span": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Span"
          },
          "control": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Control"
          }
        },
        "additionalProperties": true,
        "type": "object",
        "title": "T27Config"
      },
      "TextureResponse": {
        "properties": {
          "measure": {
            "type": "string",
            "title": "Measure",
            "default": "std of DoG(sigma=3, sigma=30) of the frame as read"
          },
          "texture": {
            "additionalProperties": {
              "type": "number"
            },
            "type": "object",
            "title": "Texture"
          },
          "n": {
            "type": "integer",
            "title": "N"
          }
        },
        "type": "object",
        "required": [
          "texture",
          "n"
        ],
        "title": "TextureResponse",
        "description": "\u2b50 **THE MEASUREMENT \u2014 and it is CORE, not mosaic.** `std(DoG(3,30))` answers \"how much\ntexture is in this image\", which is a property of the FRAME. Every future feature asks it (a\nsegmentation feature must not segment glare; the browser wants to say \"these frames are empty\").\n\nIt is also **free** here and costs +3.0 s / +624 MiB anywhere else: the matcher's band-passed\nstack and this measure are **the same array**.\n\n\u26d4 There is **no threshold here, no list, and no policy.** Turning a number into a *proposal*\nis the mosaic feature's job (`POST /api/mosaic/screen/propose`), and turning a proposal into a\n*decision* is the human's \u2014 it lands in the document. Never in the session."
      },
      "ThumbsResponse": {
        "properties": {
          "grid": {
            "type": "integer",
            "title": "Grid"
          },
          "cell": {
            "type": "integer",
            "title": "Cell"
          },
          "trials": {
            "items": {
              "type": "integer"
            },
            "type": "array",
            "title": "Trials"
          },
          "n": {
            "type": "integer",
            "title": "N"
          },
          "version": {
            "type": "string",
            "title": "Version",
            "description": "`{nonce}.{tone.version}` \u2014 pass it as `?v=`."
          }
        },
        "type": "object",
        "required": [
          "grid",
          "cell",
          "trials",
          "n",
          "version"
        ],
        "title": "ThumbsResponse",
        "description": "The contact sheet: ONE sprite sheet. Cell `i` holds `trials[i]` at\n`(x, y) = ((i % grid) * cell, (i // grid) * cell)`."
      },
      "TileRecord": {
        "properties": {
          "status": {
            "type": "string",
            "enum": [
              "unplaced",
              "unverified",
              "anchor",
              "excluded"
            ],
            "title": "Status"
          },
          "state": {
            "type": "string",
            "enum": [
              "unplaced",
              "unverified",
              "anchored",
              "excluded"
            ],
            "title": "State"
          },
          "x": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "X"
          },
          "y": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Y"
          },
          "r": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "R",
            "description": "Draw radius/opacity hint. Display only."
          },
          "pass": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Pass"
          },
          "machine": {
            "anyOf": [
              {
                "prefixItems": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "number"
                  }
                ],
                "type": "array",
                "maxItems": 2,
                "minItems": 2
              },
              {
                "type": "null"
              }
            ],
            "title": "Machine",
            "description": "What the solver said, translated into the document's frame. \u2b50 Its mere presence makes the provenance non-independent \u2014 see `Provenance`."
          },
          "moved_px": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Moved Px",
            "description": "|position \u2212 machine|."
          },
          "ncc": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Ncc"
          },
          "margin": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Margin"
          },
          "npix": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Npix"
          },
          "n_anchors": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "N Anchors"
          },
          "blank": {
            "type": "boolean",
            "title": "Blank",
            "description": "MEASURED. The matcher refuses this tile. **It excludes nothing and it never will.** Never cleared \u2014 it is a fact about the pixels.",
            "default": false
          },
          "texture": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Texture"
          },
          "judged_at": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Judged At"
          },
          "seq": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Seq",
            "description": "Judgement order. The stale cascade is seq >."
          },
          "human": {
            "type": "boolean",
            "title": "Human",
            "description": "A hand put this position here. \u26a0\ufe0f `advance()` must NEVER re-match over it \u2014 when he drags a tile it is usually *because the matcher was wrong*.",
            "default": false
          },
          "stale": {
            "type": "boolean",
            "title": "Stale",
            "description": "Matched against an anchor field that has since changed. Cleared only by a GLOBAL re-check that agrees within 5 px \u2014 which is allowed to say NO.",
            "default": false
          },
          "diverted": {
            "type": "boolean",
            "title": "Diverted",
            "description": "Sitting on the SOLVER's position because the match was not trustworthy. Dies the instant a hand rewrites the position. **Not** 'the human accepted the machine'.",
            "default": false
          },
          "divert_reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Divert Reason"
          },
          "rejected_match": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/RejectedMatch"
              },
              {
                "type": "null"
              }
            ]
          },
          "alt_rank": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Alt Rank",
            "description": "Which peak the human chose. **0-indexed in storage; DISPLAYED as +1.** The number he reads and the number he presses must agree."
          },
          "last_xy": {
            "anyOf": [
              {
                "prefixItems": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "number"
                  }
                ],
                "type": "array",
                "maxItems": 2,
                "minItems": 2
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Xy",
            "description": "The position an `E` took away. For undo."
          },
          "source": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Source",
            "description": "Human-readable provenance of the position."
          },
          "note": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Note"
          },
          "excluded_reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Excluded Reason"
          },
          "unusable_reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Unusable Reason"
          }
        },
        "additionalProperties": true,
        "type": "object",
        "required": [
          "status",
          "state"
        ],
        "title": "TileRecord",
        "description": "One tile. `extra=\"allow\"`: unknown per-tile keys round-trip verbatim (a hand-written note in\nsomebody's ground truth must survive an app that has never heard of it).\n\n**Facts that are NOT states** (`blank` / `diverted` / `human` / `stale` / `machine` / `seq` /\n`alt_rank`): model them as fields, never as states, or the counters and the exports go wrong.\n\n\u26a0\ufe0f **`ncc` is defined as THE NCC AT THE TILE'S FINAL POSITION**, on every path. On a divert the\ntile is *not* at the match's best position, so writing the rejected alias's score here would\nattribute it to the position that shipped. Re-measure with one `POST /api/mosaic/match/score`.\n\n\u26a0\ufe0f A tile that leaves `excluded` must **stop claiming it was thrown out** \u2014 `excluded_reason` /\n`unusable_reason` die with the judgement, or a tile goes into the exported ground truth reading\n`status: \"anchor\", excluded: true`. **But `blank` is NOT cleared**: that is a *measurement*\nabout the pixels, not a judgement."
      },
      "TolerancePx": {
        "properties": {
          "region_default": {
            "type": "number",
            "title": "Region Default"
          }
        },
        "additionalProperties": true,
        "type": "object",
        "required": [
          "region_default"
        ],
        "title": "TolerancePx",
        "description": "\u26d4 `region_default` is **REQUIRED** \u2014 `benchmark/score.py :: load_gt()` reads it, at the top\nlevel, and the exporter asserts it is there before the file leaves the process."
      },
      "Tone": {
        "properties": {
          "lo": {
            "type": "number",
            "title": "Lo"
          },
          "hi": {
            "type": "number",
            "title": "Hi"
          },
          "level": {
            "type": "number",
            "title": "Level"
          },
          "flat_sigma": {
            "type": "number",
            "title": "Flat Sigma"
          },
          "pct_lo": {
            "type": "number",
            "title": "Pct Lo"
          },
          "pct_hi": {
            "type": "number",
            "title": "Pct Hi"
          },
          "n_sample": {
            "type": "integer",
            "title": "N Sample"
          },
          "auto": {
            "type": "boolean",
            "title": "Auto"
          },
          "version": {
            "type": "integer",
            "title": "Version",
            "description": "Bumped on every change. Half of the pixel cache-buster \u2014 see `Session.nonce`."
          }
        },
        "type": "object",
        "required": [
          "lo",
          "hi",
          "level",
          "flat_sigma",
          "pct_lo",
          "pct_hi",
          "n_sample",
          "auto",
          "version"
        ],
        "title": "Tone",
        "description": "The display window. \u26a0\ufe0f\u26a0\ufe0f **GLOBAL, NEVER PER-TILE.** A per-tile percentile stretch\nover-brightens near-empty frames and makes overlapping tiles disagree in brightness \u2014 **which\ndestroys the Difference-mode check the whole verification loop depends on.** There is no\nper-tile path and there must never be one. Tone is display-only: it never touches the matcher\n(which sees band-passed, mean-subtracted pixels) and never touches the exported TIFF."
      },
      "ToneSnapshot": {
        "properties": {
          "lo": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Lo"
          },
          "hi": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Hi"
          },
          "level": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Level"
          },
          "flat_sigma": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Flat Sigma"
          },
          "pct_lo": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Pct Lo"
          },
          "pct_hi": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Pct Hi"
          },
          "auto": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Auto"
          }
        },
        "additionalProperties": true,
        "type": "object",
        "title": "ToneSnapshot",
        "description": "A *display* snapshot, so a reload resumes where he left off. Core owns the tone; the document\nonly records it. (\u26d4 The opacity slider is **not** here: `.camea.json` records the mosaic, not\nthe operator's viewing preferences.)"
      },
      "ToneUpdate": {
        "properties": {
          "lo": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Lo"
          },
          "hi": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Hi"
          },
          "auto": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Auto",
            "description": "true = recompute and discard lo/hi."
          }
        },
        "additionalProperties": false,
        "type": "object",
        "title": "ToneUpdate"
      },
      "TraceHealthPayload": {
        "properties": {
          "n_samples": {
            "type": "integer",
            "title": "N Samples"
          },
          "fill_value": {
            "type": "integer",
            "title": "Fill Value"
          },
          "fill_fraction": {
            "type": "number",
            "title": "Fill Fraction",
            "description": "Share of samples equal to the commonest value. A live electrode carries thermal noise, so a healthy window sits low; near 1.0 means the decoder did not reconstruct the stream."
          },
          "distinct_values": {
            "type": "integer",
            "title": "Distinct Values"
          },
          "flat": {
            "type": "boolean",
            "title": "Flat",
            "description": "\u26d4 True = NOT a usable waveform. The UI must say so rather than draw a convincing flat line."
          }
        },
        "type": "object",
        "required": [
          "n_samples",
          "fill_value",
          "fill_fraction",
          "distinct_values",
          "flat"
        ],
        "title": "TraceHealthPayload",
        "description": "How much of a returned window is one repeated value \u2014 the honesty check on a waveform."
      },
      "TrialMeta": {
        "properties": {
          "trial": {
            "type": "integer",
            "title": "Trial"
          },
          "type": {
            "type": "string",
            "title": "Type",
            "description": "The log.txt trial type, e.g. \"Snapshot\"."
          },
          "time": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Time"
          },
          "gap_s": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Gap S",
            "description": "Seconds since the previous SNAPSHOT. null for the first."
          },
          "w": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "W"
          },
          "h": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "H"
          },
          "dtype": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Dtype"
          },
          "bytes": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Bytes"
          },
          "flip_x": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Flip X"
          },
          "flip_y": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Flip Y"
          },
          "dat": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Dat"
          }
        },
        "type": "object",
        "required": [
          "trial",
          "type"
        ],
        "title": "TrialMeta",
        "description": "\u26a0\ufe0f **Shape is PER-TRIAL.** It is parsed from that trial's XML. It is never inferred from a\nfile size, and a trial with `frames != 1` is rejected, not reshaped."
      },
      "ValidateDocumentRequest": {
        "properties": {
          "doc": {
            "$ref": "#/components/schemas/Document"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "doc"
        ],
        "title": "ValidateDocumentRequest"
      },
      "ValidationError": {
        "properties": {
          "loc": {
            "items": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "integer"
                }
              ]
            },
            "type": "array",
            "title": "Location"
          },
          "msg": {
            "type": "string",
            "title": "Message"
          },
          "type": {
            "type": "string",
            "title": "Error Type"
          },
          "input": {
            "title": "Input"
          },
          "ctx": {
            "type": "object",
            "title": "Context"
          }
        },
        "type": "object",
        "required": [
          "loc",
          "msg",
          "type"
        ],
        "title": "ValidationError"
      },
      "ValidationReport": {
        "properties": {
          "ok": {
            "type": "boolean",
            "title": "Ok"
          },
          "problems": {
            "items": {
              "$ref": "#/components/schemas/DocumentProblem"
            },
            "type": "array",
            "title": "Problems"
          }
        },
        "type": "object",
        "required": [
          "ok"
        ],
        "title": "ValidationReport",
        "description": "\u26d4 **NO TRIAL NUMBER IS SPECIAL.** Nothing here may reject a document for WHICH trials it\nplaced. The guard that used to live in v1 (\"tile 284 is thrown out and carries a position\")\nmade the user's own session **unsaveable** the moment he anchored 284."
      },
      "VideoBuildRequest": {
        "properties": {
          "analysis_id": {
            "type": "string",
            "title": "Analysis Id"
          },
          "config": {
            "anyOf": [
              {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "number"
                    },
                    {
                      "type": "integer"
                    },
                    {
                      "type": "boolean"
                    },
                    {
                      "type": "string"
                    },
                    {
                      "additionalProperties": {
                        "anyOf": [
                          {
                            "type": "number"
                          },
                          {
                            "type": "integer"
                          },
                          {
                            "type": "boolean"
                          },
                          {
                            "type": "string"
                          },
                          {
                            "type": "null"
                          }
                        ]
                      },
                      "type": "object"
                    },
                    {
                      "items": {
                        "anyOf": [
                          {
                            "type": "number"
                          },
                          {
                            "type": "integer"
                          },
                          {
                            "type": "boolean"
                          },
                          {
                            "type": "string"
                          },
                          {
                            "type": "null"
                          }
                        ]
                      },
                      "type": "array"
                    },
                    {
                      "type": "null"
                    }
                  ]
                },
                "type": "object"
              },
              {
                "type": "null"
              }
            ],
            "title": "Config",
            "description": "VideoConfig overrides, for development and tests. The UI sends null: a normal user never tunes computer-vision parameters to get a mosaic."
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "analysis_id"
        ],
        "title": "VideoBuildRequest"
      },
      "VideoCanvasSize": {
        "properties": {
          "w": {
            "type": "integer",
            "title": "W"
          },
          "h": {
            "type": "integer",
            "title": "H"
          }
        },
        "type": "object",
        "required": [
          "w",
          "h"
        ],
        "title": "VideoCanvasSize"
      },
      "VideoElectrodeMapRequest": {
        "properties": {
          "analysis_id": {
            "type": "string",
            "title": "Analysis Id"
          },
          "array_coverage": {
            "type": "string",
            "enum": [
              "full",
              "partial"
            ],
            "title": "Array Coverage",
            "description": "Did the user say the WHOLE chip is in frame? 'full' enforces the device shape (220 x 120 = 26,400 electrodes, either orientation) and corrects a near miss; 'partial' enforces nothing but the 17.5 um scale, and 1-1 is the top-left of the imaged region. R45.8 \u2014 he picks before mapping; the server default is the assumption-free one.",
            "default": "partial"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "analysis_id"
        ],
        "title": "VideoElectrodeMapRequest",
        "description": "`POST /api/videomosaic/electrodes/map` \u2014 fit the grid of the saved video mosaic."
      },
      "VideoKeyframeRecord": {
        "properties": {
          "x": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "X"
          },
          "y": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Y"
          },
          "segment": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Segment"
          },
          "reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Reason"
          },
          "placed": {
            "type": "boolean",
            "title": "Placed",
            "default": false
          }
        },
        "additionalProperties": true,
        "type": "object",
        "title": "VideoKeyframeRecord",
        "description": "One automatically selected video frame. `x`/`y` are canvas top-left corners (R19),\nnull when the frame could not be tied to the mosaic (`placed: false`)."
      },
      "VideoMosaicBuildResult": {
        "properties": {
          "kind": {
            "type": "string",
            "const": "videomosaic_build",
            "title": "Kind",
            "default": "videomosaic_build"
          },
          "doc": {
            "$ref": "#/components/schemas/VideoMosaicDocument"
          },
          "canvas": {
            "$ref": "#/components/schemas/VideoCanvasSize"
          },
          "stats": {
            "additionalProperties": true,
            "type": "object",
            "title": "Stats"
          },
          "outputs": {
            "additionalProperties": {
              "type": "string"
            },
            "type": "object",
            "title": "Outputs",
            "description": "logical name -> FILENAME of each artifact (mosaic, preview, positions, build). They sit in the project folder itself, named after the project. Filenames, not paths: a saved project moves (R43), and a recorded absolute path would go stale."
          }
        },
        "type": "object",
        "required": [
          "doc",
          "canvas",
          "stats",
          "outputs"
        ],
        "title": "VideoMosaicBuildResult",
        "description": "`job.result` of a `videomosaic_build` job. The document is already SAVED server-side\nwhen this appears \u2014 the UI refetches or takes `doc` as-is; it never authors."
      },
      "VideoMosaicDocument": {
        "properties": {
          "schema_version": {
            "type": "string",
            "title": "Schema Version"
          },
          "app": {
            "$ref": "#/components/schemas/AppStamp"
          },
          "id": {
            "type": "string",
            "title": "Id"
          },
          "feature": {
            "type": "string",
            "title": "Feature"
          },
          "dataset": {
            "type": "string",
            "title": "Dataset"
          },
          "experiment": {
            "type": "string",
            "title": "Experiment",
            "default": ""
          },
          "data_dir": {
            "type": "string",
            "title": "Data Dir"
          },
          "dataset_key": {
            "type": "string",
            "title": "Dataset Key"
          },
          "created": {
            "type": "string",
            "title": "Created"
          },
          "modified": {
            "type": "string",
            "title": "Modified"
          },
          "provenance": {
            "$ref": "#/components/schemas/Provenance"
          },
          "source": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/VideoSource"
              },
              {
                "type": "null"
              }
            ]
          },
          "config": {
            "anyOf": [
              {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "number"
                    },
                    {
                      "type": "integer"
                    },
                    {
                      "type": "boolean"
                    },
                    {
                      "type": "string"
                    },
                    {
                      "additionalProperties": {
                        "anyOf": [
                          {
                            "type": "number"
                          },
                          {
                            "type": "integer"
                          },
                          {
                            "type": "boolean"
                          },
                          {
                            "type": "string"
                          },
                          {
                            "type": "null"
                          }
                        ]
                      },
                      "type": "object"
                    },
                    {
                      "items": {
                        "anyOf": [
                          {
                            "type": "number"
                          },
                          {
                            "type": "integer"
                          },
                          {
                            "type": "boolean"
                          },
                          {
                            "type": "string"
                          },
                          {
                            "type": "null"
                          }
                        ]
                      },
                      "type": "array"
                    },
                    {
                      "type": "null"
                    }
                  ]
                },
                "type": "object"
              },
              {
                "type": "null"
              }
            ],
            "title": "Config"
          },
          "build": {
            "anyOf": [
              {
                "additionalProperties": true,
                "type": "object"
              },
              {
                "type": "null"
              }
            ],
            "title": "Build",
            "description": "{built_at, canvas, outputs, stats} \u2014 the last build's summary; the full forensics live in outputs/build.json."
          },
          "keyframes": {
            "additionalProperties": {
              "$ref": "#/components/schemas/VideoKeyframeRecord"
            },
            "type": "object",
            "title": "Keyframes"
          },
          "electrodes": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/ElectrodeMapBlock"
              },
              {
                "type": "null"
              }
            ],
            "description": "The fitted electrode-grid summary (2026-08-11); stale when its source_stamp no longer equals build.built_at. None until Map electrodes runs."
          },
          "regions": {
            "items": {
              "$ref": "#/components/schemas/RegionRecord"
            },
            "type": "array",
            "title": "Regions",
            "description": "Fixed-field calcium recordings located on this mosaic (2026-08-11), newest last. Empty until Locate regions runs."
          }
        },
        "additionalProperties": true,
        "type": "object",
        "required": [
          "schema_version",
          "app",
          "id",
          "feature",
          "dataset",
          "data_dir",
          "dataset_key",
          "created",
          "modified",
          "provenance"
        ],
        "title": "VideoMosaicDocument",
        "description": "A videomosaic document: envelope + `source`/`config`/`build`/`keyframes`, flat."
      },
      "VideoProbeRequest": {
        "properties": {
          "path": {
            "type": "string",
            "title": "Path",
            "description": "Path of a video file (mp4/avi/\u2026) \u2014 anything cv2 decodes."
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "path"
        ],
        "title": "VideoProbeRequest"
      },
      "VideoSource": {
        "properties": {
          "path": {
            "type": "string",
            "title": "Path"
          },
          "name": {
            "type": "string",
            "title": "Name"
          },
          "width": {
            "type": "integer",
            "title": "Width"
          },
          "height": {
            "type": "integer",
            "title": "Height"
          },
          "fps": {
            "type": "number",
            "title": "Fps"
          },
          "n_frames": {
            "type": "integer",
            "title": "N Frames"
          },
          "duration_s": {
            "type": "number",
            "title": "Duration S"
          },
          "size_bytes": {
            "type": "integer",
            "title": "Size Bytes"
          }
        },
        "type": "object",
        "required": [
          "path",
          "name",
          "width",
          "height",
          "fps",
          "n_frames",
          "duration_s",
          "size_bytes"
        ],
        "title": "VideoSource",
        "description": "The probe receipt. `fps`/`n_frames` are what the container CLAIMS (phone video is often\nVFR) \u2014 good for a receipt, never for arithmetic. Probing decodes a real frame, so a receipt\nmeans 'this will decode', not 'a header parsed'."
      }
    }
  }
}